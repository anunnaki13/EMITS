---
phase: 01-production-audit-onboarding
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - pltu-tenayan-full-backup/docs/audit/FRONTEND_MAP.md
autonomous: true
requirements:
  - AUDIT-03
must_haves:
  truths:
    - "A reviewer can open FRONTEND_MAP.md and see every frontend route declared in App.js with its page component file."
    - "Each page lists the /api/* endpoints it consumes, derived from grepping the page source for axios / fetch calls."
    - "Each route is marked as protected (wrapped in ProtectedRoute) or public, identifying the auth boundary."
    - "AuthContext's auth surface (login POST, /api/auth/me rehydrate, getAuthHeader) is documented in a dedicated 'Auth boundary' section, including where the JWT is stored."
    - "Layout/sidebar navigation entries are cross-referenced to routes — any nav entry pointing at a route that does not exist (or vice versa) is flagged."
  artifacts:
    - path: pltu-tenayan-full-backup/docs/audit/FRONTEND_MAP.md
      provides: "Frontend route → page component → consumed /api/* endpoints map, with auth boundary called out."
      contains: "| Route |"
      min_lines: 80
  key_links:
    - from: pltu-tenayan-full-backup/docs/audit/FRONTEND_MAP.md
      to: pltu-tenayan-full-backup/frontend/src/App.js
      via: "Route table is derived from <Route> declarations in App.js."
      pattern: "App.js"
    - from: pltu-tenayan-full-backup/docs/audit/FRONTEND_MAP.md
      to: pltu-tenayan-full-backup/frontend/src/contexts/AuthContext.js
      via: "Auth boundary section documents the AuthContext surface."
      pattern: "AuthContext"
    - from: pltu-tenayan-full-backup/docs/audit/FRONTEND_MAP.md
      to: pltu-tenayan-full-backup/docs/audit/ENDPOINT_AUDIT.md
      via: "Each consumed endpoint should appear in the live ENDPOINT_AUDIT inventory; mismatches are flagged."
      pattern: "ENDPOINT_AUDIT"
---

<objective>
Produce FRONTEND_MAP.md: for every frontend route declared in App.js, map its page component file and the set of /api/* endpoints that page calls. Mark each route protected/public, document the auth boundary (AuthContext + ProtectedRoute), and cross-check sidebar nav links against actual routes.

Purpose: Phase 2 (Authentication Stabilization) needs to know exactly which pages depend on which auth-gated endpoints to write regression tests. Phase 3 (Documentation Refresh) needs an authoritative page-level surface to keep README/documentation.md aligned. Phase 7 (refactor) needs the per-page endpoint list before splitting page files.
Output: pltu-tenayan-full-backup/docs/audit/FRONTEND_MAP.md (markdown, table-driven, grep-verifiable).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/intel/context.md
@pltu-tenayan-full-backup/frontend/src/App.js
@pltu-tenayan-full-backup/frontend/src/contexts/AuthContext.js
@pltu-tenayan-full-backup/frontend/src/components/Layout.js

<interfaces>
<!-- Frontend conventions every page follows (per intel/context.md) -->
- All API calls go through process.env.REACT_APP_BACKEND_URL with /api prefix (CONS-api-base + IMPLICIT-006).
- Auth state lives in AuthContext.js: reads token from localStorage, validates via GET /api/auth/me, exposes getAuthHeader().
- Protected paths are wrapped in <ProtectedRoute>.
- Pages live under frontend/src/pages/ — one file per domain.

Known page set (from intel/context.md TOPIC: Project Structure):
  Login, Dashboard, Vessel, Barge, Trucking, Biomassa, POBatubara, MeritOrder,
  SmartStock, SumberPemakaian, SmartBlending, AIIntelligence, Laporan,
  COAReconciliation, DisputeMonitor, Settings.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the route table from App.js and the per-page endpoint call list</name>
  <files>pltu-tenayan-full-backup/docs/audit/.work/routes.txt, pltu-tenayan-full-backup/docs/audit/.work/page-endpoints.txt, pltu-tenayan-full-backup/docs/audit/.work/sidebar-links.txt</files>
  <read_first>
    - pltu-tenayan-full-backup/frontend/src/App.js (full file — every Route, every ProtectedRoute wrap)
    - pltu-tenayan-full-backup/frontend/src/contexts/AuthContext.js (full file — login flow, /api/auth/me rehydrate, getAuthHeader, token storage)
    - pltu-tenayan-full-backup/frontend/src/components/Layout.js (sidebar / nav menu — extract every Link / NavLink href)
    - pltu-tenayan-full-backup/frontend/src/pages/ (directory listing — confirm page files exist before reading them)
  </read_first>
  <action>
    Capture three flat-text inputs that the next task will weave into FRONTEND_MAP.md.

    1. mkdir -p pltu-tenayan-full-backup/docs/audit/.work

    2. Extract the route table from App.js. Do NOT regex by hand on the file in this plan — the executor reads App.js and emits a TSV with one row per Route declaration:
       path&lt;TAB&gt;component&lt;TAB&gt;protected
       e.g.
       /&lt;TAB&gt;Dashboard&lt;TAB&gt;true
       /login&lt;TAB&gt;Login&lt;TAB&gt;false
       /vessel&lt;TAB&gt;Vessel&lt;TAB&gt;true
       …
       Save to pltu-tenayan-full-backup/docs/audit/.work/routes.txt.

       Determination rules:
       - "protected" = the &lt;Route&gt; element (or its children) is wrapped in &lt;ProtectedRoute&gt; OR uses an `element` prop whose value passes through ProtectedRoute. If unclear, mark `unknown` and add a note in column 4.
       - The component column is the imported page component name (e.g., "Vessel", "COAReconciliation"), not its file path.

    3. For each unique page component referenced in routes.txt, find its source file (canonical location: frontend/src/pages/&lt;Component&gt;.js — fall back to .jsx / .tsx / index.js if needed) and extract the set of /api/* endpoints it calls. Use this grep against each page file:

       grep -nE "(axios|fetch|apiClient|api)\.(get|post|put|delete|patch)\s*\(|`\$\{[A-Z_]*BACKEND_URL\}/api/|\"/api/|'/api/" pltu-tenayan-full-backup/frontend/src/pages/&lt;Component&gt;.js

       Then derive the endpoint paths from those matches. A match like  axios.get(`${API}/api/vessels?page=${p}&page_size=${ps}`)  yields endpoint `/api/vessels`. Strip query strings and concrete id values; preserve `{id}` style placeholders where the call uses a variable.

       Write one TSV file with one row per (page, endpoint) pair:
       page&lt;TAB&gt;method&lt;TAB&gt;endpoint&lt;TAB&gt;source-line
       e.g.
       Vessel&lt;TAB&gt;GET&lt;TAB&gt;/api/vessels&lt;TAB&gt;Vessel.js:42
       Vessel&lt;TAB&gt;POST&lt;TAB&gt;/api/upload/vessel&lt;TAB&gt;Vessel.js:118
       …
       Save to pltu-tenayan-full-backup/docs/audit/.work/page-endpoints.txt.

       If a page calls a URL the executor cannot statically resolve to a /api/* path (e.g., URL is built dynamically via a helper), record it as `&lt;dynamic&gt;` in the endpoint column with the source-line reference, so Task 2 can flag it.

    4. Extract sidebar / nav entries from Layout.js. One row per Link/NavLink:
       label&lt;TAB&gt;href&lt;TAB&gt;icon-or-other-note
       Save to pltu-tenayan-full-backup/docs/audit/.work/sidebar-links.txt.

    5. Sanity-check counts:
       wc -l pltu-tenayan-full-backup/docs/audit/.work/routes.txt
       wc -l pltu-tenayan-full-backup/docs/audit/.work/page-endpoints.txt
       wc -l pltu-tenayan-full-backup/docs/audit/.work/sidebar-links.txt

    Do NOT modify any source under frontend/. Read-only audit.
  </action>
  <verify>
    <automated>test -s pltu-tenayan-full-backup/docs/audit/.work/routes.txt &amp;&amp; test -s pltu-tenayan-full-backup/docs/audit/.work/page-endpoints.txt &amp;&amp; test -s pltu-tenayan-full-backup/docs/audit/.work/sidebar-links.txt &amp;&amp; [ "$(wc -l &lt; pltu-tenayan-full-backup/docs/audit/.work/routes.txt)" -ge 10 ] &amp;&amp; [ "$(wc -l &lt; pltu-tenayan-full-backup/docs/audit/.work/page-endpoints.txt)" -ge 10 ]</automated>
  </verify>
  <acceptance_criteria>
    - routes.txt has at least 10 rows (the project ships at least 16 pages per intel/context.md; 10 is a low-bar smoke test).
    - Every row in routes.txt has 3 tab-separated columns; column 3 ("protected") is one of: true, false, unknown.
    - page-endpoints.txt has at least 10 rows; every row's third column either starts with `/api/` or equals `&lt;dynamic&gt;`.
    - sidebar-links.txt is non-empty (Layout.js declares at least the per-domain navigation set).
    - No frontend source file was modified.
  </acceptance_criteria>
  <done>The route table, page-endpoint call list, and sidebar nav list are captured to .work/ as flat-text inputs ready for Task 2 to weave together.</done>
</task>

<task type="auto">
  <name>Task 2: Write FRONTEND_MAP.md weaving routes, pages, endpoints, and the auth boundary</name>
  <files>pltu-tenayan-full-backup/docs/audit/FRONTEND_MAP.md</files>
  <read_first>
    - pltu-tenayan-full-backup/docs/audit/.work/routes.txt
    - pltu-tenayan-full-backup/docs/audit/.work/page-endpoints.txt
    - pltu-tenayan-full-backup/docs/audit/.work/sidebar-links.txt
    - pltu-tenayan-full-backup/frontend/src/contexts/AuthContext.js (re-read — Task 1 only extracted; Task 2 documents the auth boundary in prose)
    - pltu-tenayan-full-backup/docs/audit/ENDPOINT_AUDIT.md ONLY IF the file already exists (cross-check pass; if it does not exist yet, skip the cross-check section and add a one-line note "ENDPOINT_AUDIT.md was not present at write time; cross-check deferred").
  </read_first>
  <action>
    Build pltu-tenayan-full-backup/docs/audit/FRONTEND_MAP.md with the following structure (in this order):

    ```
    # Frontend Map — Routes, Pages, Endpoints, Auth Boundary

    **Source tree:** pltu-tenayan-full-backup/frontend/src/
    **Captured:** &lt;ISO-8601 timestamp&gt;
    **Scope:** read-only audit. No frontend source file was modified.

    ## Route → Page → Endpoints

    | Route | Page component | Source file | Protected? | Endpoints consumed |
    |-------|----------------|-------------|------------|--------------------|

    Build one row per line in .work/routes.txt. The "Endpoints consumed" cell joins (with `, `) every endpoint from .work/page-endpoints.txt where column 1 (page) matches this row's page component. If the page emits no /api/* call, write "—". If any of its calls were `&lt;dynamic&gt;`, append " (+ dynamic call(s) — see Notes)".

    ## Per-page detail

    For each distinct page in routes.txt, render a subsection. Within each subsection:

    ### &lt;PageComponent&gt;
    - Route(s): &lt;list of routes mapped to this component&gt;
    - Source: frontend/src/pages/&lt;file&gt;
    - Protected: yes / no / unknown
    - Endpoints called (with method):
      - GET /api/&lt;path&gt;  (line N)
      - POST /api/&lt;path&gt; (line M)
      - …
    - Dynamic calls (could not statically resolve): line K — note context if visible.

    ## Auth boundary

    Document the auth surface the rest of the frontend depends on. Read AuthContext.js and write a prose section that names, for each of these, the actual function/exported symbol and the file:line reference:

    1. **Login submission** — which AuthContext function POSTs to /api/auth/login? File:line.
    2. **Token storage** — which storage mechanism (localStorage key name) holds the JWT? File:line.
    3. **Session rehydrate** — which function re-validates the token via GET /api/auth/me on mount? File:line.
    4. **getAuthHeader** — what does it return; how do pages consume it? File:line.
    5. **Logout** — which function clears the token; does it also clear server-side state? File:line.
    6. **ProtectedRoute** — which file declares it; what does it do when token is absent or rehydrate fails? File:line (look in App.js or a sibling component).

    Then a short "Auth boundary diagram" in markdown (no images):

    ```
    Page (e.g., Vessel.js)
       ↓ uses getAuthHeader()
    AuthContext (token from localStorage)
       ↓ Authorization: Bearer &lt;JWT&gt;
    /api/* (FastAPI)
    ```

    ## Sidebar / nav cross-check

    | Nav label | Nav href | Route exists? | Page component | Notes |
    |-----------|----------|---------------|----------------|-------|

    Build from .work/sidebar-links.txt. "Route exists?" = yes if the href matches a path in .work/routes.txt; otherwise "no — orphan nav entry".

    Then list:
    - Routes WITHOUT a corresponding nav entry (declared but not surfaced in the sidebar) — accessibility gap.
    - Nav entries WITHOUT a corresponding route — dead link.

    ## Cross-check vs ENDPOINT_AUDIT.md

    (Include this section only if pltu-tenayan-full-backup/docs/audit/ENDPOINT_AUDIT.md already exists. The two plans run in the same wave — if the executor is processing this plan in parallel and ENDPOINT_AUDIT.md is not yet on disk, write the placeholder note and continue.)

    For every endpoint listed in the per-page detail, look up its row in ENDPOINT_AUDIT.md's Inventory table. Render a small table:
    | Endpoint | Page(s) consuming | In live OpenAPI? | Live status |

    Flag rows where:
    - Page consumes endpoint, endpoint missing from live OpenAPI → "frontend-references-dead-endpoint"
    - Page consumes endpoint, endpoint live status starts with `broken-` → "frontend-uses-broken-endpoint"

    These two flags are critical Phase 2 inputs.

    ## Methodology

    Inputs in pltu-tenayan-full-backup/docs/audit/.work/.
    Endpoint extraction grep pattern recorded in this file's source for reproducibility.
    No mutation, no service restart.
    ```

    File must not contain credentials. After writing, run:
    grep -iE '(password|secret|bearer\s+[A-Za-z0-9._-]{10,})' pltu-tenayan-full-backup/docs/audit/FRONTEND_MAP.md
    Expected: no matches (occurrences in section titles like "**Token storage**" are fine because they are not key=value).

    Do NOT modify App.js, AuthContext.js, Layout.js, or any page file.
  </action>
  <verify>
    <automated>test -s pltu-tenayan-full-backup/docs/audit/FRONTEND_MAP.md &amp;&amp; [ "$(grep -v '^#' pltu-tenayan-full-backup/docs/audit/FRONTEND_MAP.md | grep -cE '^\| /')" -ge 10 ] &amp;&amp; grep -q '## Auth boundary' pltu-tenayan-full-backup/docs/audit/FRONTEND_MAP.md &amp;&amp; grep -q '## Sidebar / nav cross-check' pltu-tenayan-full-backup/docs/audit/FRONTEND_MAP.md &amp;&amp; grep -q '## Per-page detail' pltu-tenayan-full-backup/docs/audit/FRONTEND_MAP.md &amp;&amp; grep -qE '/api/auth/(login|me)' pltu-tenayan-full-backup/docs/audit/FRONTEND_MAP.md &amp;&amp; ! grep -iqE '(password\s*[:=]|bearer\s+[A-Za-z0-9._-]{10,})' pltu-tenayan-full-backup/docs/audit/FRONTEND_MAP.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at pltu-tenayan-full-backup/docs/audit/FRONTEND_MAP.md.
    - Route → Page → Endpoints table contains at least 10 rows whose Route column starts with `/`.
    - Per-page detail section exists and contains at least 10 distinct ### subsections (one per page component).
    - Auth boundary section names AuthContext functions for login, token storage, /api/auth/me rehydrate, getAuthHeader, logout, and ProtectedRoute — each with a file:line reference.
    - Sidebar / nav cross-check section exists with both directions of mismatch listed (orphan nav, route without nav).
    - Cross-check vs ENDPOINT_AUDIT.md section exists either with content (if ENDPOINT_AUDIT.md was present) or with the placeholder note (if it was not yet).
    - At least one row in the route table has Protected = `true` AND at least one row has Protected = `false` (the auth boundary is observable).
    - No credential value leaks into the file.
  </acceptance_criteria>
  <done>FRONTEND_MAP.md is a complete page-by-page surface map with the auth boundary documented and the nav layer cross-checked.</done>
</task>

</tasks>

<verification>
- FRONTEND_MAP.md was generated by reading App.js / AuthContext.js / Layout.js / pages/* and grepping each page for /api/* calls.
- Every route in App.js appears in the route table; every page component referenced has a per-page detail subsection.
- The auth boundary is documented with concrete file:line references for login, token storage, rehydrate, getAuthHeader, logout, and ProtectedRoute.
- Sidebar nav is cross-checked against the route table in both directions.
- File contains no credentials.
</verification>

<success_criteria>
A reviewer can open pltu-tenayan-full-backup/docs/audit/FRONTEND_MAP.md and answer Phase 1 success criterion #3:
1. Which frontend page consumes which API endpoints?
2. Where does the auth boundary live (which file, which functions, which storage)?
3. Which routes are protected vs public?
4. Where are dead links and orphan nav entries?
</success_criteria>

<output>
After completion, create `.planning/phases/01-production-audit-onboarding/01-03-SUMMARY.md` summarizing:
- Total routes mapped, total page components, total distinct endpoints called from frontend.
- List of pages with NO /api/* calls (likely pure presentational — sanity check).
- List of pages with `&lt;dynamic&gt;` calls (Phase 2/4 needs to revisit these for static call discovery).
- Auth boundary surface in 5 lines: login function, token storage key, rehydrate function, getAuthHeader name, ProtectedRoute file.
- Any orphan nav entries or routes without nav entries.
- If ENDPOINT_AUDIT.md was present at write time: count of frontend-references-dead-endpoint and frontend-uses-broken-endpoint flags.
</output>
