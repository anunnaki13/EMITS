---
phase: 07-upgrade-backlog-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pltu-tenayan-full-backup/backend/server.py
  - pltu-tenayan-full-backup/backend/routers/auth.py
  - pltu-tenayan-full-backup/backend/models/__init__.py
  - pltu-tenayan-full-backup/backend/utils/auth.py
autonomous: true
requirements: [UPGRADE-01, UPGRADE-02, UPGRADE-03]
tags: [backend, fastapi, auth-router, models, refactor]

must_haves:
  truths:
    - "/api/auth/register, /api/auth/login, /api/auth/me, and /api/users paths remain unchanged"
    - "Malformed /api/auth/* request bodies still return HTTP 400"
    - "Missing Authorization header on /api/auth/me still returns HTTP 403"
    - "Auth helpers have one canonical import path used by later routers"
    - "server.py no longer contains inline auth route handlers"
  artifacts:
    - path: "pltu-tenayan-full-backup/backend/routers/auth.py"
      provides: "Canonical auth/users router"
    - path: "pltu-tenayan-full-backup/backend/models/__init__.py"
      provides: "Auth Pydantic models imported by auth router"
---

<objective>
Extract auth and users endpoints from `server.py` into a mounted FastAPI router while preserving the exact existing auth contract.

Purpose: Establish the canonical dependency/helper pattern for the rest of Phase 7 router extraction.
Output: Auth/users router mounted once, auth models outside `server.py`, existing auth tests passing unchanged.
</objective>

<context>
@.planning/phases/07-upgrade-backlog-foundation/07-CONTEXT.md
@.planning/phases/07-upgrade-backlog-foundation/07-RESEARCH.md
@.planning/decisions/ADR-004-jwt-bcrypt-three-role-auth.md
@pltu-tenayan-full-backup/backend/server.py
@pltu-tenayan-full-backup/backend/routers/auth.py
@pltu-tenayan-full-backup/backend/tests/test_auth_session.py
@pltu-tenayan-full-backup/backend/tests/test_auth_roles.py
</context>

<tasks>

<task type="auto">
  <name>Task 07-01-01: Reconcile and mount auth router</name>
  <files>backend/server.py, backend/routers/auth.py, backend/utils/auth.py</files>
  <action>
    Compare current auth code in `server.py` with `backend/routers/auth.py`. Update the router to match current behavior exactly, then mount it under the existing `/api` prefix. Remove the duplicate inline auth handlers from `server.py` only after the mounted router passes the same paths.
  </action>
  <verify>
    <automated>cd pltu-tenayan-full-backup/backend && ./.venv/bin/python -m py_compile server.py routers/auth.py</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 07-01-02: Preserve auth validation and regression tests</name>
  <files>backend/server.py, backend/tests/test_auth_session.py, backend/tests/test_auth_roles.py</files>
  <action>
    Ensure the app-level `RequestValidationError` handler still scopes `/api/auth/*` validation failures to HTTP 400 after router extraction. Do not change test expectations unless the existing code is demonstrably wrong.
  </action>
  <verify>
    <automated>cd pltu-tenayan-full-backup/backend && AI_FAKE=1 ./.venv/bin/pytest tests/test_auth_session.py tests/test_auth_roles.py -q</automated>
  </verify>
</task>

</tasks>
