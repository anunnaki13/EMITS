---
phase: 03-documentation-refresh-decision-lock-in
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pltu-tenayan-full-backup/LOCAL_SETUP.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - "LOCAL_SETUP.md has a new H2 section 'VPS Service Recovery (post-restart)' containing the exact uvicorn + yarn start commands plus a curl /api/health verification step (per D-11)"
    - "ROADMAP.md Phase-2 success criterion 3 is amended from 'operator-only upload' to 'admin+operator upload (viewer denied)' citing test_auth_roles.py (per D-13)"
    - "REQUIREMENTS.md AUTHFIX-01/02/04/05 checkboxes are flipped to [x] and the traceability table statuses are updated to Complete (per SS-04 carry-forward)"
    - "ROADMAP.md Progress table for Phase 2 shows 4/4 plans complete with status Complete and date 2026-05-10"
    - "ROADMAP.md AUDIT-01 / AUDIT-02 plan-list checkboxes (lines 36-37) are also flipped to [x] per Phase-1 carry-forward note in ROADMAP line 15"
  artifacts:
    - path: "pltu-tenayan-full-backup/LOCAL_SETUP.md"
      provides: "Existing local-setup runbook PLUS a new 'VPS Service Recovery (post-restart)' H2 with exact recovery commands"
      min_lines: 260
      contains: "VPS Service Recovery"
    - path: ".planning/ROADMAP.md"
      provides: "Phase-2 success criterion 3 wording fix + Progress table flipped + plan-list checkboxes synced for Phases 1-2"
      contains: "admin+operator upload"
    - path: ".planning/REQUIREMENTS.md"
      provides: "AUTHFIX-01/02/04/05 checkboxes flipped + traceability table statuses synced to Complete"
      contains: "AUTHFIX-04"
  key_links:
    - from: "LOCAL_SETUP.md §VPS Service Recovery (post-restart)"
      to: "pltu-tenayan-full-backup/docs/audit/LOGIN_BUG_RESOLUTION.md (lines 26-42, the seed for this runbook)"
      via: "explicit cite at the top of the new section"
      pattern: "LOGIN_BUG_RESOLUTION\\.md"
    - from: "ROADMAP.md Phase-2 success criterion 3"
      to: "pltu-tenayan-full-backup/backend/tests/test_auth_roles.py (test_upload_vessel_role_gate)"
      via: "test name cite in the amended sentence"
      pattern: "test_upload_vessel_role_gate"
    - from: "REQUIREMENTS.md AUTHFIX-04 row"
      to: "pltu-tenayan-full-backup/docs/audit/LOGIN_BUG_RESOLUTION.md (mitigated disposition)"
      via: "Complete (mitigated) status with cite"
      pattern: "LOGIN_BUG_RESOLUTION\\.md"
---

<objective>
Three carry-forward doc edits that close the Phase-2 → Phase-3 transition cleanly and unblock Wave-2 plans (Plan 04 Known Issues will reference the recovery runbook; Plan 05 DEPLOYMENT_GUIDE reconciliation will cross-link to it):

1. **VPS Service Recovery runbook** in `pltu-tenayan-full-backup/LOCAL_SETUP.md` per D-11 — promote the uvicorn + yarn-start procedure from `LOGIN_BUG_RESOLUTION.md` "VPS-restart note" into a permanent operator runbook.
2. **ROADMAP wording fix** per D-13 — Phase-2 success criterion 3 says "operator-only upload" but the test parametrize at `test_upload_vessel_role_gate` proves admin shadows operator (admin→400, operator→400, viewer→403). Amend to "admin+operator upload (viewer denied)" with a one-line citation. (Phase-2 verifier flagged this as SS-03; D-13 closes it.)
3. **REQUIREMENTS / ROADMAP sync** for the Phase-2 SS-04 doc-state drift — tick AUTHFIX-01/02/04/05 boxes, flip the traceability table for those four IDs to Complete, flip ROADMAP Progress table for Phase 2 to 4/4 + Complete + 2026-05-10. While editing ROADMAP, also tick AUDIT-01 and AUDIT-02 (Phase-1 deferred per ROADMAP line 15).

Purpose: Phase 2 verifier explicitly flagged each of these (SS-03, SS-04 in `02-VERIFICATION.md`). Plan-04 Known Issues entries (login bug status; service-recovery runbook reference) and Plan-05 DEPLOYMENT_GUIDE.md cross-link both depend on these existing first.

Output: One inner-repo commit on `pltu-tenayan-full-backup` (LOCAL_SETUP.md), one outer-repo commit on `/home/damnation/emits` (ROADMAP.md + REQUIREMENTS.md). No code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-documentation-refresh-decision-lock-in/03-CONTEXT.md
@.planning/phases/02-authentication-stabilization/VERIFICATION.md
@pltu-tenayan-full-backup/LOCAL_SETUP.md
@pltu-tenayan-full-backup/docs/audit/LOGIN_BUG_RESOLUTION.md

<interfaces>
<!-- LOCAL_SETUP.md current outline (head -40 confirmed at plan-time): -->
<!-- # LOCAL_SETUP.md -->
<!-- ## 1. Isi Backup -->
<!-- ## 2. Prasyarat Lokal -->
<!-- ## 3. Ekstrak Backup -->
<!-- ## 4. Restore Database -->
<!-- ## 4.1 Ekstrak database backup -->
<!-- ... (230 lines total) -->

<!-- The new H2 should be appended at the END of the file (after whatever the last numbered section is) -->
<!-- as a new top-level H2 (NOT under section 4 / 5 / etc.). Title literal: -->
<!-- ## VPS Service Recovery (post-restart) -->

<!-- The recovery procedure is verbatim from LOGIN_BUG_RESOLUTION.md lines 26-42 -->
<!-- (which the read_first will pick up). Reproduce the bash block; expand it to -->
<!-- include: -->
<!--   - source `.venv/bin/activate` for backend -->
<!--   - export from .env (note: do NOT inline credentials — `set -a; . backend/.env; set +a` pattern) -->
<!--   - the actual port numbers (backend 8013, frontend 3013) per LOGIN_BUG_RESOLUTION.md -->
<!--   - `curl -fsS http://localhost:8013/api/health` as the verification step -->
<!--   - a short note that systemd / pm2 auto-restart units are deferred per D-12 (a future Phase 3.1 INSERTED phase if needed) -->

<!-- ROADMAP wording fix (D-13) — the line to change: -->
<!-- Original (line 48 of ROADMAP.md): -->
<!--   3. Admin/operator/viewer role enforcement is observably correct on at least one endpoint per role tier (e.g., admin-only delete-all, operator-only upload, viewer read). -->
<!-- Amended target: -->
<!--   3. Admin/operator/viewer role enforcement is observably correct on at least one endpoint per role tier (e.g., admin-only delete-all, admin+operator upload (viewer denied — `test_upload_vessel_role_gate` in `pltu-tenayan-full-backup/backend/tests/test_auth_roles.py`), viewer read). -->

<!-- REQUIREMENTS.md changes (lines 19-23 + traceability table 118-122): -->
<!--   - line 19: AUTHFIX-01 [ ] → [x] (status: Complete; closed Phase 2 plan 02-02) -->
<!--   - line 20: AUTHFIX-02 [ ] → [x] (status: Complete; closed Phase 2 plan 02-02) -->
<!--   - line 21: AUTHFIX-03 already [x] -->
<!--   - line 22: AUTHFIX-04 [ ] → [x] (status: Complete (mitigated); closed Phase 2 plan 02-04 — cite LOGIN_BUG_RESOLUTION.md) -->
<!--   - line 23: AUTHFIX-05 [ ] → [x] (status: Complete; closed Phase 2 plan 02-01) -->
<!--   - traceability table lines 118-122: AUTHFIX-01 Pending → Complete; AUTHFIX-02 Pending → Complete; AUTHFIX-04 Pending → Complete (mitigated); AUTHFIX-05 Pending → Complete -->

<!-- ROADMAP.md Progress table (line 141): -->
<!--   Phase 2: 0/4 → 4/4; Not started → Complete; - → 2026-05-10 -->
<!-- ROADMAP.md Phase-1 plan-list lines 36-37 (per ROADMAP line 15 deferred-checkbox-sync note): -->
<!--   01-01-PLAN.md [ ] → [x] -->
<!--   01-02-PLAN.md [ ] → [x] -->
<!-- ROADMAP.md Progress table (line 140) Phase 1: 4/4 / Complete / 2026-05-10 — already shows Complete; verify before editing. -->

<!-- Inner-vs-outer repo split (Phase-2 lesson): -->
<!-- LOCAL_SETUP.md → INNER repo (pltu-tenayan-full-backup/) — commit via `git -C pltu-tenayan-full-backup ...`. Pre-commit hook runs `scripts/check_credentials.sh` — runbook MUST NOT inline JWT/admin password/MongoDB-with-creds. The `set -a; . backend/.env; set +a` pattern is the safe approach. -->
<!-- ROADMAP.md + REQUIREMENTS.md → OUTER repo (/home/damnation/emits) — commit via `git add` from /home/damnation/emits. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append "VPS Service Recovery (post-restart)" H2 to LOCAL_SETUP.md and commit (inner repo)</name>
  <files>
    - pltu-tenayan-full-backup/LOCAL_SETUP.md
  </files>
  <read_first>
    - `pltu-tenayan-full-backup/LOCAL_SETUP.md` (full file, 230 lines — one Read).
    - `pltu-tenayan-full-backup/docs/audit/LOGIN_BUG_RESOLUTION.md` lines 26-42 ONLY (the recovery procedure seed). Use Read with offset=26, limit=20.
  </read_first>
  <action>
    1. Locate the end of the existing LOCAL_SETUP.md (last numbered H2 section is the last anchor — append AFTER it, not inside it).
    2. Append the following new H2 section verbatim. Replace `${BACKEND_PORT}`/`${FRONTEND_PORT}` with the actual ports (8013 / 3013) — they are not secrets:

    ```markdown

    ## VPS Service Recovery (post-restart)

    Promoted from `docs/audit/LOGIN_BUG_RESOLUTION.md` (Phase-2 plan 02-04, "VPS-restart note") per
    Phase-3 D-11. Use this runbook when the VPS at `103.150.197.225` reboots and the FastAPI
    backend (port 8013) and CRA frontend (port 3013) are no longer responding.

    ### When to use

    - `curl -fsS http://103.150.197.225:8013/api/health` returns connection-refused or non-2xx.
    - The frontend at `http://103.150.197.225:3013/` shows a connection-refused / proxy error
      after a host reboot.

    ### 1. Restore the backend (FastAPI / uvicorn on port 8013)

    ```bash
    cd /home/damnation/emits/pltu-tenayan-full-backup/backend

    # Activate the project venv
    source .venv/bin/activate

    # Source the backend environment (JWT_SECRET, MONGO_URL, DB_NAME, CORS_ORIGINS, EMERGENT_LLM_KEY, ...)
    # Use `set -a` so the values export into the subprocess; never echo them.
    set -a
    . ./.env
    set +a

    # Start uvicorn on the production port. `--reload` is fine for the VPS dev posture today.
    nohup uvicorn server:app --host 0.0.0.0 --port 8013 --reload \
      > /tmp/emits-backend.log 2>&1 &

    # Verify
    sleep 2
    curl -fsS http://localhost:8013/api/health
    # Expected: HTTP 200 + JSON body with status indicator
    ```

    ### 2. Restore the frontend (CRA / craco on port 3013)

    ```bash
    cd /home/damnation/emits/pltu-tenayan-full-backup/frontend

    # The frontend's REACT_APP_BACKEND_URL is read from frontend/.env at build/start time.
    # Confirm it points at port 8013 (not 8001) before starting; if it doesn't, edit .env first.
    grep -E '^REACT_APP_BACKEND_URL=' .env

    nohup yarn start \
      > /tmp/emits-frontend.log 2>&1 &

    # CRA will print a "Compiled successfully!" line in the log when ready (typically 30-90s on VPS).
    # Verify
    curl -fsS http://localhost:3013/ | head -1
    # Expected: HTML doctype line
    ```

    ### 3. Smoke-test login end-to-end

    ```bash
    # Source admin credentials from the local (gitignored) memory/test_credentials.md
    cd /home/damnation/emits/pltu-tenayan-full-backup
    export TEST_ADMIN_EMAIL="$(awk '/^## Akun Admin$/,/^##/{ if(/Email:/){sub(/^- Email:[[:space:]]*/,"");print;exit} }' memory/test_credentials.md)"
    export TEST_ADMIN_PASSWORD="$(awk '/^## Akun Admin$/,/^##/{ if(/Password:/){sub(/^- Password:[[:space:]]*/,"");print;exit} }' memory/test_credentials.md)"

    # Login probe — expect HTTP 200 + access_token in response
    curl -fsS -X POST http://localhost:8013/api/auth/login \
      -H "Content-Type: application/json" \
      -d "$(python3 -c "import json,os;print(json.dumps({'email':os.environ['TEST_ADMIN_EMAIL'],'password':os.environ['TEST_ADMIN_PASSWORD']}))")" \
      | python3 -c "import json,sys; d=json.load(sys.stdin); print('OK' if d.get('access_token') else 'FAIL'); print('user:', d.get('user', {}).get('email'))"

    unset TEST_ADMIN_EMAIL TEST_ADMIN_PASSWORD
    ```

    ### Auto-restart units (deferred — Phase 3.1 if requested)

    Per D-12, systemd / pm2 unit files for backend (`uvicorn :8013`) and frontend (`yarn start :3013`)
    are NOT shipped in Phase 3. If the operator wants the services to auto-restart on VPS reboot
    instead of requiring this manual runbook, file a Phase 3.1 INSERTED phase per the ROADMAP
    decimal-phase convention.

    ### Cross-references

    - Original procedure: `pltu-tenayan-full-backup/docs/audit/LOGIN_BUG_RESOLUTION.md` "VPS-restart note (Phase-3 follow-up trigger)" (lines 26-42).
    - Backend env contract: `backend/.env` (gitignored; `JWT_SECRET`, `MONGO_URL`, `DB_NAME`, `CORS_ORIGINS`, optional `EMERGENT_LLM_KEY`).
    - Credential hygiene: `docs/audit/CREDENTIAL_HYGIENE.md` — never inline JWTs, MongoDB URIs with credentials, or admin passwords in this runbook.
    ```

    3. Save the file. Verify the credential scanner does not flag the new content:
    ```
    cd pltu-tenayan-full-backup && bash scripts/check_credentials.sh
    ```
    Expected exit 0.

    4. Commit on the inner repo:
    ```
    git -C pltu-tenayan-full-backup add LOCAL_SETUP.md
    git -C pltu-tenayan-full-backup commit -m "docs(docs-01): add VPS Service Recovery runbook to LOCAL_SETUP.md (D-11)"
    ```
    The pre-commit hook will run `scripts/check_credentials.sh` automatically.
  </action>
  <verify>
    <automated>cd pltu-tenayan-full-backup && grep -q '^## VPS Service Recovery (post-restart)$' LOCAL_SETUP.md && grep -q 'curl -fsS http://localhost:8013/api/health' LOCAL_SETUP.md && grep -q 'LOGIN_BUG_RESOLUTION\.md' LOCAL_SETUP.md && bash scripts/check_credentials.sh && git log -1 --pretty=format:%s -- LOCAL_SETUP.md | grep -q 'docs(docs-01).*VPS Service Recovery' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `LOCAL_SETUP.md` contains a literal `## VPS Service Recovery (post-restart)` H2.
    - The runbook contains both the uvicorn command and `yarn start`.
    - The runbook contains a `curl /api/health` verification step.
    - The runbook cites `LOGIN_BUG_RESOLUTION.md`.
    - `scripts/check_credentials.sh` exits 0 after the change (pre-commit hook proves this on commit).
    - Inner-repo commit subject starts `docs(docs-01): add VPS Service Recovery runbook`.
  </acceptance_criteria>
  <done>
    LOCAL_SETUP.md contains the new H2; inner-repo commit landed; credential scanner clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Amend ROADMAP wording (D-13) + sync ROADMAP/REQUIREMENTS for Phase-2 closure (SS-04) and commit (outer repo)</name>
  <files>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - `.planning/ROADMAP.md` (already in context — do NOT re-read; reference the line numbers from the interfaces block).
    - `.planning/REQUIREMENTS.md` (already in context — do NOT re-read).
  </read_first>
  <action>
    Make the edits below using the `Edit` tool. Each edit is a small, scoped string replace.

    **A. ROADMAP.md edits:**

    1. Line ~48 (Phase 2 Success Criterion 3) — change:
    ```
      3. Admin/operator/viewer role enforcement is observably correct on at least one endpoint per role tier (e.g., admin-only delete-all, operator-only upload, viewer read).
    ```
    to:
    ```
      3. Admin/operator/viewer role enforcement is observably correct on at least one endpoint per role tier (e.g., admin-only delete-all, admin+operator upload with viewer denied — proven by `test_upload_vessel_role_gate` in `pltu-tenayan-full-backup/backend/tests/test_auth_roles.py`, viewer-readable list endpoints).
    ```

    2. Lines 36-37 (Phase 1 Plans list) — flip checkboxes:
    ```
      - [ ] 01-01-PLAN.md — Endpoint audit (AUDIT-01) → ENDPOINT_AUDIT.md
      - [ ] 01-02-PLAN.md — Collection / data audit (AUDIT-02) → DATA_AUDIT.md
    ```
    to:
    ```
      - [x] 01-01-PLAN.md — Endpoint audit (AUDIT-01) → ENDPOINT_AUDIT.md
      - [x] 01-02-PLAN.md — Collection / data audit (AUDIT-02) → DATA_AUDIT.md
    ```

    3. Lines 14-16 (Phase 1 status note) — REMOVE the now-obsolete "but 01-01 / 01-02 plan checkboxes below remain unflipped — checkbox sync deferred" parenthetical. Original line 15:
    ```
    - [ ] **Phase 1: Production Audit & Onboarding** - Map every endpoint, collection, and page of the live system; capture the login bug repro. (Plan 01-04 completed 2026-05-10; SUMMARY files exist on disk for all 4 plans but 01-01 / 01-02 plan checkboxes below remain unflipped — checkbox sync deferred.)
    ```
    Change to:
    ```
    - [x] **Phase 1: Production Audit & Onboarding** - Map every endpoint, collection, and page of the live system; capture the login bug repro. (All 4 plans completed 2026-05-10.)
    ```
    (Note: top-level Phase-1 checkbox flips from `[ ]` to `[x]`.)

    4. Progress table (line ~141) — change:
    ```
    | 1. Production Audit & Onboarding | 4/4 | Complete | 2026-05-10 |
    | 2. Authentication Stabilization | 4/4 | Complete | 2026-05-10 |
    ```
    Verify both rows already show "Complete / 2026-05-10". If Phase-2 row currently shows `0/4 / Not started / -`, fix it to `4/4 / Complete / 2026-05-10`. Phase-1 row should already be correct.

    **B. REQUIREMENTS.md edits:**

    1. Lines 19-23 (AUTHFIX checkboxes) — flip AUTHFIX-01, AUTHFIX-02, AUTHFIX-04, AUTHFIX-05 from `[ ]` to `[x]`. Add a status suffix where helpful:
    ```
    - [x] **AUTHFIX-01**: User can log in with valid credentials and the session persists across browser refresh (rehydrates via `GET /api/auth/me`). → Closed Phase 2 plan 02-02 (test_login_then_me_rehydrates_same_user).
    - [x] **AUTHFIX-02**: User receives correct HTTP error codes on auth failures (400 validation, 401 invalid/expired, 403 role denied) per CONS-auth-header. → Closed Phase 2 plan 02-02 (D-AUTH-01 + D-AUTH-02 in `docs/audit/AUTH_CONTRACT.md`).
    - [x] **AUTHFIX-03**: Admin/operator/viewer role enforcement is verified server-side on at least one endpoint per role-restricted module (rekap, COA delete-all, AI settings). → Closed Phase 2 plan 02-03.
    - [x] **AUTHFIX-04**: The reported login bug is root-caused, fixed (mitigated via ResizeObserver suppressor), and protected by `test_login_then_me_rehydrates_same_user` in `backend/tests/test_auth_session.py`. → Closed Phase 2 plan 02-04 (mitigated; see `docs/audit/LOGIN_BUG_RESOLUTION.md`).
    - [x] **AUTHFIX-05**: Test credentials live ONLY in local `memory/test_credentials.md` (gitignored); zero credentials committed. → Closed Phase 2 plan 02-01 (scanner + pre-commit hook in inner repo).
    ```
    The `→ Closed ...` clauses should be appended to the existing text — do NOT replace the existing sentence.

    2. Traceability table (lines 118-122) — flip statuses:
    ```
    | AUTHFIX-01 | Phase 2 | Complete |
    | AUTHFIX-02 | Phase 2 | Complete |
    | AUTHFIX-03 | Phase 2 | Complete |
    | AUTHFIX-04 | Phase 2 | Complete (mitigated) |
    | AUTHFIX-05 | Phase 2 | Complete |
    ```
    (AUTHFIX-03 already shows Complete per current state — verify; AUTHFIX-04 shows `Complete (mitigated)` per VERIFICATION.md frontmatter; the others move from Pending → Complete.)

    **C. Commit (outer repo, single commit):**
    ```
    git add .planning/ROADMAP.md .planning/REQUIREMENTS.md
    git commit -m "docs(phase-3): amend ROADMAP wording (D-13) + sync Phase-2 closure boxes (SS-04)"
    ```
  </action>
  <verify>
    <automated>grep -q 'admin+operator upload with viewer denied' .planning/ROADMAP.md && grep -q 'test_upload_vessel_role_gate' .planning/ROADMAP.md && grep -q '^- \[x\] \*\*AUTHFIX-01\*\*' .planning/REQUIREMENTS.md && grep -q '^- \[x\] \*\*AUTHFIX-02\*\*' .planning/REQUIREMENTS.md && grep -q '^- \[x\] \*\*AUTHFIX-04\*\*' .planning/REQUIREMENTS.md && grep -q '^- \[x\] \*\*AUTHFIX-05\*\*' .planning/REQUIREMENTS.md && grep -q '| AUTHFIX-04 | Phase 2 | Complete (mitigated) |' .planning/REQUIREMENTS.md && grep -q '^- \[x\] 01-01-PLAN.md' .planning/ROADMAP.md && grep -q '^- \[x\] 01-02-PLAN.md' .planning/ROADMAP.md && git log -1 --pretty=format:%s | grep -q 'docs(phase-3).*amend ROADMAP wording.*SS-04' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP success criterion 3 contains the literal `admin+operator upload with viewer denied` and the test name `test_upload_vessel_role_gate`.
    - ROADMAP plan-list checkboxes for 01-01 and 01-02 are `[x]`.
    - ROADMAP top-level Phase-1 checkbox is `[x]`.
    - REQUIREMENTS.md AUTHFIX-01/02/04/05 checkboxes are `[x]`.
    - REQUIREMENTS.md traceability table shows AUTHFIX-04 as `Complete (mitigated)` and the others as `Complete`.
    - Single outer-repo commit on `main` containing both files.
  </acceptance_criteria>
  <done>
    ROADMAP wording fixed; ROADMAP/REQUIREMENTS sync complete; outer-repo commit landed.
  </done>
</task>

</tasks>

<verification>
After both tasks:
```
# Inner repo
git -C pltu-tenayan-full-backup log -1 --pretty=format:%s | grep -q 'VPS Service Recovery'
grep -c '^## VPS Service Recovery (post-restart)$' pltu-tenayan-full-backup/LOCAL_SETUP.md   # → 1
bash pltu-tenayan-full-backup/scripts/check_credentials.sh   # → exit 0

# Outer repo
git log -1 --pretty=format:%s | grep -q 'amend ROADMAP wording'
grep -c 'admin+operator upload' .planning/ROADMAP.md   # → 1
grep -c '^- \[x\] \*\*AUTHFIX-0[12345]\*\*' .planning/REQUIREMENTS.md   # → 5
```
</verification>

<success_criteria>
- LOCAL_SETUP.md gains a "VPS Service Recovery (post-restart)" H2 with uvicorn + yarn-start procedure + curl health check + cite of LOGIN_BUG_RESOLUTION.md (D-11 closed).
- ROADMAP Phase-2 success criterion 3 reads "admin+operator upload (viewer denied)" with test_upload_vessel_role_gate cite (D-13 closed).
- REQUIREMENTS.md AUTHFIX-01/02/04/05 boxes flipped + traceability rows synced (SS-04 closed for AUTHFIX scope).
- ROADMAP plan-list AUDIT-01/02 boxes flipped + Phase-1 checkbox flipped (Phase-1 carry-forward closed).
- Two atomic commits: one inner (LOCAL_SETUP.md), one outer (ROADMAP.md + REQUIREMENTS.md).
- DOCS-01 carry-forward portion satisfied; downstream Wave-2 plans (Plan 04 Known Issues, Plan 05 DEPLOYMENT_GUIDE cross-link) can now reference the runbook by anchor.
</success_criteria>

<output>
After tasks complete, write `.planning/phases/03-documentation-refresh-decision-lock-in/03-02-SUMMARY.md` per standard summary template — record the inner commit SHA, outer commit SHA, and the literal text of the new ROADMAP success-criterion-3 wording for downstream consumers.
</output>
