---
phase: 06-operational-unblocks
plan: 05
type: execute
wave: 3
depends_on: [06-01, 06-04]
files_modified:
  - pltu-tenayan-full-backup/frontend/src/pages/AIChatPage.js
  - pltu-tenayan-full-backup/frontend/src/components/ai-chat/ConversationSidebar.js
  - pltu-tenayan-full-backup/frontend/src/components/ai-chat/ConversationListItem.js
  - pltu-tenayan-full-backup/frontend/src/components/ai-chat/ConversationPanel.js
  - pltu-tenayan-full-backup/frontend/src/components/ai-chat/ConversationPanelHeader.js
  - pltu-tenayan-full-backup/frontend/src/components/ai-chat/MessageList.js
  - pltu-tenayan-full-backup/frontend/src/components/ai-chat/MessageBubble.js
  - pltu-tenayan-full-backup/frontend/src/components/ai-chat/MessageInputBar.js
  - pltu-tenayan-full-backup/frontend/src/components/ai-chat/EmptyState.js
  - pltu-tenayan-full-backup/frontend/src/lib/formatRelativeTime.js
  - pltu-tenayan-full-backup/frontend/src/App.js
  - pltu-tenayan-full-backup/frontend/src/components/Layout.js
  - pltu-tenayan-full-backup/frontend/src/pages/SmartBlendingPage.js
autonomous: true
requirements: [OPS-02, OPS-04]
tags: [frontend, ai-chat-ui, indonesian-localization, sonner]

must_haves:
  truths:
    - "Route /ai-chat is registered and protected (ProtectedRoute + Layout)"
    - "Layout.js nav has 'Riwayat Percakapan AI' link adjacent to AI Intelligence"
    - "AIChatPage renders ConversationSidebar + ConversationPanel side-by-side"
    - "Sending a message displays optimistic user bubble + AI response after API resolves; on 503 the optimistic message is removed and Indonesian toast appears"
    - "EmptyState shows 'Belum ada percakapan' when user has zero conversations"
    - "All UI strings are in Indonesian per UI-SPEC §Indonesian Localization String Catalog (27 strings)"
    - "SmartBlendingPage.js English toast localized to 'Rekomendasi AI berhasil dibuat!'"
    - "ai_conversations literal string never appears anywhere in frontend src (canonical name comes from backend)"
  artifacts:
    - path: "pltu-tenayan-full-backup/frontend/src/pages/AIChatPage.js"
      provides: "Route-level container for AI chat UI"
    - path: "pltu-tenayan-full-backup/frontend/src/components/ai-chat/"
      provides: "7 sub-components (ConversationSidebar, ConversationListItem, ConversationPanel, ConversationPanelHeader, MessageList, MessageBubble, MessageInputBar, EmptyState)"
    - path: "pltu-tenayan-full-backup/frontend/src/lib/formatRelativeTime.js"
      provides: "Indonesian relative time helper"
  key_links:
    - from: "App.js"
      to: "AIChatPage"
      via: "<Route path='/ai-chat'>"
      pattern: "path=\"/ai-chat\""
    - from: "Layout.js"
      to: "/ai-chat"
      via: "<Link to='/ai-chat'>"
      pattern: "to=\"/ai-chat\""
    - from: "ConversationPanel"
      to: "POST /api/ai/conversations/{id}/messages"
      via: "axios + getAuthHeader()"
      pattern: "/api/ai/conversations.*messages"
---

<objective>
Build the AI chat UI per UI-SPEC: 8 components (page + 7 sub) + 1 helper + 1 route registration + 1 nav link + 1 incidental localization fix in SmartBlendingPage.js:71. Wires to the 4 Plan-06-04 endpoints. Handles all error states with Indonesian sonner toasts (ERR-01..06 verbatim from UI-SPEC §Toast). Uses existing shadcn primitives + sonner + react-markdown + date-fns — ZERO new npm packages (per UI-SPEC Performance Budget).

Purpose: Closes OPS-04 (UI side) + OPS-02 (UI cross-cut for error toasts).
Output: 13 frontend file modifications + working /ai-chat route + Indonesian-localized error UX.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-operational-unblocks/06-CONTEXT.md
@.planning/phases/06-operational-unblocks/06-UI-SPEC.md
@.planning/phases/06-operational-unblocks/06-RESEARCH.md
@.planning/phases/06-operational-unblocks/06-VALIDATION.md
@pltu-tenayan-full-backup/frontend/src/App.js
@pltu-tenayan-full-backup/frontend/src/components/Layout.js
@pltu-tenayan-full-backup/frontend/src/pages/AIIntelligencePage.js
@pltu-tenayan-full-backup/frontend/src/pages/SmartBlendingPage.js
@pltu-tenayan-full-backup/frontend/src/contexts/AuthContext.js
@pltu-tenayan-full-backup/frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 06-05-01: Build 8 chat components + helper + register route + nav link</name>
  <files>
    pltu-tenayan-full-backup/frontend/src/pages/AIChatPage.js,
    pltu-tenayan-full-backup/frontend/src/components/ai-chat/ConversationSidebar.js,
    pltu-tenayan-full-backup/frontend/src/components/ai-chat/ConversationListItem.js,
    pltu-tenayan-full-backup/frontend/src/components/ai-chat/ConversationPanel.js,
    pltu-tenayan-full-backup/frontend/src/components/ai-chat/ConversationPanelHeader.js,
    pltu-tenayan-full-backup/frontend/src/components/ai-chat/MessageList.js,
    pltu-tenayan-full-backup/frontend/src/components/ai-chat/MessageBubble.js,
    pltu-tenayan-full-backup/frontend/src/components/ai-chat/MessageInputBar.js,
    pltu-tenayan-full-backup/frontend/src/components/ai-chat/EmptyState.js,
    pltu-tenayan-full-backup/frontend/src/lib/formatRelativeTime.js,
    pltu-tenayan-full-backup/frontend/src/App.js,
    pltu-tenayan-full-backup/frontend/src/components/Layout.js
  </files>
  <read_first>
    - 06-UI-SPEC.md ENTIRE FILE — this plan is the literal implementation of UI-SPEC. Component inventory section (lines 226-862) is the JSX contract. API Integration Shape section (994-1110) is the axios contract.
    - 06-UI-SPEC.md Indonesian Localization String Catalog (1209-1295) — 27 strings, all verbatim.
    - pltu-tenayan-full-backup/frontend/src/pages/AIIntelligencePage.js — state-management pattern reference (useState + useEffect + axios + getAuthHeader)
    - pltu-tenayan-full-backup/frontend/src/components/Layout.js lines 249-262 (AI Intelligence link pattern)
    - pltu-tenayan-full-backup/frontend/src/App.js — existing protected-route registration pattern
    - 06-RESEARCH.md Focus 9 lines 720-792 (frontend integration target verbatim)
  </read_first>
  <action>
    Implement EVERY component per UI-SPEC verbatim. UI-SPEC is the design contract — do NOT deviate from class names, ARIA labels, structure, or Indonesian strings.

    **CRITICAL ROUTE PATH:** UI-SPEC §Route Addition uses `/ai-chat` (line 234). Planning context note ('route /ai/chat') refers to the same surface — implement using `/ai-chat` per UI-SPEC (the authoritative artifact). Both forms appear in upstream materials; UI-SPEC wins.

    Step-by-step:

    1. **Create `frontend/src/lib/formatRelativeTime.js`** — verbatim from UI-SPEC §Helper Utilities (lines 838-859). Uses date-fns + id locale (already in package.json).

    2. **Create 7 components under `frontend/src/components/ai-chat/`** — verbatim from UI-SPEC component inventory:
       - ConversationSidebar.js (UI-SPEC §2)
       - ConversationListItem.js (UI-SPEC §3) — wrapped with React.memo per UI-SPEC §Performance Budget Memoization
       - ConversationPanel.js (UI-SPEC §4) — owns messages state, loading, sending, hasMore, oldestMessageId, inputValue
       - ConversationPanelHeader.js (UI-SPEC §10)
       - MessageList.js (UI-SPEC §5) — IntersectionObserver sentinel for lazy-load + scroll position preservation per UI-SPEC §Scroll Behavior
       - MessageBubble.js (UI-SPEC §6) — wrapped with React.memo; renders user variant or AI variant with ReactMarkdown + remark-gfm + .ai-chat-response CSS class
       - MessageInputBar.js (UI-SPEC §7) — auto-grow textarea, Enter to send / Shift+Enter newline
    3. **Create `frontend/src/components/ai-chat/EmptyState.js`** (UI-SPEC §8). (UI-SPEC suggests `frontend/src/components/EmptyState.js` shared path; place under ai-chat/ since it is chat-specific — per UI-SPEC Component Inventory file naming convention.)

    4. **Create `frontend/src/pages/AIChatPage.js`** per UI-SPEC §1:
       - useState: conversations, selectedConvId, loadingConversations, error
       - useEffect: on mount fetch GET /api/ai/conversations with Authorization: Bearer <token> via getAuthHeader(); set conversations, set selectedConvId = conversations[0]?.id
       - Outer layout: <div className="flex gap-0 h-[calc(100vh-112px)]"> on lg; ConversationSidebar (flex-shrink-0 w-72) + ConversationPanel (flex-1 min-w-0)
       - "Percakapan Baru" handler: POST /api/ai/conversations -> prepend to list -> set selectedConvId
       - Error handling: try/catch with the 4-class taxonomy per UI-SPEC §Error Mapping Table (no response / 401 / 503 / other 5xx) emitting sonner toast.error with the verbatim ERR-01..06 strings

    5. **Update `frontend/src/App.js`** — add the route per UI-SPEC §Route Addition:
       ```
       <Route path="/ai-chat" element={<ProtectedRoute><Layout><AIChatPage /></Layout></ProtectedRoute>} />
       ```
       Import: import AIChatPage from "@/pages/AIChatPage";

    6. **Update `frontend/src/components/Layout.js`** — add nav link per RESEARCH Focus 9 lines 734-749 verbatim:
       - Add MessageSquare to the lucide-react import line at top (verify it's not already imported)
       - Insert <Link to="/ai-chat" ...>Riwayat Percakapan AI</Link> adjacent to the existing AI Intelligence link (currently at lines 249-262)
       - Use the exact same classes as the AI Intelligence link template (the `location.pathname === "/ai-chat"` active class branch + idle branch)

    7. **Test in dev mode** — DO NOT restart live :3013 frontend (runtime constraint). The dev server picks up changes via HMR. Verify mountable component graph by running `yarn build` (or `craco build`) in the inner frontend directory; build success is the gate.

    Verbatim Indonesian strings to copy from UI-SPEC §Copywriting Contract (lines 187-218):
    - SDB-02: "Percakapan Baru"
    - SDB-03 / fallback: "Percakapan tanpa judul"
    - EMP-01: "Belum ada percakapan"
    - EMP-02: "Mulai percakapan baru untuk bertanya kepada AI."
    - EMP-03 / button: "Mulai Percakapan"
    - MSG-01 / placeholder: "Ketik pesan..."
    - MSG-02 / aria-label: "Kirim pesan"
    - MSG-04: "AI sedang mengetik..."
    - MSG-05: "Memuat pesan lebih lama..."
    - MSG-06: "Awal percakapan"
    - HDR-01: "Riwayat Percakapan AI"
    - HDR-02: "Kelola percakapan AI Anda lintas sesi"
    - NAV-01: "Riwayat Percakapan AI"
  </action>
  <acceptance_criteria>
    - All 12 files exist at paths in files_modified
    - grep -rn "Riwayat Percakapan AI" pltu-tenayan-full-backup/frontend/src returns matches in Layout.js + AIChatPage.js
    - grep -c "path=\"/ai-chat\"" pltu-tenayan-full-backup/frontend/src/App.js == 1
    - grep -c "to=\"/ai-chat\"" pltu-tenayan-full-backup/frontend/src/components/Layout.js == 1
    - grep -c "ai_conversations" pltu-tenayan-full-backup/frontend/src/ -r == 0 (canonical hygiene — frontend reads only /api/ai/conversations endpoint paths, never the literal old collection name)
    - cd pltu-tenayan-full-backup/frontend && yarn build succeeds (or `craco build`)
    - Manual smoke (Plan 06-06 checkpoint): /ai-chat loads, sidebar renders, empty state appears for a fresh test user, "Percakapan Baru" creates conversation
    - Maps to 06-VALIDATION.md row 06-05-01
  </acceptance_criteria>
  <verify>
    <automated>cd pltu-tenayan-full-backup/frontend && yarn build 2>&1 | tail -30 | grep -E "Compiled successfully|compiled successfully|webpack compiled"</automated>
  </verify>
  <done>Frontend builds without errors; route + nav + all components present; all Indonesian strings match UI-SPEC catalog verbatim.</done>
</task>

<task type="auto">
  <name>Task 06-05-02: Indonesian error toast wiring + SmartBlendingPage incidental localization fix</name>
  <files>
    pltu-tenayan-full-backup/frontend/src/pages/AIChatPage.js,
    pltu-tenayan-full-backup/frontend/src/components/ai-chat/ConversationPanel.js,
    pltu-tenayan-full-backup/frontend/src/pages/SmartBlendingPage.js
  </files>
  <read_first>
    - 06-UI-SPEC.md §Toast — Error Handling lines 763-806 (verbatim toast.error patterns for ERR-01..06)
    - 06-UI-SPEC.md §State Management Approach (lines 968-989) — exact 4-class error taxonomy
    - 06-UI-SPEC.md Incidental Fix section (lines 1290-1297)
    - 06-RESEARCH.md Focus 9 lines 794-806 (incidental fix verbatim)
    - pltu-tenayan-full-backup/frontend/src/pages/SmartBlendingPage.js line ~71 (verify exact line of toast.success English string)
  </read_first>
  <action>
    Implement the Indonesian toast UX across AIChatPage and ConversationPanel by adding a shared error handler helper (inline or as `frontend/src/lib/handleApiError.js` — your discretion). The handler takes the axios error + a retry callback and emits the correct sonner toast per UI-SPEC §State Management Approach lines 968-989 verbatim:

    Error taxonomy (4 classes per UI-SPEC):
    1. !err.response (network) -> toast.error("Tidak terhubung ke server.", { action: { label: "Coba lagi", onClick: retryCallback }, duration: 6000 })
    2. status 401 -> toast.error("Sesi habis. Silakan login ulang.", { duration: 5000 }); then navigate("/login")
    3. status 503 -> toast.error("Layanan AI tidak tersedia", { description: err.response.data?.detail || "Layanan AI sementara tidak tersedia. Silakan coba lagi sebentar.", action: { label: "Coba lagi", onClick: retryCallback }, duration: 8000 })
    4. other 4xx/5xx -> toast.error("Terjadi kesalahan pada server. Silakan coba lagi.", { duration: 5000 })

    Wire the handler into:
    - AIChatPage.js: list-conversations fetch failure + create-conversation failure
    - ConversationPanel.js: get-messages failure + send-message failure (with optimistic-message rollback before toast on 503)

    503 send-message specific behavior per UI-SPEC API Integration Shape lines 1092-1100:
    - User submits message; ConversationPanel optimistically appends user message
    - On 503, REMOVE the optimistic user message from local state, then call the shared error handler with retry callback that re-submits the same content
    - On 5xx (non-503), same rollback then generic toast
    - On 401, rollback + auth-expired toast + navigate("/login")

    Incidental fix per RESEARCH Focus 9 line 794-806 + UI-SPEC Incidental Fix:
    - Locate SmartBlendingPage.js — current line ~71 contains: `toast.success("AI recommendation generated successfully!");`
    - REPLACE with: `toast.success("Rekomendasi AI berhasil dibuat!");`
    - Verify the exact line number before editing (RESEARCH says line 71; verify via grep -n).

    Also ensure that any 503 from POST /api/smart-blending/recommend (which Plan 06-01 already wires) surfaces the same Indonesian toast — SmartBlendingPage's existing error handling likely needs the same 4-class taxonomy. Inspect SmartBlendingPage for the current axios catch block; if it surfaces a raw error message, replace with the shared handleApiError helper (or inline the same taxonomy). Per OPS-02 SC: "no raw BadGatewayError surfaced to UI".

    Use NO new npm packages. Sonner + axios + react-router-dom (for navigate) are already in package.json.
  </action>
  <acceptance_criteria>
    - grep -rn "Layanan AI tidak tersedia" pltu-tenayan-full-backup/frontend/src returns at least 2 matches (AIChatPage + SmartBlendingPage error path OR shared handler)
    - grep -rn "Tidak terhubung ke server" pltu-tenayan-full-backup/frontend/src returns at least 1 match
    - grep -rn "Sesi habis. Silakan login ulang" pltu-tenayan-full-backup/frontend/src returns at least 1 match
    - grep -rn "Coba lagi" pltu-tenayan-full-backup/frontend/src returns at least 2 matches (network + 503)
    - grep -rn "Rekomendasi AI berhasil dibuat!" pltu-tenayan-full-backup/frontend/src/pages/SmartBlendingPage.js returns exactly 1 match
    - grep -rn "AI recommendation generated successfully" pltu-tenayan-full-backup/frontend/src returns 0 matches (English string fully replaced)
    - cd pltu-tenayan-full-backup/frontend && yarn build succeeds
    - Maps to 06-VALIDATION.md row 06-05-02
  </acceptance_criteria>
  <verify>
    <automated>cd pltu-tenayan-full-backup/frontend && (! grep -rn "AI recommendation generated successfully" src/) && grep -c "Rekomendasi AI berhasil dibuat!" src/pages/SmartBlendingPage.js | grep -q "^1$" && grep -rn "Layanan AI tidak tersedia" src/ | wc -l | awk '{ exit ($1 >= 1) ? 0 : 1 }'</automated>
  </verify>
  <done>4-class Indonesian error UX wired; SmartBlendingPage English toast localized; zero English error strings reach the user surface; build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend 503 response.data.detail -> sonner toast description | Server-controlled string rendered in toast (Indonesian) |
| LLM markdown response -> ReactMarkdown -> DOM | Untrusted LLM output rendered as markdown |
| User input -> textarea -> POST body | Untrusted user input crosses to backend (backend validation in Plan 06-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Tampering / Cross-Site Scripting | XSS via LLM-generated markdown rendered in MessageBubble AI variant | mitigate | ReactMarkdown ^10.1 sanitizes by default; no rehype-raw plugin used; remark-gfm only adds GFM features (tables, autolinks). HTML strings in markdown are escaped, not rendered. Verified via UI-SPEC §10 (component uses react-markdown with remarkPlugins=[remarkGfm] only — no rehypeRaw). |
| T-06-05-02 | Information Disclosure | Credentials accidentally typed into chat (e.g. user types password) leak to LLM provider | mitigate (partial) | UI displays warning copy is out of scope this phase; backend strips/normalizes content via Plan 06-04 422 empty-guard; full credential-scanner deferred. Operator runbook for Plan 06-06 includes a note "do not type passwords into chat". Accept residual risk for v1. |
| T-06-05-03 | Repudiation | Optimistic user message displayed but not persisted on 503 | mitigate | Optimistic message rollback on 503 (UI-SPEC API Integration Shape lines 1092-1100); user sees toast indicating message was not sent; user can retry |
| T-06-05-04 | Spoofing | Forged conversation_id in URL bar -> attempts to read other user's history | mitigate | Backend filter by user_id in Plan 06-04 returns 404 on cross-user fetch; UI handles 404 by clearing selectedConvId and showing EmptyState (UI-SPEC §API Integration Shape line 1045) |
</threat_model>

<verification>
- yarn build succeeds for frontend (no compile errors, no missing import)
- Grep gates: zero "AI recommendation generated successfully", at least 1 each of "Layanan AI tidak tersedia" / "Tidak terhubung ke server" / "Sesi habis"
- Manual smoke (deferred to Plan 06-06 cutover): /ai-chat loads, empty state, create conversation, send message under live OpenRouter key
</verification>

<success_criteria>
1. /ai-chat route reachable + protected; Layout.js nav has "Riwayat Percakapan AI" link
2. 8 components match UI-SPEC inventory verbatim (ARIA, classes, Indonesian strings)
3. Sonner toasts emit verbatim ERR-01..06 Indonesian copy
4. SmartBlendingPage:71 toast localized to "Rekomendasi AI berhasil dibuat!"
5. Zero new npm packages added
6. yarn build succeeds
</success_criteria>

<output>
Create .planning/phases/06-operational-unblocks/06-05-SUMMARY.md with: per-component file:line summary, build output, screenshot or DOM-snapshot description (if available), and a checklist confirming every UI-SPEC string ID (NAV-01..A11Y-07, TS-01..06) was implemented.

## Commit Boundary
- Inner repo: 13 frontend file modifications
- Outer repo: 06-05-SUMMARY.md, STATE.md
</output>
