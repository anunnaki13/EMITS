---
phase: 06-operational-unblocks
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pltu-tenayan-full-backup/backend/server.py
  - pltu-tenayan-full-backup/backend/tests/test_smart_blending_data.py
autonomous: true
requirements: [OPS-01]
tags: [smart-blending, aggregation, data-correctness]

must_haves:
  truths:
    - "db.smartstock.aggregate($sum: \"$total_penerimaan\") returns non-zero against seeded test DB"
    - "db.sumberpemakaian.aggregate($sum: \"$total_pemakaian\") returns non-zero against seeded test DB"
    - "GET /api/ai/quick/smart-stock returns current_stock != 0 with seeded data"
    - "All 7 server.py aggregation/projection sites in RESEARCH §Focus 2 table use correct field names"
  artifacts:
    - path: "pltu-tenayan-full-backup/backend/tests/test_smart_blending_data.py"
      provides: "3 Wave-0 tests for aggregation correctness (smartstock, sumberpemakaian, integration)"
      contains: "test_smartstock_sum|test_sumberpemakaian_sum|test_smart_stock_endpoint"
  key_links:
    - from: "server.py:2395,2398,2863,2868,2877"
      to: "MongoDB smartstock + sumberpemakaian aggregations"
      via: "$sum: $total_penerimaan / $total_pemakaian"
      pattern: "\\$sum.*\\$total_(penerimaan|pemakaian)"
---

<objective>
Fix the 7 server.py aggregation/projection field-name mismatches that cause `/api/ai/quick/smart-stock` and `get_database_context()` to return zeros (Phase-5 CP2 surfaced this latent data bug; D-06..D-08). Each fix uses the canonical field name verified by live mongosh probe (RESEARCH §Focus 2 verbatim table). Add 3 unit tests in `test_smart_blending_data.py` against the Phase-4 seeded test DB to catch future field-name drift.

Purpose: Closes OPS-01 (data side). The provider migration in Plan 06-01 only makes LLM calls work — these field-name fixes ensure the LLM receives non-zero data, which is required for D-08 smoke test (smart-blending recommendation at GCV 4000/4200/4500) to pass at cutover.
Output: 7 surgical edits + 3 green pytest tests + grep gate that no aggregation references the removed field names (`$tonase`, `$batubara_mt`, `$biomassa_mt`, `$tanggal`, `$energy_mwh`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-operational-unblocks/06-CONTEXT.md
@.planning/phases/06-operational-unblocks/06-RESEARCH.md
@.planning/phases/06-operational-unblocks/06-VALIDATION.md
@.planning/decisions/ADR-009-canonical-smartstock.md
@.planning/decisions/ADR-010-canonical-sumberpemakaian.md
@.planning/intel/constraints.md
@pltu-tenayan-full-backup/backend/tests/factories/smartstock.py
@pltu-tenayan-full-backup/backend/tests/factories/sumberpemakaian.py
</context>

<tasks>

<task type="auto">
  <name>Task 06-02-01: Apply 7 server.py aggregation field-name fixes (verbatim from RESEARCH §Focus 2)</name>
  <files>pltu-tenayan-full-backup/backend/server.py</files>
  <read_first>
    - 06-RESEARCH.md §Focus 2 lines 358-426 (field mismatch table with line numbers, before/after)
    - 06-RESEARCH.md §Code Examples Pattern 2 lines 1059-1083 (corrected aggregation pattern)
    - 06-RESEARCH.md Pitfall 5 lines 982-987 (data-quality vs code-correctness note)
    - server.py lines 2375-2410, 2855-2895 (modified sections — read both regions to confirm current state before editing)
    - Phase-5 hotfix at line ~2891 (`or 0` coercion — must remain intact)
  </read_first>
  <action>
    Apply ALL 7 fixes per RESEARCH §Focus 2 table verbatim. Read each region first; line numbers may have drifted slightly since RESEARCH probe — confirm via grep before each edit.

    **FIX 1 — server.py lines 2379-2384** (`get_database_context()`, smartstock projection):
    - BEFORE: projection includes fields `"source"`, `"supplier"`, `"cargo"`, `"tonase"`
    - AFTER: change projection to `{"_id":0, "date":1, "total_penerimaan":1, "suppliers":1}`

    **FIX 2 — server.py lines 2387-2392** (`get_database_context()`, sumberpemakaian projection):
    - BEFORE: projection includes fields `"tanggal"`, `"energy_mwh"`, `"batubara_mt"`, `"biomassa_mt"`, `"sfc"`
    - AFTER: change projection to `{"_id":0, "date":1, "total_pemakaian":1, "suppliers":1}`

    **FIX 3 — server.py lines 2395-2397** (`get_database_context()`, stock summary):
    - BEFORE: `db.smartstock.aggregate([{"$group": {"_id": None, "total": {"$sum": "$tonase"}}}])`
    - AFTER: `db.smartstock.aggregate([{"$group": {"_id": None, "total": {"$sum": "$total_penerimaan"}}}])`

    **FIX 4 — server.py lines 2398-2400** (`get_database_context()`, pemakaian summary):
    - BEFORE: `db.sumberpemakaian.aggregate([{"$group": {"_id": None, "total_batubara": {"$sum": "$batubara_mt"}, "total_biomassa": {"$sum": "$biomassa_mt"}}}])`
    - AFTER: `db.sumberpemakaian.aggregate([{"$group": {"_id": None, "total_pemakaian": {"$sum": "$total_pemakaian"}}}])` (collapsed to single field; downstream readers updated to read `total_pemakaian` instead of split)
    - Downstream: search any usage in the same function of `total_batubara` / `total_biomassa` keys (likely in the prompt-building string), replace with `total_pemakaian` single value.

    **FIX 5 — server.py lines 2863-2865** (`/api/ai/quick/smart-stock` smartstock total):
    - BEFORE: `db.smartstock.aggregate([{"$group": {"_id": None, "total": {"$sum": "$tonase"}}}])`
    - AFTER: `db.smartstock.aggregate([{"$group": {"_id": None, "total": {"$sum": "$total_penerimaan"}}}])`

    **FIX 6 — server.py lines 2868-2874** (`/api/ai/quick/smart-stock` sumberpemakaian totals):
    - BEFORE: `db.sumberpemakaian.aggregate([{"$group": {"_id": None, "total_batubara": {"$sum": "$batubara_mt"}, "total_biomassa": {"$sum": "$biomassa_mt"}}}])`
    - AFTER: `db.sumberpemakaian.aggregate([{"$group": {"_id": None, "total_pemakaian": {"$sum": "$total_pemakaian"}}}])`. Update downstream return-shape code to expose `total_pemakaian` instead of `total_batubara` + `total_biomassa`.

    **FIX 7 — server.py lines 2877-2885** (`/api/ai/quick/smart-stock` sumberpemakaian averages):
    - BEFORE: `db.sumberpemakaian.aggregate([{"$group": {"_id": None, "avg_batubara": {"$avg": "$batubara_mt"}, "avg_energy": {"$avg": "$energy_mwh"}}}])`
    - AFTER: `db.sumberpemakaian.aggregate([{"$group": {"_id": None, "avg_pemakaian": {"$avg": "$total_pemakaian"}}}])`. Remove `avg_energy` (no source field). Update downstream return-shape code accordingly; the existing `or 0` coercion at line ~2891 (Phase-5 CP2 hotfix) remains in place — preserve verbatim.

    **PRESERVE:** Phase-5 None-coercion hotfix at lines ~2891-2894 (`or 0` patterns). These are unrelated to the field-name fix and must remain intact (verified in Phase-5 plan 05-04 SUMMARY).
  </action>
  <acceptance_criteria>
    - `grep -c '"\$tonase"' pltu-tenayan-full-backup/backend/server.py` == 0
    - `grep -c '"\$batubara_mt"' pltu-tenayan-full-backup/backend/server.py` == 0
    - `grep -c '"\$biomassa_mt"' pltu-tenayan-full-backup/backend/server.py` == 0
    - `grep -c '"\$energy_mwh"' pltu-tenayan-full-backup/backend/server.py` == 0
    - `grep -c '"\$total_penerimaan"' pltu-tenayan-full-backup/backend/server.py` >= 2 (FIX 3 + FIX 5)
    - `grep -c '"\$total_pemakaian"' pltu-tenayan-full-backup/backend/server.py` >= 3 (FIX 4 + FIX 6 + FIX 7)
    - `grep -c "or 0" pltu-tenayan-full-backup/backend/server.py` >= 4 (Phase-5 hotfix preserved)
    - Maps to 06-VALIDATION.md row 06-02-01 + 06-02-02
  </acceptance_criteria>
  <verify>
    <automated>cd pltu-tenayan-full-backup/backend && (grep -c '"\$tonase"\|"\$batubara_mt"\|"\$biomassa_mt"\|"\$energy_mwh"' server.py | grep -q "^0$")</automated>
  </verify>
  <done>All 7 sites use canonical field names; Phase-5 hotfix preserved; downstream prompt strings still reference valid keys.</done>
</task>

<task type="auto">
  <name>Task 06-02-02: Add 3 Wave-0 aggregation regression tests in test_smart_blending_data.py</name>
  <files>pltu-tenayan-full-backup/backend/tests/test_smart_blending_data.py (new)</files>
  <read_first>
    - pltu-tenayan-full-backup/backend/tests/factories/smartstock.py
    - pltu-tenayan-full-backup/backend/tests/factories/sumberpemakaian.py
    - pltu-tenayan-full-backup/backend/tests/conftest.py (_backend_lifecycle, _seed_baseline_data, http_client)
    - 06-VALIDATION.md rows 06-02-01..03
    - 06-RESEARCH.md §Focus 2 (live schema field names verbatim)
  </read_first>
  <action>
    Per D-07. Create `pltu-tenayan-full-backup/backend/tests/test_smart_blending_data.py` with 3 tests. Use Phase-4 factories + the `pltu_tenayan_test_<sessionid>` test DB.

    Test 1 — `test_smartstock_sum`:
    - Seed 3 smartstock docs via `smartstock_factory(total_penerimaan=1000)`, `(2000)`, `(3000)`.
    - Run aggregation `db.smartstock.aggregate([{"$group": {"_id": None, "total": {"$sum": "$total_penerimaan"}}}])`.
    - Assert returned `total == 6000`.

    Test 2 — `test_sumberpemakaian_sum`:
    - Seed 3 sumberpemakaian docs with `total_pemakaian=500`, `700`, `900`.
    - Run aggregation `db.sumberpemakaian.aggregate([{"$group": {"_id": None, "total_pemakaian": {"$sum": "$total_pemakaian"}}}])`.
    - Assert returned `total_pemakaian == 2100`.

    Test 3 — `test_smart_stock_endpoint` (integration; requires running test backend at :18013 via conftest `_backend_lifecycle`):
    - Seed 2 smartstock + 2 sumberpemakaian docs with non-zero values.
    - GET `/api/ai/quick/smart-stock` with admin JWT (via Phase-4 `jwt_helpers`).
    - Assert response 200; assert `current_stock != 0`; assert response body shape includes `total_penerimaan` (not `tonase`) and `total_pemakaian` (not `batubara_mt` / `biomassa_mt`).

    Use pytest-asyncio. Decorator `@pytest.mark.asyncio` on coroutine tests. Use the existing `mongo_test_db` fixture from conftest.

    If existing factories don't have `total_penerimaan` / `total_pemakaian` as kwargs, extend the factory (one-line addition) — note inline that this is required by Phase 6 OPS-01 field-name fix.
  </action>
  <acceptance_criteria>
    - `pltu-tenayan-full-backup/backend/tests/test_smart_blending_data.py` exists with 3 tests
    - `grep -c "def test_smartstock_sum\|def test_sumberpemakaian_sum\|def test_smart_stock_endpoint" pltu-tenayan-full-backup/backend/tests/test_smart_blending_data.py` == 3
    - All 3 tests pass under `AI_FAKE=1 pytest tests/test_smart_blending_data.py -x -q`
    - Maps to 06-VALIDATION.md rows 06-02-01, 06-02-02, 06-02-03
  </acceptance_criteria>
  <verify>
    <automated>cd pltu-tenayan-full-backup/backend && AI_FAKE=1 .venv/bin/pytest tests/test_smart_blending_data.py -x -q</automated>
  </verify>
  <done>3/3 tests green; field-name regression gate is in place; future drift will fail the suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend -> MongoDB | server.py aggregation pipelines read from canonical collections |
| test backend -> mongo_test_db | Phase-4 conftest isolates test DB by sessionid (no production data risk) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | Aggregation field-name drift over time | mitigate | 3 regression tests in test_smart_blending_data.py with canonical field names assert non-zero results; future field rename triggers test failure |
| T-06-02-02 | Information Disclosure | Test DB might accidentally hit live MongoDB | accept | Phase-4 `mongo_test_db` fixture forces `MONGO_TEST_DB_NAME=pltu_tenayan_test_<sessionid>`; cannot collide with live `pltu_tenayan` |
</threat_model>

<verification>
- `AI_FAKE=1 pytest tests/test_smart_blending_data.py -x -q` exits 0
- Grep gates (no `$tonase` / `$batubara_mt` / `$biomassa_mt` / `$energy_mwh` in server.py) all pass
- Phase-5 None-coercion hotfix preserved (`or 0` count unchanged)
- `AI_FAKE=1 pytest tests/test_ai_endpoints.py -x` still GREEN (no regression on /api/ai/quick/smart-stock contract)
</verification>

<success_criteria>
1. All 7 field-name fixes applied verbatim per RESEARCH §Focus 2 table
2. test_smart_blending_data.py exists with 3 tests, all green
3. Zero `$tonase` / `$batubara_mt` / `$biomassa_mt` / `$energy_mwh` references in server.py
4. Phase-5 None-coercion hotfix at lines ~2891-2894 preserved verbatim
5. /api/ai/quick/smart-stock returns non-zero `current_stock` against seeded test data
</success_criteria>

<output>
Create `.planning/phases/06-operational-unblocks/06-02-SUMMARY.md` with: per-fix line range, before/after diff snippet, test output, and a one-line note about data-quality caveat (RESEARCH Pitfall 5 — live data may still show zeros due to upstream upload-parser artifact; that is a separate concern outside Phase 6 scope).

## Commit Boundary
- Inner repo: server.py, tests/test_smart_blending_data.py, factories (if extended)
- Outer repo: 06-02-SUMMARY.md, STATE.md
</output>
