---
phase: 02-authentication-stabilization
plan: 04
type: execute
wave: 3
depends_on: [02]
files_modified:
  - pltu-tenayan-full-backup/docs/audit/LOGIN_BUG_RESOLUTION.md
  - pltu-tenayan-full-backup/frontend/public/index.html
autonomous: false
requirements:
  - AUTHFIX-04
must_haves:
  truths:
    - "An operator has re-run the original AUDIT-04 repro path against the live frontend at http://103.150.197.225:3013 (or http://localhost:3013) and recorded the observed Console / Network / localStorage / final-URL state for both Path A (login tab) and the register tab"
    - "The reported 'ResizeObserver loop completed with undelivered notifications' error no longer surfaces to the user (either suppressed by the existing index.html handler, or eliminated by a deeper fix landed in this plan)"
    - "Path A login flow completes end-to-end: token written to localStorage under key 'token', GET /api/auth/me returns 200, final URL is /dashboard"
    - "If the operator observes any UI failure beyond the documented ResizeObserver noise, it is captured verbatim in LOGIN_BUG_RESOLUTION.md and either fixed in this plan OR explicitly carried forward as a Phase 2.1 INSERTED phase (per ROADMAP decimal-phase convention)"
    - "A LOGIN_BUG_RESOLUTION.md document closes AUDIT-04 / AUTHFIX-04 with status (resolved / mitigated / carried-forward) and cites the regression test that protects the fix (test_auth_session.py from Plan 02-02)"
  artifacts:
    - path: "pltu-tenayan-full-backup/docs/audit/LOGIN_BUG_RESOLUTION.md"
      provides: "Operator playbook results + disposition decision (resolved / mitigated / carried-forward) + Phase-3 follow-ups"
      min_lines: 40
    - path: "pltu-tenayan-full-backup/frontend/public/index.html"
      provides: "ResizeObserver suppressor (existing) — kept, removed, or replaced based on operator findings"
      contains: "ResizeObserver"
  key_links:
    - from: "LOGIN_BUG_RESOLUTION.md"
      to: "pltu-tenayan-full-backup/docs/audit/LOGIN_BUG.md"
      via: "explicit citation of original repro paths"
      pattern: "LOGIN_BUG\\.md"
    - from: "LOGIN_BUG_RESOLUTION.md"
      to: "backend/tests/test_auth_session.py"
      via: "regression-test citation (AUTHFIX-04 says 'protected by at least one regression test')"
      pattern: "test_auth_session\\.py"
    - from: "frontend/public/index.html ResizeObserver suppressor"
      to: "Login.js:186-197 Radix Select"
      via: "page-level error handler"
      pattern: "ResizeObserver"
---

<objective>
Close AUTHFIX-04: the reported login bug is root-caused, fixed (or explicitly mitigated), and protected by a regression test. Phase 1 plan 01-04 captured the repro (LOGIN_BUG.md) and isolated the backend layer as healthy (200/401/422 path B + 200/400 path C). The bug is purely UI-side. Two possibilities remain after Plan 02-02:

A. The reported "ResizeObserver loop completed with undelivered notifications" error is the ENTIRE bug and the existing suppressor at `frontend/public/index.html:49-65` already mitigates it for end users (CRA dev-server overlay no longer surfaces it). In this case: AUTHFIX-04 is closed with status `mitigated`, and the regression test from Plan 02-02 (`test_login_then_me_rehydrates_same_user`) is the protective coverage at the contract layer. The frontend file is left untouched.

B. The operator observes a SEPARATE UI failure during the re-test (e.g., toast-success-but-no-navigate, blank dashboard after login, register-tab role-select crashes the form, etc.) that LOGIN_BUG.md did not capture. In this case: capture the exact observed behavior, decide whether the fix fits in Plan 02-04 (one-paragraph code change in AuthContext.js or Login.js) OR requires a new phase. If the latter: insert Phase 2.1 per the ROADMAP decimal-phase convention and document the handoff.

This plan REQUIRES a human checkpoint because the disambiguation cannot be performed by Claude on this VPS (no headless browser available — Phase 1 plan 01-04 documented this; the operator playbook at LOGIN_BUG.md:198-217 is the canonical re-test ritual). After the operator reports back, Claude either (a) writes the resolution doc and commits, or (b) writes the resolution doc, applies a small targeted fix, runs the test suite, commits.

Purpose: explicitly close the AUDIT-04 → AUTHFIX-04 thread with operator-confirmed status; do NOT silently rely on the ResizeObserver suppressor without ground-truth verification.

Output: LOGIN_BUG_RESOLUTION.md + (conditionally) a small frontend fix + (conditionally) test additions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-authentication-stabilization/02-02-PLAN.md
@pltu-tenayan-full-backup/docs/audit/LOGIN_BUG.md
@pltu-tenayan-full-backup/frontend/src/pages/Login.js
@pltu-tenayan-full-backup/frontend/src/contexts/AuthContext.js
@pltu-tenayan-full-backup/frontend/public/index.html

<interfaces>
<!-- Suppressor (existing, do NOT remove without operator confirmation): -->
<!-- frontend/public/index.html:49-65 -->
```html
<script>
    var RO_RE = /^ResizeObserver loop /;
    window.addEventListener('error', function (e) {
        if (e && e.message && RO_RE.test(e.message)) {
            e.stopImmediatePropagation();
        }
    });
    window.addEventListener('unhandledrejection', function (e) {
        var msg = e && e.reason && (e.reason.message || String(e.reason));
        if (msg && RO_RE.test(msg)) e.stopImmediatePropagation();
    });
</script>
```

<!-- Login.js suspect block (Login.js:186-197): -->
<!-- Radix Select on the register tab. Imports from @/components/ui/select which wraps @radix-ui/react-select. -->
<!-- The login tab (Login.js:90-141) has NO Radix Select — only Input + Button + Eye toggle. -->

<!-- AuthContext.js login flow (AuthContext.js:34-41): correct per Phase-1 audit. -->
<!-- AuthContext.js rehydrate (AuthContext.js:13-32): correct per Phase-1 audit. -->

Decision tree for this plan:
1. Operator runs Path A (login tab) → records Console / Network / localStorage / final URL.
2. Operator runs register-tab role-select interaction → records whether ResizeObserver error is visible.
3. Based on operator report:
   - All-clear (login works, suppressor mute): close as `mitigated` (status documented; suppressor kept; Phase-3 follow-up to evaluate Radix upgrade).
   - New unrelated UI failure: capture, decide scope.
   - Suppressor not working (ResizeObserver error visible): tighten the regex or apply a deeper fix (e.g., upgrade @radix-ui/react-select; switch role picker to a non-Radix component; add a `requestAnimationFrame` deferral in Login.js's tab-switch handler).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator re-runs LOGIN_BUG.md Path A and register-tab repro</name>
  <what-built>
    Plans 02-01 / 02-02 / 02-03 have landed on disk and committed:
    - Credential hygiene gate (gitignore + scanner + pre-commit hook)
    - Backend 422→400 reconciliation handler
    - 5-test session/contract regression suite (passing)
    - 11-test role-enforcement regression suite (passing)
    Backend is verified healthy at the contract layer. The frontend has NOT been touched in any prior plan. The ResizeObserver suppressor at `frontend/public/index.html:49-65` is exactly as Phase-1 documented. The operator now needs to provide ground-truth for the UI side.
  </what-built>
  <how-to-verify>
    Follow the operator playbook from `pltu-tenayan-full-backup/docs/audit/LOGIN_BUG.md` lines 198-217. Verbatim steps:

    **Setup**
    1. Open Chrome or Firefox in **Incognito** with DevTools open (Console + Network tabs).
    2. Source the test admin credentials locally from `pltu-tenayan-full-backup/memory/test_credentials.md`. Do NOT type the password into a chat or commit it anywhere.

    **Path A — Login tab (the originally-reported "login bug")**
    3. Visit `http://localhost:3013/login` (or `http://103.150.197.225:3013/login` if testing the live VPS).
    4. The "Masuk" tab should be selected by default.
    5. Type the admin email into the Email field.
    6. Type the admin password into the Password field.
    7. Click "Masuk".
    8. Record the following:
       - **Console**: any errors? Quote them verbatim. (Expected: none — but report whatever you see.)
       - **Network**: filter on `/api/auth/`. Confirm `POST /api/auth/login` is 200; on the next page mount, `GET /api/auth/me` is 200.
       - **Application → Local Storage → http://localhost:3013** (or VPS host): the `token` key holds a JWT (string starting with `JWT_PREFIX`).
       - **Final URL**: should be `/dashboard`.

    **Register-tab interaction (the originally-suspected ResizeObserver source)**
    9. Click the "Daftar" tab.
    10. Click the "Role" dropdown (Radix Select).
    11. Watch the Console carefully as the dropdown opens and closes. Record:
        - Is "ResizeObserver loop completed with undelivered notifications" visible? (Expected: NO — suppressor should silence it.)
        - Is any OTHER error visible? (Quote verbatim.)
    12. Select each role option (viewer / operator / admin) in turn. Note any visual or interaction issue.
    13. (Optional, do NOT submit) Type values into Name / Email / Password and observe whether the form behaves normally up to but excluding submit.

    **Refresh-persistence check (AUTHFIX-01 ground truth)**
    14. After Path A reaches /dashboard, hit browser refresh (F5 or Cmd+R).
    15. Record:
        - Does the page reload to /dashboard, or bounce to /login?
        - Network tab: is `GET /api/auth/me` called automatically? Status?
        - Application → Local Storage: does the `token` key still hold the same JWT?

    **Report back to Claude with the following filled in:**

    Path A (login tab):
    - Console errors: [verbatim or "none"]
    - POST /api/auth/login status: [200 / other]
    - GET /api/auth/me status: [200 / other]
    - localStorage `token` present: [yes / no]
    - Final URL: [/dashboard / other]

    Register-tab role-select:
    - ResizeObserver error visible in Console: [yes / no]
    - Other errors visible: [verbatim or "none"]
    - Dropdown opens and lets me pick all three roles: [yes / no]
    - Any visual glitch (cut-off, mispositioned popover, flicker): [describe or "none"]

    Refresh persistence:
    - After F5: stays on /dashboard or bounces to /login: [stays / bounces]
    - GET /api/auth/me on refresh: [200 / 401 / other]
    - localStorage token survives: [yes / no]

    Disposition (your call as the project owner):
    - [ ] All-clear — close AUTHFIX-04 as `mitigated` (suppressor kept).
    - [ ] New unrelated UI failure observed — describe it; Claude will assess fix scope.
    - [ ] Suppressor failing — ResizeObserver error still surfaces; Claude will tighten the suppressor or apply a deeper fix.
    - [ ] AUTHFIX-01 refresh path is broken — bounces to /login despite valid token.
  </how-to-verify>
  <resume-signal>
    Paste the filled-in report above. Pick one disposition box. Optionally attach a screenshot of the DevTools Console + Network panes if helpful.

    Resume options:
    - "all-clear" — Claude writes LOGIN_BUG_RESOLUTION.md as `mitigated` and commits.
    - "new ui failure: <describe>" — Claude assesses scope, writes the doc, applies a small fix if it fits in Plan 02-04 budget, otherwise inserts Phase 2.1.
    - "suppressor failing" — Claude tightens the suppressor regex (or switches to a deeper fix) and re-tests.
    - "refresh broken" — Claude debugs the rehydrate path; this likely indicates a bug in AuthContext.js:13-32 not caught by the contract test (e.g., timing of setLoading vs setUser ordering).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Conditional fix and LOGIN_BUG_RESOLUTION.md</name>
  <files>pltu-tenayan-full-backup/docs/audit/LOGIN_BUG_RESOLUTION.md, pltu-tenayan-full-backup/frontend/public/index.html (conditional)</files>
  <read_first>
    - The operator's report from Task 1 (verbatim text in the resume signal)
    - pltu-tenayan-full-backup/docs/audit/LOGIN_BUG.md (the original Phase-1 repro)
    - pltu-tenayan-full-backup/backend/tests/test_auth_session.py (Plan 02-02 — the protective regression)
    - pltu-tenayan-full-backup/frontend/public/index.html (lines 49-65 for the existing suppressor)
    - pltu-tenayan-full-backup/frontend/src/contexts/AuthContext.js (if the operator reported refresh-broken)
  </read_first>
  <action>
    Branch on the operator's disposition:

    **Branch A — "all-clear":**
    1. Do NOT modify any frontend file. The existing suppressor stays.
    2. Create `pltu-tenayan-full-backup/docs/audit/LOGIN_BUG_RESOLUTION.md` (>= 40 lines) with sections:
       - `# Login Bug Resolution (AUTHFIX-04)`
       - `## Status` — "Mitigated. Original symptom (ResizeObserver loop in Console) is suppressed by the handler at `frontend/public/index.html:49-65`. Operator-confirmed end-to-end on YYYY-MM-DD: login works, register-tab role-select interaction does not surface the error to the user, refresh persistence works."
       - `## Operator report` — copy the operator's verbatim report (Console / Network / localStorage / final URL / refresh result for Path A and register-tab and refresh).
       - `## Disposition rationale` — paragraph explaining why we accept `mitigated` rather than insisting on a deeper fix: (a) the underlying Radix internal ResizeObserver behavior is upstream-library and not a project bug; (b) end users do not see the error; (c) the suppressor is narrowly scoped to messages matching `/^ResizeObserver loop /` and does not swallow any other error class; (d) Phase 7 modular refactor or Phase 8 polish can revisit Radix upgrade if upstream ships a fix.
       - `## Regression coverage` — explicit citation: `backend/tests/test_auth_session.py::test_login_then_me_rehydrates_same_user` is the AUTHFIX-04 protective regression. It exercises the contract path (login → token → /me rehydrate) that the originally-reported bug obscured. Cite test name + line refs.
       - `## Cross-references` — links to LOGIN_BUG.md, AUTH_CONTRACT.md (Plan 02-02), and the relevant ROADMAP / REQUIREMENTS lines (AUTHFIX-04, AUDIT-04).
       - `## Phase-3 follow-ups` — short bullet list:
         - DOCS-01: README/documentation.md "Known Issues" section should reflect the resolved status.
         - Phase 7/8: evaluate `@radix-ui/react-select` upgrade once upstream resolves the loop emission.
       - `## Phase-1 audit-probe cleanup status` — confirm whether the 3 audit-probe-* synthetic users were cleaned (Plan 02-02 conftest autouse fixture deleted them; record the deleted_count from that run).
    3. Commit (inner repo): `git add docs/audit/LOGIN_BUG_RESOLUTION.md && git commit -m "docs(authfix-04): close login bug as mitigated (operator-verified, suppressor sufficient)"`.

    **Branch B — "new ui failure":**
    1. Read the operator's description carefully. Categorize:
       - Single-file fix (1 paragraph code change in AuthContext.js or Login.js)? Apply it; cite the change in the resolution doc.
       - Multi-file or speculative fix? Do NOT apply. Write the resolution doc with status `carried-forward`, propose Phase 2.1 in ROADMAP.md, hand back to the user for approval.
    2. If single-file fix: apply it, run `pytest backend/tests/test_auth_session.py backend/tests/test_auth_roles.py -v` to confirm the contract suite still passes, then ask the operator to re-run the playbook one more time before commit.
    3. Resolution doc as in Branch A but with status `resolved (with fix)` and a `## Code change` section showing the diff.

    **Branch C — "suppressor failing":**
    1. Inspect the verbatim error message the operator reported. If the regex `/^ResizeObserver loop /` does not match it, broaden carefully:
       - Variant: "ResizeObserver loop limit exceeded" — different message; widen regex to `/^ResizeObserver loop /`.
       - Variant: error reaches the React error boundary instead of `window.error` — the suppressor needs to also handle React's error overlay; this is a deeper fix.
    2. Update `frontend/public/index.html:49-65`. Preserve the existing suppressor structure; ONLY widen the regex if needed. If a deeper fix is required, write the doc as `carried-forward` and hand back.
    3. Operator re-tests; commit only after all-clear.

    **Branch D — "refresh broken":**
    1. Read AuthContext.js:13-32. Likely cause: setLoading flips to false BEFORE setUser, causing ProtectedRoute to redirect to /login during the fetch window. Apply targeted fix:
       - Move `setLoading(false)` to the END of the try block (after setUser/setToken) AND inside the catch block (after clearing). Currently it's outside both — confirm.
       - OR introduce a separate `rehydrating` state that gates ProtectedRoute distinct from `loading`.
    2. After fix, operator re-tests refresh path. Commit only after green.

    In ALL branches: the resolution doc MUST exist with the operator's report quoted, the disposition recorded, the regression test cited, and Phase-3 follow-ups listed.
  </action>
  <verify>
    <automated>cd pltu-tenayan-full-backup && test -f docs/audit/LOGIN_BUG_RESOLUTION.md && test "$(wc -l < docs/audit/LOGIN_BUG_RESOLUTION.md)" -ge 40 && grep -qE '^# Login Bug Resolution' docs/audit/LOGIN_BUG_RESOLUTION.md && grep -qE '^## Status$' docs/audit/LOGIN_BUG_RESOLUTION.md && grep -qE '^## Operator report$' docs/audit/LOGIN_BUG_RESOLUTION.md && grep -qE '^## Regression coverage$' docs/audit/LOGIN_BUG_RESOLUTION.md && grep -qE 'test_login_then_me_rehydrates_same_user' docs/audit/LOGIN_BUG_RESOLUTION.md && grep -qE 'AUTHFIX-04' docs/audit/LOGIN_BUG_RESOLUTION.md && grep -qE 'LOGIN_BUG\.md' docs/audit/LOGIN_BUG_RESOLUTION.md && grep -qE '(test_me_with_expired_token_returns_401.*PASSED|expired-token.*green|JWT_SECRET.*set)' docs/audit/AUTH_CONTRACT.md && bash scripts/check_credentials.sh && pytest backend/tests/test_auth_session.py -v --tb=short 2>&1 | tail -5 | grep -qE '5 passed' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `pltu-tenayan-full-backup/docs/audit/LOGIN_BUG_RESOLUTION.md` exists, >= 40 lines.
    - Contains required headings: `# Login Bug Resolution (AUTHFIX-04)`, `## Status`, `## Operator report`, `## Disposition rationale`, `## Regression coverage`, `## Cross-references`, `## Phase-3 follow-ups`, `## Phase-1 audit-probe cleanup status` (`grep -cE '^## ' docs/audit/LOGIN_BUG_RESOLUTION.md` >= 7).
    - Status line is one of: `Mitigated`, `Resolved (with fix)`, `Carried forward to Phase 2.1`.
    - Cites `test_login_then_me_rehydrates_same_user` (the regression from Plan 02-02) by name.
    - Cites `LOGIN_BUG.md` (the original Phase-1 repro).
    - Cites `AUTHFIX-04` (the requirement).
    - Operator's verbatim report is quoted in `## Operator report`.
    - `pytest backend/tests/test_auth_session.py -v` reports `5 passed` (no skips). The expired-token leg MUST be exercised — `4 passed, 1 skipped` is not acceptable for AUTHFIX-04 closure because it means the 401-for-expired clause of CONS-auth-header was never validated.
    - `docs/audit/AUTH_CONTRACT.md` records a green expired-token execution (Plan 02-02 must have run with JWT_SECRET sourced from `backend/.env`).
    - Credential scanner exits 0.
    - If branch B/C/D modified any source file, the change is small and reviewable; the resolution doc has a `## Code change` subsection showing the diff.
    - Inner-repo commit landed with message containing `authfix-04`.
  </acceptance_criteria>
  <done>AUTHFIX-04 has a written disposition (mitigated / resolved / carried-forward), regression coverage cited, contract tests still green, commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → live frontend | Trust the operator's report verbatim; do not infer state Claude cannot observe. |
| ResizeObserver suppressor → all window errors | Suppressor is a global window.error handler; could mask real errors if regex is too broad. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | I (Information disclosure) | ResizeObserver suppressor swallows unrelated errors | mitigate | Regex `/^ResizeObserver loop /` is anchor-prefixed and very specific; only matches the Chrome/Firefox emission pattern. Resolution doc records why broader patterns are NOT used. |
| T-02-18 | T (Tampering) | Operator misreports state, leading to incorrect closure | accept | Mitigation: regression test `test_login_then_me_rehydrates_same_user` runs in CI/local on every change; if the contract path breaks, tests catch it regardless of operator report. |
| T-02-19 | E (Elevation) | Hidden bug allows session persistence to fail silently | mitigate | AUTHFIX-01 regression test in Plan 02-02 explicitly covers the rehydrate path. If branch D applies (refresh broken), the fix is verified by re-running the test plus operator re-test. |
</threat_model>

<verification>
End-to-end check:
1. Operator-verified report present in `LOGIN_BUG_RESOLUTION.md`.
2. Status line states one of {Mitigated, Resolved (with fix), Carried forward to Phase 2.1}.
3. `pytest backend/tests/test_auth_session.py` → 5 passed (no regression introduced).
4. Credential scanner exits 0.
5. Inner-repo commit with `authfix-04`.
</verification>

<success_criteria>
- AUTHFIX-04 satisfied: original repro re-run by operator, outcome documented, status decided.
- Regression test cited (Plan 02-02 `test_login_then_me_rehydrates_same_user`).
- If a code change was applied, it's small, scoped, reviewed, and the test suite still passes.
- If the issue requires more work, Phase 2.1 is proposed (not silently deferred).
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-stabilization/02-04-SUMMARY.md` documenting:
- Operator-confirmed disposition (mitigated / resolved / carried-forward).
- Verbatim operator report (quoted, no credentials).
- Code change applied (if any) with file:line refs and diff snippet.
- Regression test confirmation: `pytest backend/tests/test_auth_session.py` → 5 passed.
- Inner-repo commit hash.
- Phase 2.1 proposal (if branch B/D required carry-forward).
</output>
