---
phase: 05-collection-naming-debt-resolution
plan: 04
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03]
files_modified:
  - pltu-tenayan-full-backup/DATABASE_SCHEMA.md
autonomous: false
requirements: [DEBT-02, DEBT-04, DEBT-05]
tags: [production-cutover, database-schema, cleanup, phase-5, debt-04, debt-05]

must_haves:
  truths:
    - "Before any production touch, the operator confirms (via a blocking human-verify checkpoint) that they have executed `mongodump` and verified the backup directory contains 13 .bson files."
    - "Before any production touch, the operator confirms (via a blocking human-verify checkpoint) that the dryrun `--apply --verify` on `pltu_tenayan_migration_dryrun` reported `[OK] Checksums match` and 4 `[SKIP] — does not exist` lines."
    - "Before the legacy-drop step, the operator confirms (via a blocking human-verify checkpoint) that ≥48 hours have elapsed since the read-switch deploy AND that the §4 observation checklist is fully satisfied."
    - "DATABASE_SCHEMA.md no longer contains any `legacy` markings (or contains at most 1 in an explicit `Resolved 2026-05-11 by Phase 5` annotation per RESEARCH Focus 8 Option A)."
    - "DATABASE_SCHEMA.md §13.2 `ai_conversations` subsection is deleted; §12.2 `settings` subsection is deleted; §9.1 heading is renamed `smartstock` (was `smartstock / smart_stock`); §10.1 heading is renamed `sumberpemakaian`."
    - "DATABASE_SCHEMA.md §16.1 Naming Koleksi standardization bullets are replaced with a `Resolved by Phase 5 (2026-05-11)` note."
  artifacts:
    - path: "pltu-tenayan-full-backup/DATABASE_SCHEMA.md"
      provides: "Schema doc with all `legacy` markings removed; Phase-3 duplicate-pair audit table converted to a Resolution Log per RESEARCH Focus 8 Option A"
      contains: "Resolved by Phase 5\\|smartstock\\|sumberpemakaian\\|app_settings\\|ai_chat_history"
  key_links:
    - from: "pltu-tenayan-full-backup/DATABASE_SCHEMA.md"
      to: ".planning/decisions/ADR-009..012"
      via: "Resolution Log cites each ADR by ID"
      pattern: "ADR-009|ADR-010|ADR-011|ADR-012"
    - from: "pltu-tenayan-full-backup/DATABASE_SCHEMA.md §Resolution Log"
      to: "pltu-tenayan-full-backup/MIGRATION_RUNBOOK.md"
      via: "Cross-reference for the resolution procedure"
      pattern: "MIGRATION_RUNBOOK.md"
---

<objective>
Drive the production cutover (D-13) and close DEBT-05 with the DATABASE_SCHEMA.md cleanup. This plan is **autonomous: false** — it contains 3 blocking human-verify checkpoints that gate the live mongod operations. The executor automates everything possible (DATABASE_SCHEMA.md edits, post-state verification), but pauses for explicit operator confirmation at each step that touches production data.

Purpose:
- Close DEBT-02 (applied step): operator runs the migration script on production after the dryrun has passed.
- Close DEBT-04 (production cutover): backup taken, read-switch deployed, observation window elapsed, legacy collections dropped.
- Close DEBT-05 (documentation cleanup): all `legacy` markings removed from DATABASE_SCHEMA.md.

Output: Inner-repo commit updating `pltu-tenayan-full-backup/DATABASE_SCHEMA.md`. The cutover itself produces no committed artifacts beyond the operator-recorded backup directory and the operational state of mongod. The operator's confirmation responses to the 3 checkpoints become part of the plan execution log.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@.planning/phases/05-collection-naming-debt-resolution/05-CONTEXT.md
@.planning/phases/05-collection-naming-debt-resolution/05-RESEARCH.md
@.planning/phases/05-collection-naming-debt-resolution/05-VALIDATION.md

@.planning/decisions/ADR-009-canonical-smartstock.md
@.planning/decisions/ADR-010-canonical-sumberpemakaian.md
@.planning/decisions/ADR-011-canonical-app-settings.md
@.planning/decisions/ADR-012-canonical-ai-chat-history.md

# The runbook the operator follows step-by-step during this plan
@pltu-tenayan-full-backup/MIGRATION_RUNBOOK.md

# The script the operator invokes
@pltu-tenayan-full-backup/scripts/migrate_collection_names.py

# The file being cleaned up in Task 05-04-04
@pltu-tenayan-full-backup/DATABASE_SCHEMA.md

<interfaces>
<!-- DATABASE_SCHEMA.md edit-points table (verbatim from RESEARCH Focus 8 lines 619-674,
     verified by grep 2026-05-11). Task 05-04-04 MUST address every row. -->

DATABASE_SCHEMA.md CLEANUP EDIT POINTS (apply in this exact order — Phase-3 audit
table conversion last):

| # | Approx Line | Current Content | Action |
|---|-------------|-----------------|--------|
| 1 | 13 | "Beberapa nama koleksi menunjukkan adanya legacy/transitional structure" | Rewrite: state collection names are standardized (post Phase 5 2026-05-11); remove "legacy/transitional structure" reference |
| 2 | 27 | `- smart_stock` (in collection list) | Delete the line entirely |
| 3 | 28 | `- sumber_pemakaian` (in collection list) | Delete the line entirely |
| 4 | 34 | `- ai_conversations` (in collection list) | Delete the line entirely |
| 5 | 37 | "smartstock dan smart_stock tampak hidup berdampingan; ini mengindikasikan naming legacy/transisi" | Delete the paragraph |
| 6 | 38 | "sumber_pemakaian dan sumberpemakaian juga mengindikasikan pola serupa" | Delete the paragraph |
| 7 | 40 | "ai_chat_history dan ai_conversations…" | Delete the paragraph |
| 8 | 374 | `## 9.1 smartstock / smart_stock` | Rename to `## 9.1 smartstock` |
| 9 | 412 | `## 9.2 Catatan naming` + paragraph "kode aktif list smart stock membaca dari db.smartstock, tetapi nama smart_stock juga muncul…" | Delete the entire §9.2 |
| 10 | 419 | `## 10.1 sumber_pemakaian / sumberpemakaian` | Rename to `## 10.1 sumberpemakaian` |
| 11 | 452 | "Seperti smart stock, ada indikasi naming legacy yang harus distandardisasi" | Delete the paragraph |
| 12 | 554 | `## 12.2 settings` subsection (entire section: "Muncul di kode, namun penggunaan aktifnya perlu diverifikasi. Potensial legacy/global settings collection.") | Delete the entire §12.2 |
| 13 | 576 | "Masih relevan untuk compatibility/history lama, namun fitur session-based yang lebih jelas kini menggunakan ai_conversations" | Rewrite: "ai_chat_history is the canonical AI chat history collection (per ADR-012). The previous ai_conversations duplicate has been resolved by Phase 5 (2026-05-11)." |
| 14 | 578 | `## 13.2 ai_conversations` subsection (entire section) | Delete the entire §13.2 |
| 15 | 629 | `ai_conversations.user_id → users.id` row in relations table | Delete the row |
| 16 | 651–656 | §16.1 Naming Koleksi with 4 standardization bullets | Replace bullets with: "Resolved by Phase 5 (2026-05-11). See ADR-009 (smartstock), ADR-010 (sumberpemakaian), ADR-011 (app_settings), ADR-012 (ai_chat_history). Procedure: pltu-tenayan-full-backup/MIGRATION_RUNBOOK.md." |
| 17 | 682 | `sumber_pemakaian` row in index recommendations table | Update to `sumberpemakaian` |
| 18 | 685 | `ai_conversations` row in index recommendations table | Delete the row |
| 19 | 695-738 | §"Duplicate Pair Active Read Targets (Phase-3 audit, 2026-05-10)" entire section | Convert to "Duplicate Pair Resolution Log" per RESEARCH Focus 8 Option A — see template below |

§"Duplicate Pair Resolution Log" REPLACEMENT TEMPLATE (RESEARCH Focus 8 Option A):

```markdown
## Duplicate Pair Resolution Log

Phase-3 plan 03-05 audited 4 duplicate-name collection pairs. Phase 5 (2026-05-11)
resolved each pair: a canonical name was locked in an ADR, the legacy collection
was confirmed empty in the live `pltu_tenayan` DB, server.py reads were switched
to the canonical name, and the empty legacy collection was dropped after a ≥48h
observation window. See `pltu-tenayan-full-backup/MIGRATION_RUNBOOK.md` for the
procedure.

| Pair | Canonical (kept) | Legacy (resolved) | ADR | Resolved | Notes |
|------|------------------|--------------------|------|----------|-------|
| smartstock / smart_stock | smartstock | smart_stock | [ADR-009](../../.planning/decisions/ADR-009-canonical-smartstock.md) | Resolved by Phase 5 (2026-05-11) | Legacy 0 records; absent from live DB at drop time |
| sumberpemakaian / sumber_pemakaian | sumberpemakaian | sumber_pemakaian | [ADR-010](../../.planning/decisions/ADR-010-canonical-sumberpemakaian.md) | Resolved by Phase 5 (2026-05-11) | Legacy 0 records; absent from live DB at drop time |
| app_settings / settings | app_settings | settings | [ADR-011](../../.planning/decisions/ADR-011-canonical-app-settings.md) | Resolved by Phase 5 (2026-05-11) | Legacy 0 records; 3 server.py reads switched (lines 2427, 2926, 4346); legacy absent from live DB |
| ai_chat_history / ai_conversations | ai_chat_history | ai_conversations | [ADR-012](../../.planning/decisions/ADR-012-canonical-ai-chat-history.md) | Resolved by Phase 5 (2026-05-11) | Legacy 0 records; zero server.py reads (no code edits required); legacy absent from live DB |

**Method (Phase 3 audit, preserved for historical context):**
`grep -nE 'db\.(smartstock|smart_stock|sumberpemakaian|sumber_pemakaian|app_settings|settings|ai_chat_history|ai_conversations)\.' backend/server.py`
plus live `db.<name>.countDocuments({})` against the production VPS
(read-only — no writes performed by the audit).
```

After the resolution log, DATABASE_SCHEMA.md must contain zero remaining `legacy`
markings (case-insensitive), with the exception of the words "Phase-3 audit" or
similar historical references that do NOT use the word "legacy". The validation
target is `grep -ic "legacy" DATABASE_SCHEMA.md` ≤ 1 (per VALIDATION row 05-04-04).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 05-04-01: HUMAN CHECKPOINT — Confirm mongodump backup taken + integrity-verified (DEBT-04 backup)</name>
  <files>
    /home/damnation/backups/pre-phase5-YYYYMMDD-HHMMSS/   (operator-created backup directory on the VPS — NOT a repo file)
  </files>
  <what-built>
    Plans 05-01..05-03 are committed. ADRs locked. Server.py legacy reads removed (Plan 05-02). MIGRATION_RUNBOOK.md exists with verbatim commands. The migration script and pytest gates are green.

    Plan 05-02 has NOT yet been deployed to the live VPS — the inner-repo commits exist on the developer machine and need to land on `103.150.197.225` before the read-switch takes effect. Before deploying, the operator MUST take a `mongodump` backup of the live `pltu_tenayan` DB.

    Reference: `pltu-tenayan-full-backup/MIGRATION_RUNBOOK.md` §1 "Backup Procedure (D-09)" has the verbatim commands.
  </what-built>
  <action>
    Operator action (do NOT proceed without operator sign-off):

    The executor pauses and instructs the operator to perform the §1 procedure from `pltu-tenayan-full-backup/MIGRATION_RUNBOOK.md` on the VPS `103.150.197.225`. The verbatim commands are reproduced below for convenience. The operator MUST verify all 4 sub-steps before replying with the resume signal.

    See `<how-to-verify>` for the exact verification steps. The executor takes no automated action in this task — the entire purpose of the checkpoint is to pause until the operator has demonstrably taken a verified backup.
  </action>
  <how-to-verify>
    Operator executes the following on the VPS `103.150.197.225`:

    1. SSH to the VPS as `damnation`.
    2. Run §1 of MIGRATION_RUNBOOK.md verbatim:
       ```bash
       BACKUP_DIR="/home/damnation/backups/pre-phase5-$(date +%Y%m%d-%H%M%S)"
       mongodump --uri mongodb://localhost:27017 --db pltu_tenayan --out "$BACKUP_DIR"
       ls "$BACKUP_DIR/pltu_tenayan/"*.bson | wc -l
       ```
       Expected: `13` (or the actual count from `mongosh pltu_tenayan --eval "db.getCollectionNames().length"`).
    3. Spot-check at least 3 .bson files with `bsondump`:
       ```bash
       for f in "$BACKUP_DIR/pltu_tenayan/"vessels.bson "$BACKUP_DIR/pltu_tenayan/"coa_reconciliation.bson "$BACKUP_DIR/pltu_tenayan/"smartstock.bson; do
         echo "=== $(basename $f) ==="; bsondump "$f" 2>/dev/null | head -1
       done
       ```
       Expected: each prints a JSON-shaped first document (no corruption).
    4. Tighten permissions: `chmod 700 /home/damnation/backups/`.

    Operator reports back with:
    - The exact `BACKUP_DIR` path (record it — needed if rollback is later required).
    - The .bson file count.
    - Confirmation that bsondump smoke passed for the 3 sampled collections.
  </how-to-verify>
  <verify>
    <automated>echo "Manual checkpoint — operator must reply with: approved: BACKUP_DIR=<path> bson_count=<N>. The executor does NOT auto-verify; resume-signal capture is the verification."</automated>
  </verify>
  <resume-signal>
    Reply with `approved: BACKUP_DIR=/home/damnation/backups/pre-phase5-YYYYMMDD-HHMMSS bson_count=13` (substitute actual values) — OR report issues (e.g., "mongodump failed: connection refused" → halt, investigate before proceeding).
  </resume-signal>
  <done>
    Operator has replied with the `approved: BACKUP_DIR=... bson_count=...` resume signal. The BACKUP_DIR value is recorded in the plan SUMMARY for rollback traceability. No proceed without this signal.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 05-04-02: HUMAN CHECKPOINT — Confirm dryrun on `pltu_tenayan_migration_dryrun` passed + read-switch deploy successful (DEBT-02 dryrun + DEBT-04 cutover)</name>
  <files>
    pltu-tenayan-full-backup/backend/server.py   (deployed to VPS; operator runs git pull + uvicorn restart — no repo file edit in this task)
  </files>
  <what-built>
    Following Task 05-04-01 sign-off, the operator runs §2 (Dryrun) and §3 (Read-Path Switch Deploy) of MIGRATION_RUNBOOK.md. The dryrun proves zero-data-loss against a production snapshot; the deploy applies the Plan 05-02 server.py edits to the live backend.
  </what-built>
  <action>
    Operator action (do NOT proceed without operator sign-off):

    The executor pauses and instructs the operator to execute §2 (Dryrun) followed by §3 (Read-Path Switch Deploy) of `pltu-tenayan-full-backup/MIGRATION_RUNBOOK.md` on the VPS, in that order. The dryrun MUST complete with `[OK] Checksums match` before the read-switch deploy proceeds. After the deploy, the operator runs the AI-module smoke tests from RESEARCH Focus 6.

    See `<how-to-verify>` for the step-by-step procedure with verbatim commands. The executor takes no automated action — this is a pure pause-for-operator-confirmation gate.
  </action>
  <how-to-verify>
    Operator executes the following on the VPS:

    **Step A — Dryrun (MIGRATION_RUNBOOK.md §2):**

    1. Run the §2 command block verbatim:
       ```bash
       SNAP_DIR="/tmp/pltu_tenayan_snapshot_$(date +%Y%m%d-%H%M%S)"
       mongodump --uri mongodb://localhost:27017 --db pltu_tenayan --out "$SNAP_DIR"
       mongorestore \
         --uri mongodb://localhost:27017 \
         --nsFrom 'pltu_tenayan.*' \
         --nsTo 'pltu_tenayan_migration_dryrun.*' \
         "$SNAP_DIR"
       cd /home/damnation/emits/pltu-tenayan-full-backup
       python3 scripts/migrate_collection_names.py --target-db pltu_tenayan_migration_dryrun --apply --verify
       ```
    2. Expected stdout:
       - 4 `[SKIP] <legacy> — does not exist` lines (because the legacy collections are absent from the live DB; the dryrun snapshot inherits that absence).
       - `[OK] Checksums match before and after. Zero data loss confirmed.`
    3. Drop dryrun DB + clean snapshot dir:
       ```bash
       mongosh pltu_tenayan_migration_dryrun --eval "db.dropDatabase()"
       rm -rf "$SNAP_DIR"
       ```

    **Step B — Read-switch deploy (MIGRATION_RUNBOOK.md §3):**

    1. `cd /home/damnation/emits/pltu-tenayan-full-backup && git pull --ff-only`
    2. Restart uvicorn per LOCAL_SETUP.md §"VPS Service Recovery" (verbatim).
    3. Smoke-test per RESEARCH Focus 6 / MIGRATION_RUNBOOK §3 step 3 — source `TEST_ADMIN_EMAIL` / `TEST_ADMIN_PASSWORD`, get TOKEN, hit `/api/ai/quick/smart-stock` and `/api/ai/quick/coa-alerts`.
    4. Expected: `/api/ai/quick/smart-stock` returns `total_penerimaan > 0` (was 0 pre-cutover — this is the silent-data-gap fix from ADR-009). `/api/ai/quick/coa-alerts` returns 200. `/api/health` returns 200.

    Operator reports back with:
    - Dryrun stdout (paste the `[SKIP]`/`[OK]` lines).
    - Confirmation that uvicorn restart succeeded (`curl /api/health` returns 200).
    - One-line excerpt from the `/api/ai/quick/smart-stock` response showing `total_penerimaan > 0`.
    - The cutover timestamp (for the §4 observation-window record).
  </how-to-verify>
  <verify>
    <automated>echo "Manual checkpoint — operator must reply with: approved: dryrun_passed=true cutover_at=<ts> smartstock_total=<N>. No automated verification."</automated>
  </verify>
  <resume-signal>
    Reply with `approved: dryrun_passed=true cutover_at=YYYY-MM-DD HH:MM TZ smartstock_total=<N>` — OR report issues (any 5xx / connection refused / checksum mismatch / `[HALT]` line → halt; consult MIGRATION_RUNBOOK.md §7a code-only rollback).
  </resume-signal>
  <done>
    Operator has replied with the `approved: dryrun_passed=true cutover_at=...` resume signal. The cutover timestamp is recorded — used to compute the earliest legacy-drop time (cutover_at + 48h).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 05-04-03: HUMAN CHECKPOINT — Confirm ≥48h observation window elapsed + run `--apply` on production (DEBT-04 legacy-drop)</name>
  <files>
    pltu-tenayan-full-backup/scripts/migrate_collection_names.py   (operator invokes from VPS with --target-db pltu_tenayan --apply --verify — no repo file edit in this task)
  </files>
  <what-built>
    Tasks 05-04-01 and 05-04-02 have signed off. The read-switch deploy has been live for ≥48 hours. This checkpoint authorises the operator to run `migrate_collection_names.py --apply --target-db pltu_tenayan` against the live DB.

    Per the migration script's safety guard (Plan 05-02 Task 05-02-02), `--target-db pltu_tenayan` must be passed explicitly — there is no way to drop production by accident.
  </what-built>
  <action>
    Operator action (do NOT proceed without operator sign-off):

    The executor pauses and instructs the operator to (a) verify the observation window has elapsed, (b) re-run the pytest regression gate, (c) execute the production `--apply --verify` of the migration script. If any sub-step fails, HALT and consult `MIGRATION_RUNBOOK.md §7` Rollback.

    See `<how-to-verify>` for the step-by-step procedure. The executor takes no automated action — this is a pause-for-operator-confirmation gate that produces the actual production drop.
  </action>
  <how-to-verify>
    Operator executes the following on the VPS:

    **Step A — Verify observation window:**
    1. Calculate: `cutover_at + 48 hours <= now()` MUST hold. If not, wait.
    2. Run the §4 observation checklist (MIGRATION_RUNBOOK.md §4) end-to-end one more time — backend.log clean, `/api/health` 200, AI module smoke still returning real data, `mongosh pltu_tenayan` shows no legacy collection names.

    **Step B — pytest regression gate (§5):**
    1. Source credentials per TEST-RUNNER.md One-time setup.
    2. ```bash
       cd /home/damnation/emits/pltu-tenayan-full-backup/backend
       .venv/bin/pytest tests/ -q
       echo "exit=$?"
       ```
    3. Expected: `exit=0` and summary `N passed in T.Ts`. If pytest fails, HALT — do not run `--apply` on production. Investigate the failure per D-14.

    **Step C — Production `--apply --verify` (§6):**
    1. Final pre-flight dry-run:
       ```bash
       cd /home/damnation/emits/pltu-tenayan-full-backup
       python3 scripts/migrate_collection_names.py --target-db pltu_tenayan --dry-run
       ```
       Expected: 4 `[SKIP] <legacy> — does not exist` lines.
    2. If output is clean, proceed with `--apply --verify`:
       ```bash
       python3 scripts/migrate_collection_names.py --target-db pltu_tenayan --apply --verify
       ```
       Expected: 4 `[SKIP]` lines + `[OK] Checksums match before and after.`
    3. Post-state verification:
       ```bash
       mongosh pltu_tenayan --eval "['smart_stock','sumber_pemakaian','settings','ai_conversations'].forEach(c=>print(c+': '+db.getCollectionNames().includes(c)))"
       ```
       Expected: all 4 print `false`.

    Operator reports back with:
    - Observation window confirmation (cutover_at + 48h <= now).
    - pytest exit code (must be 0).
    - The 4 `[SKIP]` lines + the `[OK] Checksums match` line.
    - The post-state `false`-x4 mongosh output.
  </how-to-verify>
  <verify>
    <automated>echo "Manual checkpoint — operator must reply with: approved: observed_at=<ts> pytest=0 apply_passed=true post_state=all_false. No automated verification."</automated>
  </verify>
  <resume-signal>
    Reply with `approved: observed_at=YYYY-MM-DD HH:MM TZ pytest=0 apply_passed=true post_state=all_false` — OR halt and reference MIGRATION_RUNBOOK.md §7 Rollback if any step fails. If the script reports `[HALT]` (a legacy collection has > 0 documents — D-07 trip), HALT and consult the operator: investigate which silent write path created a row before deciding next action; the script has already prevented data loss.
  </resume-signal>
  <done>
    Operator has replied with the `approved: observed_at=... pytest=0 apply_passed=true post_state=all_false` resume signal. DEBT-04 (production cutover + legacy-drop after observation window) is closed. The plan now proceeds to the auto task 05-04-04 (DATABASE_SCHEMA.md cleanup).
  </done>
</task>

<task type="auto">
  <name>Task 05-04-04: Clean DATABASE_SCHEMA.md of `legacy` markings + convert Phase-3 audit table to Resolution Log (DEBT-05)</name>
  <files>
    pltu-tenayan-full-backup/DATABASE_SCHEMA.md
  </files>
  <read_first>
    - .planning/phases/05-collection-naming-debt-resolution/05-RESEARCH.md §"Research Focus 8: DATABASE_SCHEMA.md Cleanup Scope (DEBT-05)" (lines 619-674) — exact edit-points table
    - pltu-tenayan-full-backup/DATABASE_SCHEMA.md — read in chunks: lines 1-50 (intro + collection list), 370-460 (§§9.1, 9.2, 10.1 + naming notes), 540-585 (§§12.2, 13.1, 13.2), 625-660 (relations table, §16.1 standardization bullets), 678-740 (index table + Phase-3 audit table — RESEARCH Focus 8 line targets)
    - The 19 edit points in this plan's `<interfaces>` block — apply EVERY row
  </read_first>
  <action>
    Apply the 19 cleanup edits to `pltu-tenayan-full-backup/DATABASE_SCHEMA.md`. Use the Edit tool one logical edit at a time (do NOT Write-overwrite the file — it is a 740+ line schema doc with content unrelated to Phase 5).

    Process the 19 rows in the `<interfaces>` table in order. For each row:

    1. If "Action" = "Delete the line entirely" / "Delete the paragraph": use Edit with `old_string` = the line + 1 line of context above/below for uniqueness, `new_string` = the context lines only (the target line is omitted).
    2. If "Action" = "Rename to <new>": use Edit with `old_string` = the heading line (`## 9.1 smartstock / smart_stock`), `new_string` = the new heading (`## 9.1 smartstock`).
    3. If "Action" = "Rewrite": use Edit with `old_string` = the full target paragraph, `new_string` = the rewrite text (referencing the canonical name and citing the relevant ADR).
    4. For row #16 (§16.1 Naming Koleksi standardization bullets): replace the 4 bullets verbatim with the text shown in `<interfaces>`: "Resolved by Phase 5 (2026-05-11). See ADR-009 (smartstock), ADR-010 (sumberpemakaian), ADR-011 (app_settings), ADR-012 (ai_chat_history). Procedure: pltu-tenayan-full-backup/MIGRATION_RUNBOOK.md."
    5. For row #19 (§"Duplicate Pair Active Read Targets" → §"Duplicate Pair Resolution Log"): use Edit with `old_string` = the entire current §"Duplicate Pair Active Read Targets (Phase-3 audit, 2026-05-10)" section (lines 695-738, including the audit table, the "Notes on Both names actively read" paragraphs, the **Method:** paragraph, and the **Phase 5 dependency:** paragraph). Replace it with the VERBATIM "Duplicate Pair Resolution Log" template from this plan's `<interfaces>` block.

    **Important — section ordering:** apply edits in the order listed in `<interfaces>` (row #1 first, row #19 last). Earlier edits do not affect later line numbers in a destabilising way because each edit either removes a line or rewrites a paragraph in place; the line numbers of subsequent rows MAY drift by 1-3 lines per deletion — re-grep before each edit if uncertain (`grep -n "<unique substring>" DATABASE_SCHEMA.md`).

    **Mitigation for line-number drift between RESEARCH Focus 8 (audited 2026-05-11) and HEAD:** treat the RESEARCH line numbers as APPROXIMATE. The Edit tool requires `old_string` to be uniquely matched in the file — that uniqueness check is your guarantee that the right paragraph is being edited. If a paragraph's exact text has drifted, expand `old_string` to include more context until it matches uniquely.

    After all 19 edits, verify:
    - `grep -ic "legacy" pltu-tenayan-full-backup/DATABASE_SCHEMA.md` returns ≤ 1 (per VALIDATION row 05-04-04). If 0, that's also acceptable. If > 1, identify the remaining occurrences and rewrite — possibly a historical mention in section 17 or 18 that needs a "Resolved" annotation.
    - `grep -c "Resolved by Phase 5" pltu-tenayan-full-backup/DATABASE_SCHEMA.md` returns ≥ 1 (the §16.1 replacement) and ≥ 4 in the Resolution Log table rows.
    - `grep -c "smart_stock\|sumber_pemakaian\|ai_conversations" pltu-tenayan-full-backup/DATABASE_SCHEMA.md` should be very low — only references that exist in the Resolution Log "Legacy (resolved)" column.
    - `grep -c "ADR-009\|ADR-010\|ADR-011\|ADR-012" pltu-tenayan-full-backup/DATABASE_SCHEMA.md` returns ≥ 4 (each ADR cited in the Resolution Log + §16.1).
  </action>
  <acceptance_criteria>
    - `grep -ic "legacy" pltu-tenayan-full-backup/DATABASE_SCHEMA.md` returns ≤ 1 (per VALIDATION row 05-04-04)
    - `grep -c "Resolved by Phase 5" pltu-tenayan-full-backup/DATABASE_SCHEMA.md` returns ≥ 4 (one per ADR row in the Resolution Log + §16.1 replacement)
    - `grep -cE "ADR-009|ADR-010|ADR-011|ADR-012" pltu-tenayan-full-backup/DATABASE_SCHEMA.md` returns ≥ 4
    - The previously-existing `## 9.2` "Catatan naming" section is gone: `grep -c "## 9.2 Catatan naming" pltu-tenayan-full-backup/DATABASE_SCHEMA.md` returns 0
    - The previously-existing `## 12.2 settings` section is gone: `grep -c "^## 12.2 settings" pltu-tenayan-full-backup/DATABASE_SCHEMA.md` returns 0
    - The previously-existing `## 13.2 ai_conversations` section is gone: `grep -c "^## 13.2 ai_conversations" pltu-tenayan-full-backup/DATABASE_SCHEMA.md` returns 0
    - The Resolution Log table header is present: `grep -c "Duplicate Pair Resolution Log" pltu-tenayan-full-backup/DATABASE_SCHEMA.md` returns ≥ 1
    - The Resolution Log table has 4 data rows: `grep -cE "^\| (smartstock / smart_stock|sumberpemakaian / sumber_pemakaian|app_settings / settings|ai_chat_history / ai_conversations)" pltu-tenayan-full-backup/DATABASE_SCHEMA.md` returns 4
    - File still parses as Markdown: `wc -l < pltu-tenayan-full-backup/DATABASE_SCHEMA.md` returns ≥ 600 (the file is ~740 lines pre-edit; net deletions should not reduce it below 600)
  </acceptance_criteria>
  <verify>
    <automated>FILE=pltu-tenayan-full-backup/DATABASE_SCHEMA.md && [ "$(grep -ic legacy "$FILE")" -le 1 ] && [ "$(grep -c 'Resolved by Phase 5' "$FILE")" -ge 4 ] && [ "$(grep -cE 'ADR-009|ADR-010|ADR-011|ADR-012' "$FILE")" -ge 4 ] && [ "$(grep -c '## 9.2 Catatan naming' "$FILE")" = "0" ] && [ "$(grep -c '^## 12.2 settings' "$FILE")" = "0" ] && [ "$(grep -c '^## 13.2 ai_conversations' "$FILE")" = "0" ] && [ "$(grep -c 'Duplicate Pair Resolution Log' "$FILE")" -ge 1 ] && [ "$(wc -l < "$FILE")" -ge 600 ] && echo "PASS"</automated>
  </verify>
  <done>
    DATABASE_SCHEMA.md is free of `legacy` markings (≤ 1 occurrence in a historical-only annotation if needed), the Phase-3 audit table is converted to a Resolution Log citing all 4 ADRs, the §9.2 / §12.2 / §13.2 stub sections are deleted, and §16.1 Naming Koleksi standardization bullets are replaced with the Resolved-by-Phase-5 note.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan executor → live mongod | The 3 human-verify checkpoints (05-04-01, -02, -03) are the trust boundary. The executor cannot bypass them. |
| Operator → backup directory | The operator owns `/home/damnation/backups/` and the retention policy. The plan does not auto-delete or auto-create anything outside `pltu-tenayan-full-backup/`. |
| DATABASE_SCHEMA.md edits → downstream readers | DATABASE_SCHEMA.md is the canonical schema doc; bad edits silently mislead future contributors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-premature-drop-01 | Tampering | Task 05-04-03 (legacy-drop on production) | mitigate | `autonomous: false`. The checkpoint requires explicit operator confirmation that ≥48 hours have elapsed AND that pytest exited 0 AND that the §4 observation checklist is satisfied. The migration script's own safety guard (require `--target-db pltu_tenayan` explicitly, plus the pre-drop count==0 guard from D-07) is a second-tier defence. |
| T-backup-not-actually-taken-01 | Repudiation | Task 05-04-01 (backup checkpoint) | mitigate | Operator must report the EXACT `BACKUP_DIR` path and the `.bson` file count. The resume signal embeds these values; the SUMMARY records them. If rollback is later needed, the path is on file. |
| T-deploy-without-dryrun-01 | Tampering | Task 05-04-02 (cutover sequence) | mitigate | Task 05-04-02 explicitly orders Dryrun FIRST then Deploy. The how-to-verify pastes both steps into one operator-visible block. The runbook §2 and §3 are also in that strict order. |
| T-schema-doc-drift-01 | Tampering | Task 05-04-04 (DATABASE_SCHEMA.md) | mitigate | Each of the 19 edit points is enumerated in the `<interfaces>` block. The Edit tool's unique-substring requirement on `old_string` guarantees the right paragraph is targeted. Acceptance criteria includes grep gates that the §9.2 / §12.2 / §13.2 sections are gone AND that the Resolution Log table has exactly 4 data rows AND that all 4 ADRs are cited. |
| T-legacy-still-grepped-01 | Tampering | Task 05-04-04 final grep gate | mitigate | `grep -ic "legacy" DATABASE_SCHEMA.md ≤ 1` is the explicit gate. If more legacy occurrences remain post-edit, the verify command fails and the executor must do another Edit pass. |
| T-pytest-skip-on-mongo-down-01 | Repudiation | Task 05-04-03 Step B (pytest gate) | accept | If mongod is down on the developer machine during the gate run, pytest may skip the migration-script tests but still exit 0 on the non-DB suite. This is the existing Phase-4 behaviour. Mitigation is operational: the gate is to be run on the VPS where mongod is up. |
| T-checkpoint-bypass-01 | Tampering | autonomous: false enforcement | accept | The plan declares `autonomous: false` in frontmatter. The execute-plan orchestrator pauses at each `checkpoint:human-verify` task and refuses to advance without a resume signal. If a malicious actor bypasses the orchestrator, that's outside the scope of this plan's threat model — it's a question of orchestrator integrity, not plan design. |
</threat_model>

<verification>
After all 4 tasks complete (3 checkpoints signed off + DATABASE_SCHEMA.md cleaned):

```bash
FILE=pltu-tenayan-full-backup/DATABASE_SCHEMA.md

# DEBT-05: legacy markings purged
LEGACY_COUNT=$(grep -ic 'legacy' "$FILE")
[ "$LEGACY_COUNT" -le 1 ] && echo "[OK] legacy=$LEGACY_COUNT"

# DEBT-05: Resolution Log present
[ "$(grep -c 'Resolved by Phase 5' "$FILE")" -ge 4 ] && echo "[OK] Resolution Log"

# DEBT-05: ADR cross-links
[ "$(grep -cE 'ADR-009|ADR-010|ADR-011|ADR-012' "$FILE")" -ge 4 ] && echo "[OK] all 4 ADRs cited"

# Stub sections removed
[ "$(grep -c '## 9.2 Catatan naming' "$FILE")" = "0" ] && echo "[OK] §9.2 removed"
[ "$(grep -c '^## 12.2 settings' "$FILE")" = "0" ] && echo "[OK] §12.2 removed"
[ "$(grep -c '^## 13.2 ai_conversations' "$FILE")" = "0" ] && echo "[OK] §13.2 removed"

# Resolution Log table
[ "$(grep -c 'Duplicate Pair Resolution Log' "$FILE")" -ge 1 ] && echo "[OK] Log header"

# Production state (manual — operator reports from VPS)
# Per Task 05-04-03 resume signal, expect:
#  - mongosh: smart_stock=false, sumber_pemakaian=false, settings=false, ai_conversations=false
#  - migration script --apply: 4 [SKIP] + [OK] Checksums match
#  - pytest: exit 0

echo "PHASE 5 PLAN 05-04 VERIFIED"
```
</verification>

<success_criteria>
- Task 05-04-01 signed off: mongodump backup taken, .bson count verified, bsondump smoke passed.
- Task 05-04-02 signed off: dryrun produced 4 `[SKIP]` + `[OK] Checksums match`; uvicorn restart succeeded; `/api/ai/quick/smart-stock` now returns real data (total_penerimaan > 0).
- Task 05-04-03 signed off: ≥48h observation window elapsed; pytest exit 0; `--apply --verify` produced 4 `[SKIP]` + `[OK] Checksums match`; post-state mongosh reports 4× `false`.
- Task 05-04-04 auto-passed: DATABASE_SCHEMA.md cleanup verified by 8 grep gates.
- DEBT-02, DEBT-04, DEBT-05 all closed.
- Phase 5 ready for `/gsd-verify-work`.
</success_criteria>

<output>
After all 4 tasks complete, create `.planning/phases/05-collection-naming-debt-resolution/05-04-SUMMARY.md` documenting:
- Operator-reported values from each checkpoint:
  - BACKUP_DIR + .bson count (Task 05-04-01)
  - cutover_at timestamp + smartstock_total post-cutover (Task 05-04-02)
  - observed_at + pytest exit + post-state mongosh output (Task 05-04-03)
- DATABASE_SCHEMA.md grep-gate output (Task 05-04-04, paste verbatim).
- Final state: DEBT-01..DEBT-05 all closed; Phase 5 ready for `/gsd-verify-work`.
- Update STATE.md and ROADMAP.md (outer repo) to mark Phase 5 complete (commit_docs=true per `.planning/config.json`).
</output>
