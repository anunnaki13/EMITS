---
phase: 01-production-audit-onboarding
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - pltu-tenayan-full-backup/docs/audit/LOGIN_BUG.md
autonomous: true
requirements:
  - AUDIT-04
must_haves:
  truths:
    - "A reviewer can open LOGIN_BUG.md and reproduce the login and registration failure path step-by-step against the live system at http://103.150.197.225:3013."
    - "Each repro path lists observed behavior (HTTP codes, browser console output, network responses) and expected behavior, with a clear divergence callout."
    - "At least one suspected component is named with file:line evidence (Login.js role-select Radix combobox is the working hypothesis from intel; the repro must either confirm or refute it)."
    - "The benign ResizeObserver suppressor already applied at frontend/public/index.html is documented as 'temporary mitigation' so Phase 2 knows the surface state."
    - "Backend-side login behavior is captured via direct curl against POST /api/auth/login (separating frontend-only from end-to-end failure)."
  artifacts:
    - path: pltu-tenayan-full-backup/docs/audit/LOGIN_BUG.md
      provides: "Reproducible login + register bug repro with observed-vs-expected divergence and named suspect component(s)."
      contains: "## Reproduction"
      min_lines: 60
  key_links:
    - from: pltu-tenayan-full-backup/docs/audit/LOGIN_BUG.md
      to: pltu-tenayan-full-backup/frontend/src/pages/Login.js
      via: "Suspect component section names file:line of the role-select Radix component."
      pattern: "Login.js"
    - from: pltu-tenayan-full-backup/docs/audit/LOGIN_BUG.md
      to: http://103.150.197.225:8013/api/auth/login
      via: "Backend repro section curls the live login endpoint directly to isolate API behavior from UI behavior."
      pattern: "curl"
    - from: pltu-tenayan-full-backup/docs/audit/LOGIN_BUG.md
      to: pltu-tenayan-full-backup/frontend/public/index.html
      via: "Mitigation status section documents the existing ResizeObserver suppressor."
      pattern: "ResizeObserver"
---

<objective>
Produce LOGIN_BUG.md: a written, reproducible record of the login and registration failure paths against the live VPS — steps, observed vs expected behavior, network/console evidence, and a named suspect component. This is Phase 1's read-only deliverable; Phase 2 owns the actual fix.

Purpose: Phase 2 (Authentication Stabilization) needs a concrete repro to drive AUTHFIX-01..04 (fix login, write regression test). Without a written repro, Phase 2 starts by re-deriving the symptom — wasted context. The user explicitly noted the underlying registration flow has not been validated end-to-end yet, so this plan must capture BOTH the existing-user login path AND the new-user register path.
Output: pltu-tenayan-full-backup/docs/audit/LOGIN_BUG.md (markdown, narrative + evidence blocks, grep-verifiable).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/intel/constraints.md
@pltu-tenayan-full-backup/frontend/src/pages/Login.js
@pltu-tenayan-full-backup/frontend/src/contexts/AuthContext.js
@pltu-tenayan-full-backup/frontend/public/index.html

<interfaces>
<!-- Auth contract under test (CONS-auth-header) -->
- Endpoints: POST /api/auth/login, POST /api/auth/register, GET /api/auth/me
- Public (no token): /api/auth/login, /api/auth/register, /api/, /api/health
- Token: JWT bearer, Authorization: Bearer &lt;JWT&gt;
- Error codes: 400 validation, 401 invalid/expired, 403 role denied, 404 not found, 500 internal

<!-- Reported symptom (from PHASE planning context) -->
- Runtime error: "ResizeObserver loop completed with undelivered notifications"
- Triggered by: Login.js role-select Radix component
- Mitigation in place: benign suppressor in frontend/public/index.html
- Outstanding: register flow not validated end-to-end

<!-- Test credentials -->
- Live in pltu-tenayan-full-backup/memory/test_credentials.md (gitignored).
- NEVER inline into LOGIN_BUG.md or any committed file.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the backend-only repro and capture network/UI evidence for the frontend repro</name>
  <files>pltu-tenayan-full-backup/docs/audit/.work/login-backend.txt, pltu-tenayan-full-backup/docs/audit/.work/register-backend.txt, pltu-tenayan-full-backup/docs/audit/.work/login-frontend-evidence.md</files>
  <read_first>
    - pltu-tenayan-full-backup/memory/test_credentials.md (read at probe time only — do NOT copy into committed artifacts)
    - pltu-tenayan-full-backup/frontend/src/pages/Login.js (full file — identify the role-select Radix component, the form submit handler, where it calls AuthContext.login)
    - pltu-tenayan-full-backup/frontend/src/contexts/AuthContext.js (full file — login function and rehydrate /api/auth/me path)
    - pltu-tenayan-full-backup/frontend/public/index.html (find the ResizeObserver suppressor block — record line numbers)
  </read_first>
  <action>
    Capture three evidence files. Read-only — no source modifications, no service restarts.

    1. mkdir -p pltu-tenayan-full-backup/docs/audit/.work

    2. Backend-only login repro. Source test creds from memory/test_credentials.md at probe time and use them via a temp file or env vars; NEVER write them into the .work/ output. Capture:

       - Status code, response headers, response body for valid credentials:
         curl -sS -i -X POST http://103.150.197.225:8013/api/auth/login \
           -H 'Content-Type: application/json' \
           --data-binary @&lt;tempfile-with-valid-creds&gt; \
           &gt; pltu-tenayan-full-backup/docs/audit/.work/login-backend.txt 2&gt;&amp;1

       - Then append a separator and a second probe with deliberately invalid credentials (email that exists, password wrong) to confirm the 401 path:
         echo '--- INVALID CREDS ---' &gt;&gt; pltu-tenayan-full-backup/docs/audit/.work/login-backend.txt
         curl -sS -i -X POST http://103.150.197.225:8013/api/auth/login \
           -H 'Content-Type: application/json' \
           -d '{"email":"&lt;known-user-email-from-creds-file&gt;","password":"definitely-not-real-12345"}' \
           &gt;&gt; pltu-tenayan-full-backup/docs/audit/.work/login-backend.txt 2&gt;&amp;1

       - Then a third probe with a malformed body to confirm the 400 path:
         echo '--- MALFORMED ---' &gt;&gt; pltu-tenayan-full-backup/docs/audit/.work/login-backend.txt
         curl -sS -i -X POST http://103.150.197.225:8013/api/auth/login \
           -H 'Content-Type: application/json' \
           -d '{"email":"not-an-email"}' \
           &gt;&gt; pltu-tenayan-full-backup/docs/audit/.work/login-backend.txt 2&gt;&amp;1

       Before saving, redact any captured token: replace any occurrence matching the regex 'JWT_PREFIX[A-Za-z0-9._-]{20,}' with '&lt;REDACTED-JWT&gt;' and any occurrence of the cleartext password from memory/test_credentials.md with '&lt;REDACTED-PASSWORD&gt;'. Verify by grep that no JWT and no cleartext password remain in login-backend.txt before proceeding.

    3. Backend-only register repro. The user explicitly flagged the register flow as "not validated end-to-end". Use a synthetic email that does not collide with existing users (e.g., audit-probe-&lt;timestamp&gt;@example.invalid) and a synthetic password (NOT the real test password — generate a throwaway like  pwgen 24 1  or a fixed obviously-fake string). Capture all three role values supported per IMPLICIT-004:

       for ROLE in admin operator viewer; do
         echo "--- REGISTER role=$ROLE ---" &gt;&gt; pltu-tenayan-full-backup/docs/audit/.work/register-backend.txt
         curl -sS -i -X POST http://103.150.197.225:8013/api/auth/register \
           -H 'Content-Type: application/json' \
           -d '{"email":"audit-probe-'"$ROLE"'-'"$(date +%s)"'@example.invalid","password":"AuditProbe!throwaway-2026","name":"Audit Probe '"$ROLE"'","role":"'"$ROLE"'"}' \
           &gt;&gt; pltu-tenayan-full-backup/docs/audit/.work/register-backend.txt 2&gt;&amp;1
       done

       This DOES insert documents into the live `users` collection. That is a real-side-effect; the audit accepts it because:
         (a) The phase brief explicitly says register has not been validated end-to-end and Phase 1 must capture repro;
         (b) The synthetic emails use the .invalid TLD (RFC 6761 reserved) and a recognizable prefix `audit-probe-` so Phase 5 can clean them up.

       Append a final cleanup-note row to register-backend.txt (NOT a deletion — just a record):
       echo '--- CLEANUP-NOTE ---' &gt;&gt; pltu-tenayan-full-backup/docs/audit/.work/register-backend.txt
       echo 'Three audit-probe-* users were inserted with role admin/operator/viewer. They use email TLD .invalid and prefix audit-probe-. Phase 2 (regression test) or Phase 5 (cleanup) may remove them via a deleteMany filter on email regex /^audit-probe-/.' &gt;&gt; pltu-tenayan-full-backup/docs/audit/.work/register-backend.txt

       Redact any returned JWTs the same way as in step 2.

    4. Frontend evidence collection. The repro itself happens in the browser at http://103.150.197.225:3013. The agent cannot drive a real browser interactively, so the evidence file is a structured worksheet the executor fills in by:
       (a) reading Login.js to identify the visible UI elements (form fields, role select, submit button) — record their data-testid values or their JSX line numbers;
       (b) listing the precise click sequence that triggers the bug (Open /login → focus role select → keyboard arrow / mouse click → ResizeObserver error fires);
       (c) for each step, listing the EXPECTED outcome and the OBSERVED outcome.

       The OBSERVED outcomes are filled from one of two sources:
       - If the executor has access to a headless browser (puppeteer, playwright, chromium-headless) AND the running environment allows it: launch http://103.150.197.225:3013/login, intercept console + network, click through the repro, save console errors and network responses to login-frontend-evidence.md.
       - Otherwise: mark each OBSERVED row as "browser-driven verification deferred to operator" and provide the exact DevTools commands the operator should run (Console tab error capture; Network tab filter on /api/auth/; Application tab → Local Storage to confirm/deny token write). The structure must still be valid markdown and the document must explicitly note "automation-not-available: see Operator playbook below" so Phase 2 knows the residual gap.

       Either way, write a structured markdown file at pltu-tenayan-full-backup/docs/audit/.work/login-frontend-evidence.md containing:
       - "## UI element inventory" — bullet list of the role-select component file:line (from Login.js) and its surrounding form inputs.
       - "## Repro click path" — numbered list of clicks/keypresses.
       - "## Expected vs observed" — table per step.
       - "## Console error excerpt" — verbatim error text (or "deferred — Operator playbook").
       - "## Network observations" — POST /api/auth/login request payload shape (without password value), response status, response body shape (without token value).
       - "## Operator playbook" — present unconditionally so a human can re-verify any "deferred" rows in 5 minutes.

    5. Sanity-check redaction:
       grep -E 'JWT_PREFIX[A-Za-z0-9._-]{20,}' pltu-tenayan-full-backup/docs/audit/.work/login-backend.txt pltu-tenayan-full-backup/docs/audit/.work/register-backend.txt
       Expected: no matches.
       grep -F "$REAL_PASSWORD" pltu-tenayan-full-backup/docs/audit/.work/login-backend.txt pltu-tenayan-full-backup/docs/audit/.work/register-backend.txt
       Expected: no matches.

    Do NOT modify Login.js, AuthContext.js, public/index.html, or any other source. Do NOT remove the ResizeObserver suppressor. Phase 2 owns the fix.
  </action>
  <verify>
    <automated>test -s pltu-tenayan-full-backup/docs/audit/.work/login-backend.txt &amp;&amp; test -s pltu-tenayan-full-backup/docs/audit/.work/register-backend.txt &amp;&amp; test -s pltu-tenayan-full-backup/docs/audit/.work/login-frontend-evidence.md &amp;&amp; ! grep -qE 'JWT_PREFIX[A-Za-z0-9._-]{20,}' pltu-tenayan-full-backup/docs/audit/.work/login-backend.txt &amp;&amp; ! grep -qE 'JWT_PREFIX[A-Za-z0-9._-]{20,}' pltu-tenayan-full-backup/docs/audit/.work/register-backend.txt &amp;&amp; grep -q 'INVALID CREDS' pltu-tenayan-full-backup/docs/audit/.work/login-backend.txt &amp;&amp; grep -q 'CLEANUP-NOTE' pltu-tenayan-full-backup/docs/audit/.work/register-backend.txt</automated>
  </verify>
  <acceptance_criteria>
    - login-backend.txt has three sections (valid creds, INVALID CREDS, MALFORMED) and contains no JWT (no string matching `JWT_PREFIX[A-Za-z0-9._-]{20,}`) and no cleartext password.
    - register-backend.txt has three role probes (admin, operator, viewer) and a CLEANUP-NOTE row, and contains no JWT.
    - login-frontend-evidence.md exists, is non-empty, and contains the five required sections (UI element inventory, Repro click path, Expected vs observed, Console error excerpt, Network observations) plus an Operator playbook.
    - No source file under frontend/ or backend/ was modified.
    - The ResizeObserver suppressor in frontend/public/index.html was NOT removed (verified by `grep -q ResizeObserver pltu-tenayan-full-backup/frontend/public/index.html` returning a match).
  </acceptance_criteria>
  <done>Three evidence artifacts (.work/login-backend.txt, .work/register-backend.txt, .work/login-frontend-evidence.md) capture backend behavior, register behavior, and frontend repro structure — ready for Task 2 to weave into LOGIN_BUG.md.</done>
</task>

<task type="auto">
  <name>Task 2: Write LOGIN_BUG.md with reproductions, observed-vs-expected, and named suspect components</name>
  <files>pltu-tenayan-full-backup/docs/audit/LOGIN_BUG.md</files>
  <read_first>
    - pltu-tenayan-full-backup/docs/audit/.work/login-backend.txt
    - pltu-tenayan-full-backup/docs/audit/.work/register-backend.txt
    - pltu-tenayan-full-backup/docs/audit/.work/login-frontend-evidence.md
    - pltu-tenayan-full-backup/frontend/src/pages/Login.js (re-read — Task 2 cites file:line for the suspect component)
    - pltu-tenayan-full-backup/frontend/public/index.html (find ResizeObserver suppressor — cite line range)
  </read_first>
  <action>
    Build pltu-tenayan-full-backup/docs/audit/LOGIN_BUG.md with the following structure:

    ```
    # Login + Register Bug Repro

    **Live frontend:** http://103.150.197.225:3013
    **Live backend:** http://103.150.197.225:8013
    **Captured:** &lt;ISO-8601 timestamp&gt;
    **Scope:** Phase 1 (AUDIT-04) — read-only repro. Fix lives in Phase 2 (AUTHFIX-01..04).

    ## Symptom (as reported)

    Runtime error in the browser console when interacting with the role-select dropdown on the Login page:

    > ResizeObserver loop completed with undelivered notifications.

    A benign suppressor was applied at frontend/public/index.html (lines &lt;START&gt;-&lt;END&gt;) as a temporary mitigation. The suppressor masks the console error but does not address the underlying interaction. Registration flow has not been validated end-to-end.

    ## Reproduction

    ### Path A — Existing user login via UI

    Preconditions:
    - Live frontend reachable at http://103.150.197.225:3013/login.
    - Test credentials from memory/test_credentials.md (gitignored — credentials NOT inlined here).

    Steps:
    1. Open http://103.150.197.225:3013/login in a fresh incognito window with DevTools open (Console + Network tabs).
    2. Enter the test admin email in the email input.
    3. Enter the test admin password in the password input.
    4. Open the role select (Radix-based combobox; defined in frontend/src/pages/Login.js around lines &lt;cite from Login.js&gt;).
    5. Pick a role (e.g., admin).
    6. Click the Submit button.

    Expected:
    - Console: no errors.
    - Network: POST /api/auth/login → 200 with body { access_token | token, user: { id, email, role } }.
    - localStorage: token written under key &lt;cite from AuthContext.js&gt;.
    - Navigation: redirect to /dashboard (or first protected route).
    - GET /api/auth/me on next mount: 200 with the same user.

    Observed:
    &lt;Fill from .work/login-frontend-evidence.md "Expected vs observed" section. Each step's observed row is one of:
       - matches expected;
       - "ResizeObserver loop completed with undelivered notifications" fires on step 4 (role select interaction);
       - browser-driven verification deferred to operator — see Operator playbook below.&gt;

    ### Path B — Existing user login via API (UI bypass)

    Steps:
    1. POST http://103.150.197.225:8013/api/auth/login with valid creds.
    2. POST same with invalid password.
    3. POST same with malformed body.

    Expected per CONS-auth-header:
    - Step 1 → 200 with { access_token | token, user }
    - Step 2 → 401
    - Step 3 → 400

    Observed (from .work/login-backend.txt, redacted):
    &lt;Paste the status codes and the relevant headers from each section. Do NOT paste any JWT or password — those were redacted at capture time. Show response BODY shape only (e.g., { token: "&lt;REDACTED-JWT&gt;", user: { id, email, role } }).&gt;

    Divergence:
    &lt;If observed matches expected per step, write "no divergence at API layer". If any step diverges from CONS-auth-header (wrong code, wrong shape), call it out explicitly with the actual vs expected codes.&gt;

    ### Path C — New user registration via API

    Steps:
    1. POST http://103.150.197.225:8013/api/auth/register with role=admin and a synthetic email under .invalid TLD.
    2. Same with role=operator.
    3. Same with role=viewer.

    Expected:
    - Each → 200 or 201 with the created user id, OR a documented error per CONS-auth-header.
    - Same email re-submission → 4xx (not 500).
    - Side-effect: a row in `users` collection per successful submission.

    Observed (from .work/register-backend.txt, redacted):
    &lt;Paste the status codes and response body shape per role. Do NOT paste JWTs.&gt;

    Side-effect note:
    Three synthetic users with email prefix `audit-probe-` and TLD `.invalid` were inserted into the live `users` collection during this repro. They are documented in `.work/register-backend.txt` under `--- CLEANUP-NOTE ---`. Phase 2 may remove them as part of regression-test setup; Phase 5 may also clean them up.

    ## Suspected component

    **Primary suspect:** &lt;component name&gt; — Login.js line &lt;NN&gt;.

    Read pltu-tenayan-full-backup/frontend/src/pages/Login.js and identify the role-select component (likely a Radix Select / Combobox imported from @radix-ui/react-select or wrapped in components/ui/select.js). Cite:
      - The import line (e.g., `import { Select, SelectTrigger, ... } from "@/components/ui/select";` at Login.js:&lt;N&gt;).
      - The JSX block that renders it (Login.js:&lt;N&gt;-&lt;M&gt;).
      - Any associated state setter and where in the submit handler the selected role is read.

    Mechanism hypothesis (one paragraph, plain prose):
    Radix Select internally observes its trigger size with ResizeObserver to position the popover. When the popover repositions in the same frame as a layout change (e.g., the page's vh-100 flex layout reflowing on focus), the observer fires recursively and the browser logs the loop warning. The warning is benign for rendering but breaks form submission only if a downstream error boundary catches it; otherwise it is purely cosmetic and the real failure (if any) is elsewhere. Phase 2 must distinguish (a) benign console noise that happens to coincide with a form-submit failure caused by a separate root cause, vs (b) a true error-boundary unwind that aborts submit. The Path B + Path C API repros isolate (a): if backend returns 200 for valid creds, the frontend failure is a UI-only error-boundary issue, not an auth issue.

    **Secondary suspects (rule out or confirm in Phase 2):**
    - AuthContext.js login() error handling — does it await the network response before navigating? File:line.
    - ProtectedRoute redirect race — does it redirect before the token is persisted to localStorage? File:line.
    - The frontend/public/index.html ResizeObserver suppressor — does it ALSO suppress unrelated errors that should bubble? File:line range.

    ## Mitigation status

    - frontend/public/index.html lines &lt;START&gt;-&lt;END&gt; contain a benign suppressor that swallows the ResizeObserver console error. This was applied as a temporary fix and is NOT a Phase-2 substitute. Phase 2 should evaluate whether the suppressor stays after the underlying interaction is fixed.

    ## Operator playbook (5-minute manual re-verification)

    Reproduce the steps under "Path A" with a real browser. Expected console state, expected network state, and expected localStorage state are listed above. If any row was marked "browser-driven verification deferred to operator", the operator fills it in and reports back. Pure check; no fixes.

    ## Methodology

    Backend repros captured via curl into pltu-tenayan-full-backup/docs/audit/.work/{login-backend.txt,register-backend.txt} with JWTs and passwords redacted at capture time. Frontend repro structure captured into .work/login-frontend-evidence.md. No source files modified.
    ```

    Final redaction sweep — run before declaring done:
    grep -nE '(JWT_PREFIX[A-Za-z0-9._-]{20,}|password\s*[:=]\s*["'\''][^"'\'' ]+["'\''])' pltu-tenayan-full-backup/docs/audit/LOGIN_BUG.md
    Expected: no matches. If anything matches, redact and re-verify.

    Do NOT modify Login.js, AuthContext.js, or public/index.html. Do NOT propose code changes — Phase 2 owns the fix. The "Suspected component" section is a hypothesis with file:line evidence, NOT a patch.
  </action>
  <verify>
    <automated>test -s pltu-tenayan-full-backup/docs/audit/LOGIN_BUG.md &amp;&amp; grep -q '## Reproduction' pltu-tenayan-full-backup/docs/audit/LOGIN_BUG.md &amp;&amp; grep -q '### Path A' pltu-tenayan-full-backup/docs/audit/LOGIN_BUG.md &amp;&amp; grep -q '### Path B' pltu-tenayan-full-backup/docs/audit/LOGIN_BUG.md &amp;&amp; grep -q '### Path C' pltu-tenayan-full-backup/docs/audit/LOGIN_BUG.md &amp;&amp; grep -q '## Suspected component' pltu-tenayan-full-backup/docs/audit/LOGIN_BUG.md &amp;&amp; grep -q 'Login.js' pltu-tenayan-full-backup/docs/audit/LOGIN_BUG.md &amp;&amp; grep -q 'ResizeObserver' pltu-tenayan-full-backup/docs/audit/LOGIN_BUG.md &amp;&amp; grep -q '/api/auth/login' pltu-tenayan-full-backup/docs/audit/LOGIN_BUG.md &amp;&amp; grep -q '/api/auth/register' pltu-tenayan-full-backup/docs/audit/LOGIN_BUG.md &amp;&amp; ! grep -qE 'JWT_PREFIX[A-Za-z0-9._-]{20,}' pltu-tenayan-full-backup/docs/audit/LOGIN_BUG.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at pltu-tenayan-full-backup/docs/audit/LOGIN_BUG.md.
    - All three reproduction paths exist as headings: ### Path A (UI login), ### Path B (API login), ### Path C (API register).
    - Each path has both an "Expected" subsection and an "Observed" subsection.
    - The Suspected component section names a specific component with a Login.js file:line reference.
    - The ResizeObserver suppressor in frontend/public/index.html is documented with a line-range citation.
    - The /api/auth/login and /api/auth/register endpoints both appear in the document.
    - No JWT (no string matching `JWT_PREFIX[A-Za-z0-9._-]{20,}`) appears in the document.
    - No cleartext password value from memory/test_credentials.md appears in the document.
    - No source file under frontend/ was modified by this task.
    - The document does NOT propose code changes (no `diff`, no `--- a/`, no "change line N to ..." instructions). Phase 2 owns the fix.
  </acceptance_criteria>
  <done>LOGIN_BUG.md is a complete, reproducible record of the login + register failure surface with backend + frontend evidence and a named suspect — Phase 2 can read it as the only input it needs to start AUTHFIX-01.</done>
</task>

</tasks>

<verification>
- LOGIN_BUG.md was generated from live curl captures (Path B, Path C) and a structured frontend repro worksheet (Path A).
- All three repro paths (UI login, API login, API register) are documented with Expected vs Observed.
- A specific suspect component is named with a file:line citation in Login.js.
- The ResizeObserver suppressor in public/index.html is documented with line range.
- No credentials or JWTs leak into any committed artifact.
- The synthetic registration users created during the repro are documented for cleanup in Phase 2 / Phase 5.
</verification>

<success_criteria>
A reviewer can open pltu-tenayan-full-backup/docs/audit/LOGIN_BUG.md and answer Phase 1 success criterion #4:
1. What are the steps to reproduce the login failure?
2. What is observed vs what is expected?
3. Which component is suspected and what is the file:line evidence?
4. What is the current mitigation status (the ResizeObserver suppressor)?
5. Does the registration flow also fail, and how?
</success_criteria>

<output>
After completion, create `.planning/phases/01-production-audit-onboarding/01-04-SUMMARY.md` summarizing:
- Whether Path B (API login) returned 200 / 401 / 400 as expected per CONS-auth-header (one line per probe).
- Whether Path C (API register) returned a documented success or a documented failure per role (admin/operator/viewer).
- The specific suspect component named for Phase 2 (file:line).
- Whether any audit-probe-* synthetic users were inserted into the live `users` collection (count, email pattern).
- Open questions Phase 2 inherits (e.g., "browser-driven verification deferred to operator for Path A step 4").
</output>
