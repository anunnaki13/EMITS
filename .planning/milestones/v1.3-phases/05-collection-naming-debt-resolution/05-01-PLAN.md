---
phase: 05-collection-naming-debt-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/decisions/ADR-009-canonical-smartstock.md
  - .planning/decisions/ADR-010-canonical-sumberpemakaian.md
  - .planning/decisions/ADR-011-canonical-app-settings.md
  - .planning/decisions/ADR-012-canonical-ai-chat-history.md
autonomous: true
requirements: [DEBT-01]
tags: [adr, collection-naming, phase-5, debt-01]

must_haves:
  truths:
    - "A reviewer can open .planning/decisions/ and see 4 new ADR files (ADR-009..012) — one per duplicate-collection pair."
    - "Each ADR follows the MADR-format structure used by ADR-001..008 (Status / Context / Decision / Consequences / Alternatives / References)."
    - "Each ADR's Status line reads exactly `Accepted (locked, 2026-05-11)` (or the current date) — matches ADR-001..008 pattern."
    - "Each ADR cites the Phase-3 audit table row (DATABASE_SCHEMA.md lines 695-735) AND the server.py:line evidence for the canonical choice."
    - "ADR-012 explicitly notes that `ai_conversations` is absent from server.py (zero legacy reads) and absent from the live pltu_tenayan DB (drop step is a no-op)."
  artifacts:
    - path: ".planning/decisions/ADR-009-canonical-smartstock.md"
      provides: "Canonical name decision: smartstock (NOT smart_stock), per D-01"
      contains: "Accepted (locked"
      min_lines: 45
    - path: ".planning/decisions/ADR-010-canonical-sumberpemakaian.md"
      provides: "Canonical name decision: sumberpemakaian (NOT sumber_pemakaian), per D-02"
      contains: "Accepted (locked"
      min_lines: 45
    - path: ".planning/decisions/ADR-011-canonical-app-settings.md"
      provides: "Canonical name decision: app_settings (NOT settings), per D-03"
      contains: "Accepted (locked"
      min_lines: 45
    - path: ".planning/decisions/ADR-012-canonical-ai-chat-history.md"
      provides: "Canonical name decision: ai_chat_history (NOT ai_conversations), per D-04; ai_conversations absent from server.py"
      contains: "Accepted (locked"
      min_lines: 45
  key_links:
    - from: ".planning/decisions/ADR-009-canonical-smartstock.md"
      to: "pltu-tenayan-full-backup/DATABASE_SCHEMA.md §Duplicate Pair Active Read Targets (lines 695-735)"
      via: "References section citation"
      pattern: "DATABASE_SCHEMA.md.*Duplicate Pair"
    - from: ".planning/decisions/ADR-009..012-*.md"
      to: ".planning/REQUIREMENTS.md DEBT-01"
      via: "References section citation"
      pattern: "DEBT-01"
    - from: ".planning/decisions/ADR-011-canonical-app-settings.md"
      to: "pltu-tenayan-full-backup/backend/server.py:2427, 2926, 4346"
      via: "Code anchor citation (legacy `db.settings.find_one` reads to be fixed in Phase 5 Plan 05-02)"
      pattern: "server\\.py:(2427|2926|4346)"
---

<objective>
Create four locked MADR-format ADRs (ADR-009..012) under `.planning/decisions/` — one per duplicate-collection pair — recording the canonical-name decisions locked in CONTEXT.md D-01..D-04. These ADRs are the citation source for Plan 05-02 (server.py legacy-read edits) and Plan 05-03 (MIGRATION_RUNBOOK.md), so they must land first.

Purpose: Close DEBT-01 ("A canonical name is recorded in an ADR for each pair"). Give Phases 6, 7, and 8 authoritative anchors so they can refer to "ADR-009" instead of re-deriving the smartstock-vs-smart_stock decision every time.

Output: Four new ADR files in `.planning/decisions/` (outer repo). Outer-repo commit only — no inner-repo changes in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

# Phase artifacts (decisions locked here)
@.planning/phases/05-collection-naming-debt-resolution/05-CONTEXT.md
@.planning/phases/05-collection-naming-debt-resolution/05-RESEARCH.md
@.planning/phases/05-collection-naming-debt-resolution/05-VALIDATION.md

# Style precedents (MADR format used by ADR-001..008)
@.planning/decisions/ADR-001-mongodb-datastore.md
@.planning/decisions/ADR-008-pagination-shape.md

# Source-of-truth audit (cited in each ADR's References)
@pltu-tenayan-full-backup/DATABASE_SCHEMA.md

<interfaces>
<!-- MADR structure (verbatim from ADR-001..008). Use this exact section order. -->
<!-- Do NOT invent new headers. Each section is required. -->

```
# ADR-NNN: [Title]

## Status

Accepted (locked, YYYY-MM-DD) — [one-line rationale, e.g., "promoted from
Phase-3 duplicate-pair audit; canonical name selected per D-NN."]

## Context

[3-6 paragraphs. Cover:
 - What the duplicate pair is and why it exists today.
 - Live row counts: canonical = X records, legacy = 0 records.
 - Active code paths reading each name (cite specific server.py:line ranges).
 - Why Phase 5 needs a locked decision now (DEBT-01..DEBT-05 traceability).]

## Decision

[The chosen canonical name (verbatim), the rejected legacy name, and what
 it means operationally:
 - Backend code reads ONLY <canonical> after Phase 5 plan 05-02 lands.
 - <legacy> is dropped in Phase 5 plan 05-04 after ≥48h observation.
 - DATABASE_SCHEMA.md "legacy" markings for this pair are removed in 05-04.]

## Consequences

**Positive:**
 - [List 3-5 bullets — e.g., AI module now reads real data, code grep
   shows zero legacy references, single source of truth, etc.]

**Negative / accepted tradeoffs:**
 - [List 2-3 bullets — e.g., 48h observation window required, drop step
   irreversible without backup restore, etc.]

## Alternatives Considered

[The rejected alternative (the legacy name) and 1-2 sentences why rejected.
 Cite D-NN as the source of the rejection rationale.
 Also note D-08 (no bidirectional copy) where relevant — the legacy
 collection is empty, so copy is unnecessary.]

## References

- **Source decision:** `.planning/phases/05-collection-naming-debt-resolution/05-CONTEXT.md` §"Implementation Decisions" D-NN.
- **Phase-3 audit:** `pltu-tenayan-full-backup/DATABASE_SCHEMA.md` §"Duplicate Pair Active Read Targets (Phase-3 audit, 2026-05-10)" lines 695-735 — row-count + code-anchor evidence per pair.
- **Code anchors (proof in effect):**
  - `pltu-tenayan-full-backup/backend/server.py:<LINE>` — canonical CRUD read (canonical name in active use TODAY).
  - `pltu-tenayan-full-backup/backend/server.py:<LINE>` — legacy read (to be fixed in Phase 5 Plan 05-02).
- **Requirement:** `.planning/REQUIREMENTS.md` DEBT-01.
- **Sibling ADRs:** `.planning/decisions/ADR-009..012` — the four canonical-name ADRs land together as a set.
```

<!-- Exact code anchors per ADR (from RESEARCH.md Focus 2, VERIFIED 2026-05-11): -->

ADR-009 smartstock canonical (D-01):
 - Canonical CRUD reads (DO NOT change): server.py:3078, 3083, 3130, 3257, 3261, 3283, 3313, 3325, 3637 (all CRUD endpoints).
 - Legacy AI reads (Phase 5 Plan 05-02 fixes): server.py:2379 (`db.smart_stock.find`), 2395 (`db.smart_stock.aggregate`), 2863 (`db.smart_stock.aggregate` in `/ai/quick/smart-stock` endpoint).
 - Live row counts: smartstock=207, smart_stock=0 (absent from DB entirely).

ADR-010 sumberpemakaian canonical (D-02):
 - Canonical CRUD reads (DO NOT change): server.py:3352, 3357, 3363, 3429, 3535, 3538, 3557, 3576.
 - Legacy AI reads (Phase 5 Plan 05-02 fixes): server.py:2387 (`db.sumber_pemakaian.find`), 2398 (`db.sumber_pemakaian.aggregate`), 2868 (`db.sumber_pemakaian.aggregate`), 2877 (`db.sumber_pemakaian.aggregate` avg_usage).
 - Live row counts: sumberpemakaian=208, sumber_pemakaian=0 (absent from DB entirely).

ADR-011 app_settings canonical (D-03):
 - Canonical reads (DO NOT change): server.py:3817 (`db.app_settings.find_one`), 3825 (`db.app_settings.update_one`), 3901 (`db.app_settings.find_one` COA KPI).
 - Legacy reads (Phase 5 Plan 05-02 fixes): server.py:2427 (`db.settings.find_one({"type": "coa"})` AI context), 2926 (`db.settings.find_one({"type": "coa"})` `/ai/quick/coa-alerts`), 4346 (`db.settings.find_one({"type": "coa"})` COA export PDF).
 - Live row counts: app_settings=1, settings=0 (absent from DB entirely).
 - Behavioural note: legacy reads currently fall back to `price_per_kcal_per_ton=50` default; post-migration uses the real configured price.

ADR-012 ai_chat_history canonical (D-04):
 - Canonical assignment (already in place): server.py:2266 `ai_chat_collection = db.ai_chat_history`.
 - Legacy reads: ZERO. `grep -n "ai_conversations" server.py` returns no matches (VERIFIED 2026-05-11). The legacy `ai_conversations` collection is also absent from the live `pltu_tenayan` DB. ADR-012 documents the canonical choice; Plan 05-02 has no code edit for this pair; Plan 05-04 drop step is a no-op (handled by the migration script's "SKIP — does not exist" branch).
 - Live row counts: ai_chat_history=10, ai_conversations=0 (absent from DB entirely).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 05-01-01: Author ADR-009 (smartstock canonical) and ADR-010 (sumberpemakaian canonical)</name>
  <files>
    .planning/decisions/ADR-009-canonical-smartstock.md
    .planning/decisions/ADR-010-canonical-sumberpemakaian.md
  </files>
  <read_first>
    - .planning/decisions/ADR-001-mongodb-datastore.md (MADR style precedent)
    - .planning/decisions/ADR-008-pagination-shape.md (most recent ADR; same style)
    - .planning/phases/05-collection-naming-debt-resolution/05-CONTEXT.md §"Implementation Decisions" D-01, D-02
    - .planning/phases/05-collection-naming-debt-resolution/05-RESEARCH.md §"Research Focus 2: Comprehensive server.py Legacy-Read Grep" (lines 183-260) — exact line numbers
    - pltu-tenayan-full-backup/DATABASE_SCHEMA.md lines 695-735 — Phase-3 audit table (the row-count + code-anchor evidence cited in each ADR)
  </read_first>
  <action>
    Create two new MADR-format ADR files. Each follows the EXACT structure shown in the `<interfaces>` block above (sections: Status, Context, Decision, Consequences, Alternatives Considered, References).

    **For ADR-009-canonical-smartstock.md:**
    - Title line: `# ADR-009: smartstock as Canonical Smart-Stock Collection Name`
    - Status: `Accepted (locked, 2026-05-11) — promoted from Phase-3 duplicate-pair audit; canonical name selected per D-01 in 05-CONTEXT.md.`
    - Context: Explain the duplicate pair (`smartstock` 207 records vs `smart_stock` 0 records / absent from live DB). State the silent data-gap: AI module reads from empty `smart_stock` at server.py:2379/2395/2863 while CRUD writes to `smartstock` at server.py:3078+. Cite the Phase-3 audit table (DATABASE_SCHEMA.md:704). Note DEBT-01..DEBT-05 traceability to .planning/REQUIREMENTS.md.
    - Decision: "**`smartstock`** is the canonical collection name. `smart_stock` is rejected as the legacy duplicate. After Phase 5 Plan 05-02 lands, all backend reads in `server.py` reference only `db.smartstock`. The empty `smart_stock` collection is dropped in Plan 05-04 after a ≥48h observation window per D-06."
    - Consequences (Positive): AI quick-smart-stock endpoint now returns real 207-record aggregates instead of empty zeros; single source of truth; documentation cleanup; `grep -c "db\.smart_stock\b" server.py` becomes 0.
    - Consequences (Negative): 3 server.py lines must be edited atomically (2379, 2395, 2863); 48h observation window required before drop; AI endpoint behaviour changes (was: returning 0; now: returning real data) — operator must observe in 48h window.
    - Alternatives Considered: Reject `smart_stock` (snake_case-idiomatic but only 0 records; would require renaming 207 production records vs leaving them in place); reject bidirectional dual-read window (D-08 — over-engineered for empty legacy); reject snake_case refactor of all collection names (Deferred to Phase 8 polish).
    - References: 05-CONTEXT.md §D-01; DATABASE_SCHEMA.md §"Duplicate Pair Active Read Targets" lines 695-735 (especially row 704); server.py:3078 (canonical CRUD); server.py:2379, 2395, 2863 (legacy AI reads — fixed in Plan 05-02); REQUIREMENTS.md DEBT-01; sibling ADRs ADR-010..012.

    **For ADR-010-canonical-sumberpemakaian.md:**
    - Title line: `# ADR-010: sumberpemakaian as Canonical Pemakaian Source Collection Name`
    - Status: `Accepted (locked, 2026-05-11) — promoted from Phase-3 duplicate-pair audit; canonical name selected per D-02 in 05-CONTEXT.md.`
    - Context: Duplicate pair (`sumberpemakaian` 208 records vs `sumber_pemakaian` 0 records / absent from live DB). Silent data-gap: AI module reads empty `sumber_pemakaian` at server.py:2387/2398/2868/2877 while CRUD writes to `sumberpemakaian` at server.py:3352+. Cite Phase-3 audit (DATABASE_SCHEMA.md:705).
    - Decision: "**`sumberpemakaian`** is the canonical collection name. `sumber_pemakaian` is rejected as the legacy duplicate. After Phase 5 Plan 05-02 lands, all backend reads in `server.py` reference only `db.sumberpemakaian`. The empty `sumber_pemakaian` collection is dropped in Plan 05-04 after a ≥48h observation window per D-06."
    - Consequences (Positive): AI quick-smart-stock and AI-context-builder endpoints now read real 208-record pemakaian data; smart-stock AI recommendations gain real usage-rate context. Negative: 4 server.py lines must be edited atomically (2387, 2398, 2868, 2877); observation window required.
    - Alternatives Considered: Reject `sumber_pemakaian` (snake_case-idiomatic but legacy is 0 records); bidirectional copy rejected (D-08); snake_case-rename deferred to Phase 8.
    - References: 05-CONTEXT.md §D-02; DATABASE_SCHEMA.md row 705; server.py:3352 (canonical CRUD); server.py:2387, 2398, 2868, 2877 (legacy AI reads — fixed in Plan 05-02); REQUIREMENTS.md DEBT-01; sibling ADRs.

    Both ADRs must:
    - Use the date `2026-05-11` in the Status line (today per planning context).
    - Match the formatting / heading hierarchy of ADR-008-pagination-shape.md exactly.
    - Cite specific line numbers (not "around line 2400") — use the verified line numbers from RESEARCH.md Focus 2.
    - End with a "Sibling ADRs" cross-reference to the other three Phase-5 ADRs.

    Use the Write tool. No heredoc / cat.
  </action>
  <acceptance_criteria>
    - File `.planning/decisions/ADR-009-canonical-smartstock.md` exists and `grep -c "Accepted (locked, 2026-05-11)" .planning/decisions/ADR-009-canonical-smartstock.md` returns 1
    - File `.planning/decisions/ADR-010-canonical-sumberpemakaian.md` exists and `grep -c "Accepted (locked, 2026-05-11)" .planning/decisions/ADR-010-canonical-sumberpemakaian.md` returns 1
    - Each file contains all required sections (verifiable by `grep -c '^## '` returns >= 6 for the 6 standard headers: Status, Context, Decision, Consequences, Alternatives Considered, References)
    - Each file cites the Phase-3 audit table location (`grep -c "DATABASE_SCHEMA.md" .planning/decisions/ADR-009-canonical-smartstock.md` >= 1)
    - Each file cites at least 3 specific server.py line numbers (ADR-009: 3078, 2379, 2395, 2863; ADR-010: 3352, 2387, 2398, 2868, 2877)
    - Each file references DEBT-01 (`grep -c "DEBT-01" .planning/decisions/ADR-009-canonical-smartstock.md` >= 1)
  </acceptance_criteria>
  <verify>
    <automated>for n in 009 010; do FILE=$(ls .planning/decisions/ADR-$n-*.md 2>/dev/null | head -1); test -f "$FILE" && grep -q "Accepted (locked, 2026-05-11)" "$FILE" && grep -q "DATABASE_SCHEMA.md" "$FILE" && grep -q "DEBT-01" "$FILE" && [ "$(grep -c '^## ' "$FILE")" -ge 6 ] || { echo "FAIL: ADR-$n missing required content"; exit 1; }; done; echo "PASS"</automated>
  </verify>
  <done>
    Both ADR files exist with MADR-format structure, Status line `Accepted (locked, 2026-05-11)`, citations to Phase-3 audit table and specific server.py line numbers, and a References section linking to REQUIREMENTS.md DEBT-01 and sibling ADRs.
  </done>
</task>

<task type="auto">
  <name>Task 05-01-02: Author ADR-011 (app_settings canonical) and ADR-012 (ai_chat_history canonical)</name>
  <files>
    .planning/decisions/ADR-011-canonical-app-settings.md
    .planning/decisions/ADR-012-canonical-ai-chat-history.md
  </files>
  <read_first>
    - .planning/decisions/ADR-008-pagination-shape.md (style precedent)
    - .planning/phases/05-collection-naming-debt-resolution/05-CONTEXT.md §"Implementation Decisions" D-03, D-04
    - .planning/phases/05-collection-naming-debt-resolution/05-RESEARCH.md §"Research Focus 2" (lines 208-220 for `db.settings`; lines 216-220 for `ai_conversations`-grep-empty finding)
    - pltu-tenayan-full-backup/DATABASE_SCHEMA.md lines 695-735 — Phase-3 audit table (rows 706, 707)
    - The two ADRs written in Task 05-01-01 (use the same heading hierarchy)
  </read_first>
  <action>
    Create two more MADR-format ADR files. Same structure as Task 05-01-01.

    **For ADR-011-canonical-app-settings.md:**
    - Title line: `# ADR-011: app_settings as Canonical Application-Settings Collection Name`
    - Status: `Accepted (locked, 2026-05-11) — promoted from Phase-3 duplicate-pair audit; canonical name selected per D-03 in 05-CONTEXT.md.`
    - Context: Duplicate pair (`app_settings` 1 record vs `settings` 0 records / absent from live DB). Silent operational impact: COA export PDF (server.py:4346) and AI COA-alerts (server.py:2926) and AI context builder (server.py:2427) read empty legacy `settings`, falling back to hardcoded `price_per_kcal_per_ton=50`. The canonical `/settings/coa` endpoint and COA KPI endpoint already read `app_settings` (server.py:3817, 3825, 3901). Cite Phase-3 audit table row 706 (DATABASE_SCHEMA.md:706).
    - Decision: "**`app_settings`** is the canonical collection name. `settings` is rejected as the legacy duplicate. After Phase 5 Plan 05-02 lands, all 3 legacy `db.settings.find_one({"type": "coa"})` reads in `server.py` (lines 2427, 2926, 4346) reference `db.app_settings` instead. The empty `settings` collection is dropped in Plan 05-04 after a ≥48h observation window per D-06."
    - Consequences (Positive): COA export PDF and AI COA-alerts now use the real configured `price_per_kcal_per_ton` instead of the hardcoded default 50 — material behavioural fix. Single source of truth for COA pricing settings. `grep -c "db\.settings\.find_one" server.py` becomes 0.
    - Consequences (Negative): potential_loss values returned by `/api/ai/quick/coa-alerts` will change post-migration (was: based on default 50; now: based on real configured price) — operator MUST verify this in the 48h observation window per RESEARCH.md Focus 6. 3 server.py lines must be edited atomically (2427, 2926, 4346). Local variable name `settings` in the affected lines stays unchanged (minimal diff — only the `db.settings` → `db.app_settings` token changes).
    - Alternatives Considered: Reject `settings` (Python-naming-clean but legacy is 0 records; also `settings` is a Python module-namespace-collision-prone name); bidirectional copy rejected (D-08).
    - References: 05-CONTEXT.md §D-03; DATABASE_SCHEMA.md row 706; server.py:3817, 3825, 3901 (canonical reads); server.py:2427, 2926, 4346 (legacy reads — fixed in Plan 05-02); RESEARCH.md Focus 6 "Endpoint 2: /ai/quick/coa-alerts" behavioural change note; REQUIREMENTS.md DEBT-01; sibling ADRs.

    **For ADR-012-canonical-ai-chat-history.md:**
    - Title line: `# ADR-012: ai_chat_history as Canonical AI-Chat-History Collection Name`
    - Status: `Accepted (locked, 2026-05-11) — promoted from Phase-3 duplicate-pair audit; canonical name selected per D-04 in 05-CONTEXT.md.`
    - Context: Duplicate pair (`ai_chat_history` 10 records via module-level assignment at server.py:2266 vs `ai_conversations` 0 records). **Important distinguishing finding (RESEARCH.md Focus 2 line 218):** `grep -n "ai_conversations" server.py` returns ZERO results. The legacy collection is also absent from the live `pltu_tenayan` DB (verified by `db.getCollectionNames()`). This pair requires NO code edits — only ADR documentation and a no-op drop step in Plan 05-04 (the migration script's "SKIP — does not exist" branch handles the missing collection transparently). Cite Phase-3 audit row 707.
    - Decision: "**`ai_chat_history`** is the canonical collection name. `ai_conversations` is rejected as the legacy duplicate. The canonical assignment `ai_chat_collection = db.ai_chat_history` at server.py:2266 is already in place — no code edits required for this pair. Plan 05-04's `migrate_collection_names.py --apply` step will report `[SKIP] ai_conversations — does not exist` (per the script's idempotency pattern at RESEARCH.md Focus 5)."
    - Consequences (Positive): Closes DEBT-01 for this pair without any code risk (zero legacy reads to break). DATABASE_SCHEMA.md cleanup in Plan 05-04 removes the §13.2 ai_conversations stub section. Negative: Migration script reports a no-op for this collection; the operator must understand (per MIGRATION_RUNBOOK.md §2) that "SKIP — does not exist" is the EXPECTED outcome.
    - Alternatives Considered: Reject `ai_conversations` (also Python-naming-clean but legacy is 0 records and not present in DB; refactor risk for zero benefit); bidirectional copy rejected (D-08); snake_case-rename deferred to Phase 8.
    - References: 05-CONTEXT.md §D-04; DATABASE_SCHEMA.md row 707; server.py:2266 (canonical assignment); RESEARCH.md Focus 2 §"`ai_conversations` (ZERO occurrences)" lines 216-220; RESEARCH.md Pitfall E "ai_conversations does not exist in live DB"; REQUIREMENTS.md DEBT-01; sibling ADRs.

    Use the Write tool. No heredoc / cat.
  </action>
  <acceptance_criteria>
    - File `.planning/decisions/ADR-011-canonical-app-settings.md` exists and `grep -c "Accepted (locked, 2026-05-11)" .planning/decisions/ADR-011-canonical-app-settings.md` returns 1
    - File `.planning/decisions/ADR-012-canonical-ai-chat-history.md` exists and `grep -c "Accepted (locked, 2026-05-11)" .planning/decisions/ADR-012-canonical-ai-chat-history.md` returns 1
    - ADR-011 cites all 3 legacy line numbers: `for L in 2427 2926 4346; do grep -q ":$L\b\|server\.py:$L\b" .planning/decisions/ADR-011-canonical-app-settings.md || { echo "FAIL: missing line $L"; exit 1; }; done`
    - ADR-012 explicitly states ai_conversations is absent from server.py (`grep -c "ZERO\|does not exist\|absent" .planning/decisions/ADR-012-canonical-ai-chat-history.md` >= 2)
    - Each file contains all 6 standard MADR section headers
    - Each file references DEBT-01
  </acceptance_criteria>
  <verify>
    <automated>for n in 011 012; do FILE=$(ls .planning/decisions/ADR-$n-*.md 2>/dev/null | head -1); test -f "$FILE" && grep -q "Accepted (locked, 2026-05-11)" "$FILE" && grep -q "DEBT-01" "$FILE" && [ "$(grep -c '^## ' "$FILE")" -ge 6 ] || { echo "FAIL: ADR-$n missing required content"; exit 1; }; done && grep -qE "ZERO|does not exist|absent" .planning/decisions/ADR-012-canonical-ai-chat-history.md && for L in 2427 2926 4346; do grep -q "$L" .planning/decisions/ADR-011-canonical-app-settings.md || { echo "FAIL: ADR-011 missing line $L"; exit 1; }; done; echo "PASS"</automated>
  </verify>
  <done>
    Both ADR files exist, follow MADR format, cite specific server.py line numbers (ADR-011: 2427/2926/4346; ADR-012: 2266), explicitly document the "ai_conversations is absent from server.py and live DB" finding in ADR-012, and reference DEBT-01.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning artifact → downstream plans | ADRs are read by Plan 05-02, 05-03, 05-04 and by future Phases 6/7/8 as authoritative anchors. Drift between ADR text and actual code becomes a documentation-trust failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-doc-drift-01 | Information Disclosure / Repudiation | ADR-009..012 References sections | mitigate | Every ADR cites specific verified `server.py:LINE` numbers from RESEARCH.md Focus 2 (verified live 2026-05-11). Acceptance criteria includes a grep gate that every line number is present in the ADR. This makes drift mechanically detectable. |
| T-canonical-choice-misread-01 | Tampering | ADR Decision section | mitigate | Each ADR's Decision section names the canonical name in bold and the rejected name in plain text. The CONTEXT.md D-NN reference in the Status line traces the decision back to user-locked input — no Claude-discretion ambiguity. |
| T-adr-numbering-collision-01 | Tampering | ADR file naming | accept | ADR-001..008 already exist; ADR-009..012 are the next free numbers (verified by `ls .planning/decisions/`). No collision risk. |
</threat_model>

<verification>
After both tasks complete, verify the full set:

```bash
# 1. All 4 ADRs exist
ls .planning/decisions/ADR-{009,010,011,012}-*.md
# Expected: 4 file paths printed, exit 0

# 2. Each has a locked Status line with today's date
for n in 009 010 011 012; do
  FILE=$(ls .planning/decisions/ADR-$n-*.md | head -1)
  grep -q "Accepted (locked, 2026-05-11)" "$FILE" || { echo "FAIL: ADR-$n missing locked status"; exit 1; }
done

# 3. Each cites the Phase-3 audit table
for n in 009 010 011 012; do
  FILE=$(ls .planning/decisions/ADR-$n-*.md | head -1)
  grep -q "DATABASE_SCHEMA.md" "$FILE" || { echo "FAIL: ADR-$n missing audit citation"; exit 1; }
done

# 4. Each references DEBT-01
for n in 009 010 011 012; do
  FILE=$(ls .planning/decisions/ADR-$n-*.md | head -1)
  grep -q "DEBT-01" "$FILE" || { echo "FAIL: ADR-$n missing DEBT-01 reference"; exit 1; }
done

# 5. MADR structure: 6 required section headers
for n in 009 010 011 012; do
  FILE=$(ls .planning/decisions/ADR-$n-*.md | head -1)
  COUNT=$(grep -c '^## ' "$FILE")
  [ "$COUNT" -ge 6 ] || { echo "FAIL: ADR-$n has only $COUNT section headers (need >=6)"; exit 1; }
done

echo "ALL 4 ADRs valid"
```
</verification>

<success_criteria>
- 4 new files exist under `.planning/decisions/` (ADR-009 through ADR-012)
- Each file passes the 5 verification gates above
- Plan 05-02 can cite these ADRs by path in its commit messages
- DEBT-01 is closed (verifiable by the validation map row 05-01-01)
</success_criteria>

<output>
After both tasks complete, create `.planning/phases/05-collection-naming-debt-resolution/05-01-SUMMARY.md` documenting:
- Files created (4 ADR paths)
- Verification command output (the full block above, with PASS lines)
- Acknowledge any clarifications made vs the CONTEXT.md line citations (RESEARCH.md Focus 2 corrections — 10 lines not 5+, line 2377 is a string literal not a collection read)
- Next plan: 05-02 depends on these ADRs being committed (cites ADR paths in server.py edit commit messages)
</output>
