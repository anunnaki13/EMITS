---
phase: 03-documentation-refresh-decision-lock-in
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/decisions/ADR-001-mongodb-datastore.md
  - .planning/decisions/ADR-002-fastapi-python-backend.md
  - .planning/decisions/ADR-003-react-frontend-stack.md
  - .planning/decisions/ADR-004-jwt-bcrypt-three-role-auth.md
  - .planning/decisions/ADR-005-gemini-via-legacy-ai-sdk.md
  - .planning/decisions/ADR-006-api-prefix-and-frontend-base-url.md
  - .planning/decisions/ADR-007-persistence-projection-uuid-iso.md
  - .planning/decisions/ADR-008-pagination-shape.md
autonomous: true
requirements:
  - DOCS-04
  - STAB-04
must_haves:
  truths:
    - "Eight ADR files exist under .planning/decisions/ with names ADR-001 through ADR-008 and locked status"
    - "Each ADR uses the MADR layout (Status / Context / Decision / Consequences / Alternatives / References) per D-01"
    - "Each ADR's Status line reads 'Accepted (locked, 2026-05-10) — promoted from IMPLICIT-NNN' per D-02"
    - "Each ADR's References section cites (a) the IMPLICIT-NNN line in PROJECT.md, (b) at least one code anchor file:line proving the decision is in effect, (c) any related CONS-* constraint per D-03"
    - "Future planning rounds can cite ADR-001..008 as authoritative anchors instead of IMPLICIT-NNN PROJECT.md lines"
  artifacts:
    - path: ".planning/decisions/ADR-001-mongodb-datastore.md"
      provides: "Locks MongoDB-via-Motor as the primary datastore (← IMPLICIT-001)"
      min_lines: 40
      contains: "promoted from IMPLICIT-001"
    - path: ".planning/decisions/ADR-002-fastapi-python-backend.md"
      provides: "Locks FastAPI / Python 3.11+ / bcrypt / pyjwt / pandas / openpyxl / reportlab backend stack (← IMPLICIT-002)"
      min_lines: 40
      contains: "promoted from IMPLICIT-002"
    - path: ".planning/decisions/ADR-003-react-frontend-stack.md"
      provides: "Locks React 19 + React Router 7 + Tailwind + Shadcn/UI + Axios + Recharts + jsPDF + xlsx, built via Yarn (← IMPLICIT-003)"
      min_lines: 40
      contains: "promoted from IMPLICIT-003"
    - path: ".planning/decisions/ADR-004-jwt-bcrypt-three-role-auth.md"
      provides: "Locks JWT bearer + bcrypt hashing + 3-role model (admin/operator/viewer) + 400/401/403/404/500 error map (← IMPLICIT-004 + CONS-auth-header). Cross-links AUTH_CONTRACT.md."
      min_lines: 40
      contains: "promoted from IMPLICIT-004"
    - path: ".planning/decisions/ADR-005-gemini-via-legacy-ai-sdk.md"
      provides: "Locks Google Gemini (gemini-2.5-flash) via legacy-ai-sdk as the AI provider for v1 (← IMPLICIT-005). Multi-provider abstraction is v2."
      min_lines: 40
      contains: "promoted from IMPLICIT-005"
    - path: ".planning/decisions/ADR-006-api-prefix-and-frontend-base-url.md"
      provides: "Locks /api/* route prefix on backend + REACT_APP_BACKEND_URL frontend base resolution (← IMPLICIT-006 + CONS-api-base)"
      min_lines: 40
      contains: "promoted from IMPLICIT-006"
    - path: ".planning/decisions/ADR-007-persistence-projection-uuid-iso.md"
      provides: "Locks Mongo projection {_id: 0}, application-level UUID id, ISO 8601 datetimes (← IMPLICIT-007 + CONS-projection-id-contract)"
      min_lines: 40
      contains: "promoted from IMPLICIT-007"
    - path: ".planning/decisions/ADR-008-pagination-shape.md"
      provides: "Locks pagination envelope {items,total,page,page_size,total_pages}, default page=1, page_size=50, caps 500 / 50000 (← IMPLICIT-008 + CONS-pagination-shape)"
      min_lines: 40
      contains: "promoted from IMPLICIT-008"
  key_links:
    - from: "ADR-001-mongodb-datastore.md"
      to: "pltu-tenayan-full-backup/backend/server.py:7,28 (Motor import + AsyncIOMotorClient init)"
      via: "code anchor cite in References section"
      pattern: "backend/server\\.py:(7|28)"
    - from: "ADR-002-fastapi-python-backend.md"
      to: "pltu-tenayan-full-backup/backend/requirements.txt (fastapi==0.110.1, bcrypt, motor, pandas, openpyxl, reportlab)"
      via: "dep-list cite in References"
      pattern: "backend/requirements\\.txt"
    - from: "ADR-003-react-frontend-stack.md"
      to: "pltu-tenayan-full-backup/frontend/package.json (react 19, react-router 7, @radix-ui/*, tailwindcss, axios, recharts, jspdf, xlsx)"
      via: "dep-list cite in References"
      pattern: "frontend/package\\.json"
    - from: "ADR-004-jwt-bcrypt-three-role-auth.md"
      to: "pltu-tenayan-full-backup/backend/server.py:577 (create_token), :599 (require_role), :15-16 (jwt + bcrypt imports); pltu-tenayan-full-backup/docs/audit/AUTH_CONTRACT.md"
      via: "code anchor + sibling decision-record cite"
      pattern: "AUTH_CONTRACT\\.md"
    - from: "ADR-005-gemini-via-legacy-ai-sdk.md"
      to: "pltu-tenayan-full-backup/backend/server.py:19 (legacy-ai-sdk import); pltu-tenayan-full-backup/backend/requirements.txt (legacy-ai-sdk==0.1.0, google-genai, google-generativeai)"
      via: "code anchor + dep cite"
      pattern: "legacy-ai-sdk"
    - from: "ADR-006-api-prefix-and-frontend-base-url.md"
      to: "pltu-tenayan-full-backup/backend/server.py:60 (api_router = APIRouter(prefix=\"/api\")); pltu-tenayan-full-backup/frontend/.env.example or AuthContext.js (REACT_APP_BACKEND_URL)"
      via: "code anchor cite"
      pattern: "APIRouter\\(prefix=\"/api\"\\)"
    - from: "ADR-007-persistence-projection-uuid-iso.md"
      to: "pltu-tenayan-full-backup/backend/server.py:714 (db.vessels.find(query, {\"_id\": 0}))"
      via: "code anchor cite"
      pattern: "\\{\"_id\": 0\\}"
    - from: "ADR-008-pagination-shape.md"
      to: "pltu-tenayan-full-backup/backend/server.py:716-722 (items/total/page/page_size/total_pages return shape on /api/vessels list)"
      via: "code anchor cite"
      pattern: "total_pages"
---

<objective>
Promote IMPLICIT-001..008 (currently inline rows in `.planning/PROJECT.md` "Constraints" section) into 8 formal locked ADR files under `.planning/decisions/`. Each ADR uses MADR layout per D-01..D-03 and cites (a) its source IMPLICIT-NNN line, (b) at least one code anchor (file:line) proving the decision is in effect today, and (c) any related CONS-* constraint from `.planning/intel/constraints.md`.

Purpose: Today there are zero locked ADRs in the project. Future ingest passes / downstream phases / new contributors have no authoritative decision anchor — they would re-derive from PROJECT.md text every time, which is fragile. STAB-04 + DOCS-04 require these to be locked. After this plan, downstream docs (Plan 03 API_REFERENCE generator, Plan 04 PROJECT.md cross-link) cite ADR-NNN paths instead of IMPLICIT-NNN PROJECT.md lines.

Output: 8 markdown files at `.planning/decisions/ADR-001-mongodb-datastore.md` through `ADR-008-pagination-shape.md`. Outer-repo commit only — `.planning/` lives in the outer git tree, not the inner `pltu-tenayan-full-backup/` tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/intel/constraints.md
@.planning/phases/03-documentation-refresh-decision-lock-in/03-CONTEXT.md
@pltu-tenayan-full-backup/docs/audit/AUTH_CONTRACT.md

<interfaces>
<!-- ADR slug map (D-01 through D-03 finalized at plan time): -->
<!-- ADR-001 ← IMPLICIT-001 (MongoDB datastore) -->
<!-- ADR-002 ← IMPLICIT-002 (FastAPI / Python backend stack) -->
<!-- ADR-003 ← IMPLICIT-003 (React frontend stack) -->
<!-- ADR-004 ← IMPLICIT-004 (JWT + bcrypt + 3-role auth model) — sibling to AUTH_CONTRACT.md -->
<!-- ADR-005 ← IMPLICIT-005 (Gemini via legacy-ai-sdk) -->
<!-- ADR-006 ← IMPLICIT-006 (/api/* prefix + REACT_APP_BACKEND_URL) -->
<!-- ADR-007 ← IMPLICIT-007 (projection {_id:0} + UUID id + ISO 8601) -->
<!-- ADR-008 ← IMPLICIT-008 (pagination shape) -->

<!-- MADR section order (D-01): -->
<!-- # ADR-NNN: <Title> -->
<!-- ## Status -->
<!-- ## Context -->
<!-- ## Decision -->
<!-- ## Consequences -->
<!-- ## Alternatives Considered -->
<!-- ## References -->

<!-- Status line literal format (D-02): -->
<!-- Accepted (locked, 2026-05-10) — promoted from IMPLICIT-NNN -->

<!-- References section MUST contain (D-03): -->
<!-- 1. Source IMPLICIT-NNN line citation: `.planning/PROJECT.md` "Constraints" section, the IMPLICIT-NNN row -->
<!-- 2. At least one code anchor: file:line that proves the decision is in effect today (see key_links above for the anchor map) -->
<!-- 3. Related CONS-* constraint(s) from `.planning/intel/constraints.md` where applicable -->

<!-- Code anchors confirmed at plan time via grep on backend/server.py + backend/requirements.txt + frontend/package.json: -->
<!-- - server.py:7  → from motor.motor_asyncio import AsyncIOMotorClient   (IMPLICIT-001) -->
<!-- - server.py:28 → client = AsyncIOMotorClient(mongo_url)               (IMPLICIT-001) -->
<!-- - server.py:60 → api_router = APIRouter(prefix="/api")                (IMPLICIT-006) -->
<!-- - server.py:577 → def create_token(...)                                (IMPLICIT-004) -->
<!-- - server.py:599 → def require_role(allowed_roles: List[str])           (IMPLICIT-004) -->
<!-- - server.py:15-16 → import jwt; import bcrypt                          (IMPLICIT-004) -->
<!-- - server.py:19 → from legacy-ai-sdk.llm.chat import LlmChat     (IMPLICIT-005) -->
<!-- - server.py:714 → db.vessels.find(query, {"_id": 0}).sort(...)         (IMPLICIT-007) -->
<!-- - server.py:716-722 → return shape with items/total/page/page_size/total_pages (IMPLICIT-008) -->
<!-- - requirements.txt: fastapi==0.110.1, bcrypt==4.1.3, motor (verify line), pandas, openpyxl, reportlab (verify lines) — IMPLICIT-002 + IMPLICIT-005 -->
<!-- - frontend/package.json: "react": "^19.x", "react-router-dom": "^7.x", "@radix-ui/*", "tailwindcss", "axios", "recharts", "jspdf", "xlsx" — IMPLICIT-003 -->

<!-- CONS-* mapping (from .planning/intel/constraints.md): -->
<!-- - ADR-004 → CONS-auth-header (lines 13-22 of constraints.md) -->
<!-- - ADR-006 → CONS-api-base (lines 6-12 of constraints.md) -->
<!-- - ADR-007 → CONS-projection-id-contract -->
<!-- - ADR-008 → CONS-pagination-shape (lines 24-32 of constraints.md) -->
<!-- - ADR-001 → CONS-collection-inventory + CONS-collection-naming-debt -->
<!-- - ADR-005 → CONS-ai-query-endpoint (operational note) -->

<!-- Inner-vs-outer repo split (Phase-2 lesson): -->
<!-- All 8 ADR files live UNDER .planning/decisions/ which is in the OUTER repo (/home/damnation/emits). -->
<!-- They will be committed via `git add .planning/decisions/ADR-*.md && git commit ...` from /home/damnation/emits, NOT from pltu-tenayan-full-backup/. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the .planning/decisions/ directory and write ADR-001 through ADR-004</name>
  <files>
    - .planning/decisions/ADR-001-mongodb-datastore.md
    - .planning/decisions/ADR-002-fastapi-python-backend.md
    - .planning/decisions/ADR-003-react-frontend-stack.md
    - .planning/decisions/ADR-004-jwt-bcrypt-three-role-auth.md
  </files>
  <read_first>
    - `.planning/PROJECT.md` lines 86-95 (Constraints section — IMPLICIT-001..004 source rows)
    - `.planning/intel/constraints.md` (CONS-api-base, CONS-auth-header — used by ADR-004 + ADR-006)
    - `pltu-tenayan-full-backup/backend/server.py` lines 1-30 + lines 575-605 (Motor + JWT + role-decorator anchors). One Read call per file with offset/limit; do not re-read.
    - `pltu-tenayan-full-backup/backend/requirements.txt` (fastapi, bcrypt, motor, pandas, openpyxl, reportlab) — first 80 lines is enough.
    - `pltu-tenayan-full-backup/frontend/package.json` "dependencies" block (one Read).
    - `pltu-tenayan-full-backup/docs/audit/AUTH_CONTRACT.md` (cross-link target for ADR-004).
  </read_first>
  <action>
    1. Create the directory if missing: `mkdir -p .planning/decisions/`.
    2. Write each ADR using the MADR layout below. All four files use 2026-05-10 as the lock date in the Status line.

    **MADR template (use this exact section order for every ADR):**

    ```markdown
    # ADR-NNN: <Title>

    ## Status

    Accepted (locked, 2026-05-10) — promoted from IMPLICIT-NNN.

    ## Context

    <2–4 paragraphs: what the system looked like before this decision, what forces drove it, why we are locking it now. Cite the live system state — single-VPS topology, the production data already in flight, the v1-stabilize-before-upgrade posture from PROJECT.md.>

    ## Decision

    <Imperative statement of the locked rule. ONE paragraph for v1; bullets for the specific clauses.>

    ## Consequences

    **Positive:**
    - <2–4 bullets: what becomes easier or safer.>

    **Negative / accepted tradeoffs:**
    - <2–3 bullets: what becomes harder; which v2 doors stay open.>

    ## Alternatives Considered

    <2–3 alternatives with one-line rejection rationale each. NOT a v2 deferral list — these are paths we evaluated and decided against, with reason.>

    ## References

    - **Source IMPLICIT line:** `.planning/PROJECT.md` "Constraints" section, IMPLICIT-NNN row.
    - **Code anchors (proof in effect):**
      - `pltu-tenayan-full-backup/<file>:<line>` — <one-sentence what this proves>
      - <add 1+ more if relevant>
    - **Related constraints:** `.planning/intel/constraints.md` → CONS-<id> (if applicable).
    - **Sibling docs:** <e.g., `pltu-tenayan-full-backup/docs/audit/AUTH_CONTRACT.md` for ADR-004>.
    ```

    **ADR-001 — MongoDB as primary datastore:**
    - Title: "ADR-001: MongoDB (via Motor) as Primary Datastore"
    - Decision: Use MongoDB as the single primary datastore for v1, accessed from the FastAPI backend via the `motor` async driver.
    - Code anchors: `backend/server.py:7` (Motor import), `backend/server.py:28` (AsyncIOMotorClient init).
    - Alternatives Considered (rejected): Postgres (rejected — would require schema migration of 721-row COA + 461-row trucking + 207-row smartstock with no immediate operational benefit); SQLite (rejected — single-host but does not match the document-shape COA reconciliation workload); split datastore (rejected — operational overhead unjustified at current scale).
    - CONS link: CONS-collection-inventory + CONS-collection-naming-debt.
    - Min 40 lines including blank lines between sections.

    **ADR-002 — FastAPI / Python 3.11+ backend stack:**
    - Title: "ADR-002: FastAPI on Python 3.11+ as Backend Stack"
    - Decision: FastAPI 0.110.x on Python 3.11+ with the supporting libraries pinned in `backend/requirements.txt` (motor for Mongo, bcrypt + python-jose/PyJWT for auth, pandas + openpyxl + xlrd for Excel ingest, reportlab for PDF, legacy-ai-sdk for LLM).
    - Code anchors: `backend/requirements.txt` (cite the version-pinned line for fastapi, bcrypt, motor, pandas, openpyxl, reportlab — copy actual lines into the ADR).
    - Alternatives Considered (rejected): Flask (rejected — no built-in OpenAPI generation, async story weaker; we rely on `/openapi.json` for API_REFERENCE.md); Django (rejected — ORM/admin overhead unneeded with Mongo; routes are intentionally simple `/api/*`); Node/Express (rejected — Excel/Pandas data-pipeline parity would cost more than it saves).
    - CONS link: none direct; ADR-006 covers the routing.
    - Min 40 lines.

    **ADR-003 — React 19 frontend stack:**
    - Title: "ADR-003: React 19 + React Router 7 + Tailwind + Shadcn/UI Frontend Stack"
    - Decision: React 19 + React Router 7 + Tailwind CSS + Shadcn/UI (built on Radix primitives) + Axios for HTTP + Recharts for charts + jsPDF + xlsx for client-side export. Build with Yarn (CRA + craco shim).
    - Code anchors: `frontend/package.json` "dependencies" — copy the version pins for `react`, `react-router-dom`, `@radix-ui/react-select` (named because it is implicated in AUTHFIX-04), `tailwindcss`, `axios`, `recharts`, `jspdf`, `xlsx` into the ADR.
    - Alternatives Considered (rejected): Next.js (rejected — single-host CRA build serves fine; SSR not needed for an internal-ops app); Vue/Svelte (rejected — workforce skill match favors React); pure HTML + Alpine.js (rejected — interactivity in COA reconciliation + AI chat justifies a real component framework).
    - **Note in Consequences (negative):** "Radix Select on the register tab emits a benign `ResizeObserver loop` warning during Tabs-content remount; mitigated via page-level suppressor at `frontend/public/index.html:49-65`. AUTHFIX-04 carries the Radix-upgrade-or-replace evaluation forward."
    - Min 40 lines.

    **ADR-004 — JWT + bcrypt + 3-role auth model:**
    - Title: "ADR-004: JWT (Bearer) + bcrypt + Three-Role Authorization Model"
    - Decision: JWT bearer tokens issued by `/api/auth/login` and consumed by every protected route via `Authorization: Bearer <JWT>`; bcrypt hashing for passwords at rest; three roles `admin` / `operator` / `viewer` enforced server-side via `require_role(...)`; HTTP error map locked to **400 (validation), 401 (invalid/expired token), 403 (role denied / missing-Authorization-header), 404 (not found), 500 (internal/AI integration error)** per CONS-auth-header.
    - Code anchors: `backend/server.py:15-16` (jwt + bcrypt imports), `backend/server.py:577` (create_token), `backend/server.py:599` (require_role), `backend/server.py:45-56` (auth_validation_handler — D-AUTH-01 422→400 remap from Phase 2).
    - Sibling docs: `pltu-tenayan-full-backup/docs/audit/AUTH_CONTRACT.md` (Phase 2 reconciliation record — D-AUTH-01, D-AUTH-02). Cross-link in References.
    - Alternatives Considered (rejected): session cookies (rejected — REACT_APP_BACKEND_URL points at a different origin than the frontend dev-server in some setups; bearer tokens sidestep the CORS-credentials dance); OAuth2 / external IdP (rejected — single-plant scope, no SSO requirement; v2 if multi-tenant lands); API keys (rejected — no role granularity).
    - CONS link: CONS-auth-header.
    - Min 40 lines.

    3. Write each file with `Write` tool, absolute path. Do not commit yet — Task 3 commits all 8 files together.
    4. After writing, do not re-read the files to verify; rely on the automated check in `<verify>`.
  </action>
  <verify>
    <automated>for f in ADR-001-mongodb-datastore ADR-002-fastapi-python-backend ADR-003-react-frontend-stack ADR-004-jwt-bcrypt-three-role-auth; do test -f .planning/decisions/$f.md || { echo "MISSING $f"; exit 1; }; LINES=$(grep -v '^[[:space:]]*$' .planning/decisions/$f.md | wc -l); test "$LINES" -ge 30 || { echo "$f too short: $LINES non-blank lines"; exit 1; }; for h in '^## Status' '^## Context' '^## Decision' '^## Consequences' '^## Alternatives Considered' '^## References'; do grep -q "$h" .planning/decisions/$f.md || { echo "$f missing heading $h"; exit 1; }; done; grep -q 'Accepted (locked, 2026-05-10) — promoted from IMPLICIT-' .planning/decisions/$f.md || { echo "$f missing locked status line"; exit 1; }; done; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files exist at the listed paths.
    - Each file contains all 6 MADR H2 headings in order.
    - Each file's Status line matches the literal D-02 format.
    - Each file references at least one `pltu-tenayan-full-backup/...:<line>` code anchor in References.
    - ADR-004 References section cites `pltu-tenayan-full-backup/docs/audit/AUTH_CONTRACT.md`.
  </acceptance_criteria>
  <done>
    Four ADR files written with locked status, MADR layout, and code-anchor citations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write ADR-005 through ADR-008</name>
  <files>
    - .planning/decisions/ADR-005-gemini-via-legacy-ai-sdk.md
    - .planning/decisions/ADR-006-api-prefix-and-frontend-base-url.md
    - .planning/decisions/ADR-007-persistence-projection-uuid-iso.md
    - .planning/decisions/ADR-008-pagination-shape.md
  </files>
  <read_first>
    - `.planning/PROJECT.md` lines 88-95 (IMPLICIT-005..008 source rows) — already in context from Task 1, do NOT re-read.
    - `.planning/intel/constraints.md` (CONS-api-base, CONS-pagination-shape, CONS-projection-id-contract, CONS-ai-query-endpoint) — already in context, do NOT re-read.
    - `pltu-tenayan-full-backup/backend/server.py` lines 685-725 (vessels list endpoint — pagination + projection anchor). One Read call.
    - `pltu-tenayan-full-backup/backend/services/` directory listing (for ADR-005 LLM wiring anchor) — `ls` is enough; do not Read every file.
  </read_first>
  <action>
    Use the same MADR template from Task 1. All four files use 2026-05-10 as lock date.

    **ADR-005 — Gemini via legacy-ai-sdk:**
    - Title: "ADR-005: Google Gemini (gemini-2.5-flash) via legacy-ai-sdk as v1 AI Provider"
    - Decision: For the v1 AI Intelligence Agent (general / blending / boiler / contract / logistics / smart-stock / coa modules), the LLM provider is Google Gemini accessed through the `legacy-ai-sdk` Python SDK. Default model `gemini-2.5-flash`. Per-user keys override the default; the `LEGACY_LLM_KEY` env var is the fallback shared key.
    - Code anchors: `backend/server.py:19` (`from legacy-ai-sdk.llm.chat import LlmChat, UserMessage`), `backend/requirements.txt` (`legacy-ai-sdk==0.1.0`, `google-genai`, `google-generativeai`). List the directory `backend/services/` and cite any AI wiring file present (e.g., `services/ai_intelligence.py` or similar).
    - **Operational note (must appear in Consequences-Negative):** "When the Universal LLM Key budget is exhausted, calls fail with `BudgetExceededError`. This is an environmental / billing concern, not a code defect. Phase 6 (OPS-01, OPS-02) tracks budget restoration and graceful UI error surfacing."
    - Alternatives Considered (rejected for v1, deferred to v2): OpenAI direct (rejected — legacy-ai-sdk already abstracts; switching providers is v2 LLM-V2-01 scope); Anthropic Claude (rejected — same reason); local LLM (rejected — VPS does not have GPU; small-model on CPU inadequate for the prompt complexity in blending / COA modules).
    - CONS link: CONS-ai-query-endpoint (operational note section).
    - Min 40 lines.

    **ADR-006 — /api/* prefix + REACT_APP_BACKEND_URL:**
    - Title: "ADR-006: /api/\\* Backend Route Prefix + REACT_APP_BACKEND_URL Frontend Base"
    - Decision: All HTTP routes the backend exposes are mounted under the `/api` prefix; the frontend resolves its base URL via the `REACT_APP_BACKEND_URL` environment variable at build time. Production: `http://103.150.197.225:8013` (or fronted by nginx in non-dev). Dev: `http://localhost:8013`.
    - Code anchors: `backend/server.py:60` (`api_router = APIRouter(prefix="/api")`), `backend/server.py:608,637,655,665,672,685,724` (sample mount points showing every route is under `api_router.<verb>("/...")`). For frontend: cite `frontend/.env.example` if present and `frontend/src/contexts/AuthContext.js` (uses `process.env.REACT_APP_BACKEND_URL`).
    - Alternatives Considered (rejected): no prefix (rejected — same-host serving of `/` for the React build means `/api` collision-free reservation is required); per-resource prefixes like `/v1` (rejected — single-version v1; deferred to v2 if API versioning becomes necessary); nginx-rewrite-only (rejected — backend ownership of the prefix is more portable than relying on reverse-proxy config).
    - CONS link: CONS-api-base.
    - Min 40 lines.

    **ADR-007 — Persistence projection + UUID + ISO 8601:**
    - Title: "ADR-007: Mongo Projection {_id: 0} + Application-Level UUID id + ISO 8601 Datetimes"
    - Decision: All Mongo `find` / `find_one` reads use projection `{"_id": 0}` so the bson ObjectId never crosses the API boundary. The public-facing identifier is the application-level UUID stored as a string in the `id` field (generated by `uuid.uuid4()` at insert). All datetime fields are serialized as ISO 8601 strings (UTC) on read. Audit metadata fields: `created_at`, `updated_at`, `uploaded_at`, `umpire_completed_at`, `created_by`, `updated_by`, `uploaded_by`, `umpire_completed_by`.
    - Code anchors: `backend/server.py:13` (`import uuid`), `backend/server.py:14` (`from datetime import datetime, timezone, timedelta`), `backend/server.py:714` (`db.vessels.find(query, {"_id": 0}).sort("time_arrival", -1)...`).
    - Alternatives Considered (rejected): expose ObjectId as `_id` (rejected — leaks Mongo internals and changes API surface if datastore migrates later); auto-incrementing integer id (rejected — Mongo is not a SQL RDBMS; no sequence primitive that scales); UUID v7 (considered — could be retrofitted, but UUID v4 is what `uuid.uuid4()` generates and the existing data already uses v4-shaped strings; not worth the migration churn for v1).
    - CONS link: CONS-projection-id-contract.
    - Min 40 lines.

    **ADR-008 — Pagination shape:**
    - Title: "ADR-008: Paginated List Response Envelope `{items, total, page, page_size, total_pages}`"
    - Decision: Every paginated list endpoint returns the JSON envelope `{ "items": [...], "total": N, "page": P, "page_size": S, "total_pages": ceil(N/S) }`. Default `page=1`, `page_size=50`. Operational caps: `page_size <= 500` for vessels/barges/trucking/biomassa/po-batubara/merit-order/coa-reconciliation; `page_size <= 50000` for smart-stock list (see CONS-smart-stock-endpoint). Frontend MUST read `response.data.items` and never assume the response is a bare array.
    - Code anchors: `backend/server.py:685-722` (vessels list endpoint — concrete proof of the shape). Add a verbatim 6-line snippet showing the return statement so a reader sees the shape literally.
    - Alternatives Considered (rejected): cursor-based pagination with `next_cursor` token (rejected — UI uses page-number paginators; offset-based is fine at current row counts: 461 trucking + 721 COA largest); bare array + `X-Total-Count` header (rejected — frontend already reads from `response.data.items`; changing the contract breaks every list view); GraphQL connection pattern with `pageInfo` (rejected — REST contract is locked; GraphQL is not on the v1 or v2 roadmap).
    - CONS link: CONS-pagination-shape.
    - Min 40 lines.

    Write each file with the `Write` tool. Do not commit yet — Task 3 commits all 8 files together.
  </action>
  <verify>
    <automated>for f in ADR-005-gemini-via-legacy-ai-sdk ADR-006-api-prefix-and-frontend-base-url ADR-007-persistence-projection-uuid-iso ADR-008-pagination-shape; do test -f .planning/decisions/$f.md || { echo "MISSING $f"; exit 1; }; LINES=$(grep -v '^[[:space:]]*$' .planning/decisions/$f.md | wc -l); test "$LINES" -ge 30 || { echo "$f too short"; exit 1; }; for h in '^## Status' '^## Context' '^## Decision' '^## Consequences' '^## Alternatives Considered' '^## References'; do grep -q "$h" .planning/decisions/$f.md || { echo "$f missing heading $h"; exit 1; }; done; grep -q 'Accepted (locked, 2026-05-10) — promoted from IMPLICIT-' .planning/decisions/$f.md || { echo "$f missing locked status line"; exit 1; }; done; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files exist.
    - Each contains the 6 MADR H2 headings.
    - Each Status line matches the D-02 literal format.
    - ADR-008 contains a literal pagination-shape snippet citing `backend/server.py:685-722`.
    - ADR-005 Consequences section explicitly notes the BudgetExceededError operational caveat.
  </acceptance_criteria>
  <done>
    Eight ADR files complete (Task 1 + Task 2 combined). Ready to commit.
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit all 8 ADR files to outer repo (atomic)</name>
  <files>
    - .planning/decisions/ADR-001-mongodb-datastore.md
    - .planning/decisions/ADR-002-fastapi-python-backend.md
    - .planning/decisions/ADR-003-react-frontend-stack.md
    - .planning/decisions/ADR-004-jwt-bcrypt-three-role-auth.md
    - .planning/decisions/ADR-005-gemini-via-legacy-ai-sdk.md
    - .planning/decisions/ADR-006-api-prefix-and-frontend-base-url.md
    - .planning/decisions/ADR-007-persistence-projection-uuid-iso.md
    - .planning/decisions/ADR-008-pagination-shape.md
  </files>
  <read_first>
    - Run `git status --short .planning/decisions/` from the outer repo (`/home/damnation/emits`) to confirm 8 untracked ADR files.
  </read_first>
  <action>
    1. From the outer repo root (`/home/damnation/emits`):
    ```
    git add .planning/decisions/ADR-001-mongodb-datastore.md \
            .planning/decisions/ADR-002-fastapi-python-backend.md \
            .planning/decisions/ADR-003-react-frontend-stack.md \
            .planning/decisions/ADR-004-jwt-bcrypt-three-role-auth.md \
            .planning/decisions/ADR-005-gemini-via-legacy-ai-sdk.md \
            .planning/decisions/ADR-006-api-prefix-and-frontend-base-url.md \
            .planning/decisions/ADR-007-persistence-projection-uuid-iso.md \
            .planning/decisions/ADR-008-pagination-shape.md
    ```
    2. Commit with message:
    ```
    docs(phase-3): promote IMPLICIT-001..008 to locked ADRs (DOCS-04, STAB-04)

    Eight MADR-format ADR files under .planning/decisions/, each promoted from
    its corresponding IMPLICIT-NNN row in PROJECT.md and citing at least one
    code anchor (file:line) plus related CONS-* constraint. Future planning
    rounds cite ADR-NNN paths instead of re-deriving from PROJECT.md text.
    ```
    3. Use a HEREDOC for the commit message per CLI conventions. Do NOT use `--no-verify`. Outer repo has no pre-commit hook for ADR files; the inner-repo credential scanner does not run on outer commits.
    4. Verify the commit landed: `git log -1 --name-only` should list all 8 files.
  </action>
  <verify>
    <automated>git log -1 --name-only --pretty=format:%s | head -30 | tee /tmp/adr-commit.out; grep -c '^.planning/decisions/ADR-00[1-8]-' /tmp/adr-commit.out | (read N; test "$N" -eq 8 && echo PASS || { echo "Expected 8 ADR files in last commit, got $N"; exit 1; })</automated>
  </verify>
  <acceptance_criteria>
    - Single commit on outer repo `main` containing all 8 ADR files.
    - Commit subject starts `docs(phase-3): promote IMPLICIT-001..008 to locked ADRs`.
  </acceptance_criteria>
  <done>
    Eight ADR files committed atomically to outer repo.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete, run from `/home/damnation/emits`:
```
ls .planning/decisions/ADR-00{1,2,3,4,5,6,7,8}-*.md | wc -l   # → 8
git log -1 --name-only | grep -c '^.planning/decisions/ADR-' # → 8
for f in .planning/decisions/ADR-*.md; do grep -q '^## References' "$f" || echo "MISSING refs in $f"; done  # → empty
for f in .planning/decisions/ADR-*.md; do grep -q 'Accepted (locked, 2026-05-10) — promoted from IMPLICIT-' "$f" || echo "MISSING locked status in $f"; done  # → empty
```
</verification>

<success_criteria>
- 8 ADR files exist at `.planning/decisions/ADR-001..008-*.md`.
- Each file uses MADR layout (6 H2 sections in order).
- Each Status line is literally `Accepted (locked, 2026-05-10) — promoted from IMPLICIT-NNN.`
- Each References section cites (a) source IMPLICIT line, (b) at least one `pltu-tenayan-full-backup/<file>:<line>` code anchor, (c) related CONS-* constraint.
- Single atomic commit lands all 8 files on outer repo `main`.
- DOCS-04 + STAB-04 are now testably-true.
</success_criteria>

<output>
After all tasks complete, write `.planning/phases/03-documentation-refresh-decision-lock-in/03-01-SUMMARY.md` per the standard summary template — record the 8 file paths, the commit SHA, the IMPLICIT→ADR mapping table, and any code-anchor lines that drifted between plan-time and execute-time.
</output>
