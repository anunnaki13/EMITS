---
phase: 03-documentation-refresh-decision-lock-in
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - pltu-tenayan-full-backup/documentation.md
  - pltu-tenayan-full-backup/readme.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - DOCS-04
  - DOCS-05
must_haves:
  truths:
    - "documentation.md gains a new H2 section 'Known Issues' with at least 5 initial entries per D-09 + D-10 (login bug mitigated; Smart Blending budget; Excel parser pending; Collection naming debt; audit-probe synthetic users record)"
    - "Each Known Issues entry uses the format `**[<status>]** Title — short desc. (Cite: <file path>)` with status drawn from {mitigated, pending-Phase-N, accepted} per D-10"
    - "readme.md gains a one-line pointer 'See [Known Issues](documentation.md#known-issues) for current operational status' per D-09"
    - ".planning/PROJECT.md 'Constraints' section IMPLICIT-001..008 rows are augmented with a → ADR-NNN cross-link at the end of each row, OR a new 'ADR Cross-Links' section is appended that maps IMPLICIT-NNN → ADR-NNN-slug.md (planner picks the option that minimizes blast radius — see action below)"
    - ".planning/PROJECT.md 'Key Decisions' table 'Promote IMPLICIT-001..008 to locked ADRs' row status flips from '— Pending (Phase 3)' to '✓ Applied (Phase 3 plan 03-01, 2026-05-10)'"
  artifacts:
    - path: "pltu-tenayan-full-backup/documentation.md"
      provides: "Operator-facing canonical doc PLUS new 'Known Issues' H2 section with 5 initial entries"
      min_lines: 740
      contains: "## Known Issues"
    - path: "pltu-tenayan-full-backup/readme.md"
      provides: "Top-level readme PLUS one-line Known Issues pointer"
      contains: "Known Issues"
    - path: ".planning/PROJECT.md"
      provides: "Existing project doc PLUS ADR cross-link section + Key Decisions table updated for STAB-04 closure"
      contains: "ADR-001"
  key_links:
    - from: "documentation.md §Known Issues entry 1 (login bug)"
      to: "pltu-tenayan-full-backup/docs/audit/LOGIN_BUG_RESOLUTION.md"
      via: "explicit Cite: clause"
      pattern: "LOGIN_BUG_RESOLUTION\\.md"
    - from: "readme.md"
      to: "documentation.md#known-issues"
      via: "markdown anchor link"
      pattern: "documentation\\.md#known-issues"
    - from: ".planning/PROJECT.md ADR cross-link section"
      to: ".planning/decisions/ADR-001..008-*.md"
      via: "table or list with relative paths"
      pattern: "ADR-00[1-8]"
---

<objective>
Two complementary outputs, both of which depend on Plan 03-01 (ADRs exist) and Plan 03-02 (LOCAL_SETUP recovery runbook exists for Known Issues entry-1 cross-link):

1. **Known Issues section** in `pltu-tenayan-full-backup/documentation.md` (new H2 per D-09) plus a one-line pointer in `readme.md`. Initial entries per D-10:
   - **[mitigated]** Login bug — ResizeObserver suppressor sufficient; cite `docs/audit/LOGIN_BUG_RESOLUTION.md` + regression test in `backend/tests/test_auth_session.py`.
   - **[pending-Phase-6]** Smart Blending AI budget exhausted — Universal LLM Key needs top-up; cite ROADMAP §"Phase 6: Operational Unblocks" + REQUIREMENTS OPS-01.
   - **[pending-Phase-6]** Excel parser unverified against real `total penerimaan.xlsx` — cite REQUIREMENTS OPS-03.
   - **[pending-Phase-5]** Collection naming debt — duplicate pairs documented; rename deferred to Phase 5; cite `DATABASE_SCHEMA.md` + ROADMAP §"Phase 5".
   - **[accepted]** 3 audit-probe-* synthetic users from Phase-1 plan 01-04 — verified clean today (live count 0); cite `LOGIN_BUG_RESOLUTION.md` lines 75-77 + Phase-2 VERIFICATION SS-05.

2. **PROJECT.md ADR cross-link** — make the IMPLICIT→ADR mapping discoverable from PROJECT.md (Plan 03-01 created the ADRs; this plan wires them in). Approach picked at plan-time below.

Why depends on 03-01: each Known Issues entry that touches a locked decision (e.g. login mitigation references the auth contract) cites ADR-004; the PROJECT.md cross-link is the whole point of the dependency. Why depends on 03-02: the login Known Issues entry's "see also" includes LOCAL_SETUP §VPS Service Recovery for operators recovering after a VPS reboot.

Output: one inner-repo commit (documentation.md + readme.md), one outer-repo commit (PROJECT.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-documentation-refresh-decision-lock-in/03-CONTEXT.md
@pltu-tenayan-full-backup/documentation.md
@pltu-tenayan-full-backup/readme.md
@pltu-tenayan-full-backup/docs/audit/LOGIN_BUG_RESOLUTION.md

<interfaces>
<!-- documentation.md outline (head -30 of H2s confirmed at plan time): -->
<!-- 1. Gambaran Umum -->
<!-- 2. Tujuan Sistem -->
<!-- 3. Domain Fitur Lengkap -->
<!-- ... -->
<!-- 17. Dokumen Tambahan -->

<!-- Known Issues placement: append a NEW H2 AFTER the last existing numbered H2 -->
<!-- (after "## 17. Dokumen Tambahan" and any closing footer). Title literal: -->
<!-- ## Known Issues -->
<!-- (Plain — no number; the numbered sections were a SPEC layout, the new section -->
<!-- is operational state-of-the-system data.) -->

<!-- readme.md placement: insert the pointer at the END of the existing "## Ringkasan" -->
<!-- section (line ~7 area), as a one-line "> **Status terkini:** lihat ..." callout, -->
<!-- so the operator sees the link at the top of the README without burying it. -->

<!-- Status badge format (D-10): -->
<!--   `**[mitigated]**` — issue exists; surface impact mitigated; underlying root cause not eliminated -->
<!--   `**[pending-Phase-N]**` — known issue scheduled for closure in Phase N -->
<!--   `**[accepted]**` — issue verified non-impactful today; tracked for record-keeping -->

<!-- Each entry format: -->
<!--   - **[<status>]** <Title> — <one-paragraph desc>. (Cite: <relative file path[:lines]>) -->

<!-- ADR cross-link approach for PROJECT.md (decision at plan time): -->
<!-- The Constraints section already has IMPLICIT-001..008 inline rows (lines 87-94 of -->
<!-- PROJECT.md). EDITING those rows risks blast-radius (every text-search query for -->
<!-- IMPLICIT-NNN would change behavior). PREFERRED approach: append a NEW section -->
<!-- at the end of PROJECT.md called "## ADR Cross-Links (Phase-3 lock-in)" containing -->
<!-- a table that maps IMPLICIT-NNN → ADR-NNN-slug.md. The IMPLICIT lines stay -->
<!-- intact for backwards compatibility; new readers see the table and follow the -->
<!-- ADR; existing planning artifacts that grep for IMPLICIT-NNN keep working. -->

<!-- Inner-vs-outer repo split: -->
<!-- documentation.md + readme.md → INNER repo (pltu-tenayan-full-backup/). Pre-commit hook runs scanner; entries MUST NOT inline credentials. -->
<!-- PROJECT.md → OUTER repo (/home/damnation/emits). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "Known Issues" H2 to documentation.md + readme pointer + commit (inner)</name>
  <files>
    - pltu-tenayan-full-backup/documentation.md
    - pltu-tenayan-full-backup/readme.md
  </files>
  <read_first>
    - `pltu-tenayan-full-backup/documentation.md` last 50 lines (use Read with offset=720, limit=50 to get the file's tail without re-reading the whole 732-line doc).
    - `pltu-tenayan-full-backup/readme.md` lines 1-10 (the "## Ringkasan" section — already in plan-time context).
    - `pltu-tenayan-full-backup/docs/audit/LOGIN_BUG_RESOLUTION.md` lines 75-77 (audit-probe cleanup status — already in plan-time context).
    - VERIFY today: `mongosh --quiet --eval "db.getSiblingDB('pltu_tenayan').users.countDocuments({email:/^audit-probe-/})"` from the inner repo. If non-zero, FLAG IT — do not silently document "0" if reality has changed.
  </read_first>
  <action>
    1. **documentation.md — append the Known Issues H2** at the end of the file (after the current last section "## 17. Dokumen Tambahan" and any closing footer). Use the Edit tool to append; do NOT rewrite the whole file.

    Section content (verbatim, with timestamp `2026-05-10`):
    ```markdown


    ## Known Issues

    Operational state of the system as of 2026-05-10. New issues land here with
    a status badge: `**[mitigated]**` (impact contained but root cause upstream),
    `**[pending-Phase-N]**` (scheduled for closure), or `**[accepted]**` (verified
    non-impactful today). The README points here as the canonical operator-facing
    surface.

    - **[mitigated]** Login: `ResizeObserver loop completed with undelivered notifications`
      console emission on the register tab. Root cause is upstream in `@radix-ui/react-select`
      Tabs-content remount machinery. Suppressed at the page level by
      `frontend/public/index.html:49-65`. Login contract path is regression-protected
      by `backend/tests/test_auth_session.py::test_login_then_me_rehydrates_same_user`
      (Phase-2 plan 02-02). Full disposition + Phase-3 follow-up (Radix upgrade
      evaluation): see `docs/audit/LOGIN_BUG_RESOLUTION.md`. (Cite: `docs/audit/LOGIN_BUG_RESOLUTION.md`,
      `docs/audit/AUTH_CONTRACT.md`, `.planning/decisions/ADR-004-jwt-bcrypt-three-role-auth.md`)

    - **[pending-Phase-6]** Smart Blending AI: Universal LLM Key budget exhausted;
      live calls fail with `BudgetExceededError`. Code path is correct. Phase 6
      (OPS-01, OPS-02) restores the budget and adds graceful UI error surfacing.
      Until then, the Smart Blending UI surfaces the raw error — operators should
      use historical recommendations from `ai_conversations` instead. (Cite:
      `.planning/ROADMAP.md` §"Phase 6: Operational Unblocks", `.planning/REQUIREMENTS.md` OPS-01/OPS-02)

    - **[pending-Phase-6]** Excel parser verification: the parser for
      `total penerimaan.xlsx` has not been validated against a real production
      sample (only synthetic fixtures). Phase 6 OPS-03 closes this with a
      regression fixture checked into the repo. Until then, operators should
      double-check upload results against the Excel source manually. (Cite:
      `.planning/REQUIREMENTS.md` OPS-03)

    - **[pending-Phase-5]** Collection naming debt: four collection pairs
      maintain duplicate names (`smartstock`/`smart_stock`,
      `sumber_pemakaian`/`sumberpemakaian`, `app_settings`/`settings`,
      `ai_chat_history`/`ai_conversations`). Active read targets are documented
      in `DATABASE_SCHEMA.md`; legacy reads still occur in some code paths.
      Phase 5 (DEBT-01..05) picks canonical winners, migrates live data,
      and removes legacy reads. (Cite: `DATABASE_SCHEMA.md`,
      `.planning/intel/constraints.md` → CONS-collection-naming-debt,
      `.planning/ROADMAP.md` §"Phase 5: Collection Naming Debt Resolution")

    - **[accepted]** Audit-probe synthetic users: 3 `audit-probe-*@audit-probes-2026.com`
      users were inserted into the live `users` collection during Phase-1 plan
      01-04 register-flow audit. Verified clean as of 2026-05-10 (live count 0).
      Phase-2 conftest cleanup fixture (`backend/tests/conftest.py`) re-runs the
      anchor-prefixed deletion on every test session; documented for record only —
      no active leak today. (Cite: `docs/audit/LOGIN_BUG_RESOLUTION.md` lines 75-77,
      `.planning/phases/02-authentication-stabilization/VERIFICATION.md` SS-05)

    ### Adding a new entry

    1. Pick the status badge (`mitigated`, `pending-Phase-N`, or `accepted`).
    2. One paragraph: what the user observes, what the root cause is, what the
       mitigation or schedule is.
    3. Cite at least one source file (relative path from repo root).
    4. Commit on the inner repo with subject `docs(known-issues): add <slug>`.
    ```

    **CRITICAL:** if the live audit-probe count returned in `read_first` step 4 is non-zero, REPLACE the "[accepted]" entry's "live count 0" claim with the actual count and downgrade status to `[pending-Phase-3]` plus the cleanup-filter from Phase-2 SUMMARY.

    2. **readme.md — insert pointer after the Ringkasan section** (line ~7 area). Find the line `Sistem ini dibangun untuk membantu tim operasional...` (end of Ringkasan paragraph). After that paragraph, append a one-line callout:
    ```markdown

    > **Status terkini:** lihat [Known Issues](documentation.md#known-issues) untuk login-bug status, Smart Blending AI budget, dan parser Excel verification status.
    ```

    3. Verify scanner clean:
    ```
    cd pltu-tenayan-full-backup && bash scripts/check_credentials.sh
    ```
    Exit 0.

    4. Commit (inner repo):
    ```
    git -C pltu-tenayan-full-backup add documentation.md readme.md
    git -C pltu-tenayan-full-backup commit -m "docs(docs-05): add Known Issues section + readme pointer (D-09, D-10)"
    ```
  </action>
  <verify>
    <automated>cd pltu-tenayan-full-backup && grep -q '^## Known Issues$' documentation.md && grep -c '^- \*\*\[' documentation.md | (read N; test "$N" -ge 5 && echo "issues: $N") && grep -q 'LOGIN_BUG_RESOLUTION\.md' documentation.md && grep -q 'ADR-004-jwt-bcrypt-three-role-auth' documentation.md && grep -q 'OPS-01' documentation.md && grep -q 'CONS-collection-naming-debt' documentation.md && grep -q 'documentation.md#known-issues' readme.md && bash scripts/check_credentials.sh && git log -1 --pretty=format:%s | grep -q 'docs(docs-05).*Known Issues' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - documentation.md contains a literal `## Known Issues` H2.
    - At least 5 entries each starting with `- **[<status>]**`.
    - At least one entry cites `LOGIN_BUG_RESOLUTION.md` and at least one cites `ADR-004-jwt-bcrypt-three-role-auth`.
    - readme.md contains a markdown anchor link to `documentation.md#known-issues`.
    - Credential scanner exits 0.
    - Inner-repo commit subject starts `docs(docs-05): add Known Issues`.
  </acceptance_criteria>
  <done>
    Known Issues section + readme pointer landed; inner-repo commit clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add ADR cross-link section to PROJECT.md + flip Key Decisions row + commit (outer)</name>
  <files>
    - .planning/PROJECT.md
  </files>
  <read_first>
    - `.planning/PROJECT.md` lines 100-115 (Key Decisions table — already in plan-time context).
    - `.planning/decisions/` directory listing — confirm 8 ADR files exist (Plan 03-01 dependency).
  </read_first>
  <action>
    1. **Append a new H2 section to PROJECT.md at the end of the file** (after the closing "*Last updated:*" line). Title literal: `## ADR Cross-Links (Phase-3 lock-in)`.

    Content:
    ```markdown


    ## ADR Cross-Links (Phase-3 lock-in)

    Phase-3 (plan 03-01, 2026-05-10) promoted IMPLICIT-001..008 to formal locked
    ADR files under `.planning/decisions/`. Future planning rounds and downstream
    docs cite the ADR path; the IMPLICIT-NNN rows in the "Constraints" section
    above remain intact for backwards-compatible greps but are now superseded
    by the locked ADRs.

    | IMPLICIT row | ADR file | Subject |
    |--------------|----------|---------|
    | IMPLICIT-001 | [`.planning/decisions/ADR-001-mongodb-datastore.md`](decisions/ADR-001-mongodb-datastore.md) | MongoDB (via Motor) as primary datastore |
    | IMPLICIT-002 | [`.planning/decisions/ADR-002-fastapi-python-backend.md`](decisions/ADR-002-fastapi-python-backend.md) | FastAPI / Python 3.11+ backend stack |
    | IMPLICIT-003 | [`.planning/decisions/ADR-003-react-frontend-stack.md`](decisions/ADR-003-react-frontend-stack.md) | React 19 + Tailwind + Shadcn frontend |
    | IMPLICIT-004 | [`.planning/decisions/ADR-004-jwt-bcrypt-three-role-auth.md`](decisions/ADR-004-jwt-bcrypt-three-role-auth.md) | JWT + bcrypt + 3-role auth model |
    | IMPLICIT-005 | [`.planning/decisions/ADR-005-gemini-via-legacy-ai-sdk.md`](decisions/ADR-005-gemini-via-legacy-ai-sdk.md) | Gemini via legacy-ai-sdk (v1 LLM) |
    | IMPLICIT-006 | [`.planning/decisions/ADR-006-api-prefix-and-frontend-base-url.md`](decisions/ADR-006-api-prefix-and-frontend-base-url.md) | /api/* prefix + REACT_APP_BACKEND_URL |
    | IMPLICIT-007 | [`.planning/decisions/ADR-007-persistence-projection-uuid-iso.md`](decisions/ADR-007-persistence-projection-uuid-iso.md) | Mongo projection + UUID id + ISO 8601 |
    | IMPLICIT-008 | [`.planning/decisions/ADR-008-pagination-shape.md`](decisions/ADR-008-pagination-shape.md) | Pagination envelope `{items,total,page,page_size,total_pages}` |

    Each ADR is locked dated 2026-05-10 and cites at least one code anchor in
    `pltu-tenayan-full-backup/...:<line>` proving the decision is in effect today
    plus the related CONS-* constraint from `.planning/intel/constraints.md`.
    ```

    2. **Edit the Key Decisions table** (line ~106 of PROJECT.md). Find the row:
    ```
    | Promote IMPLICIT-001..008 to locked ADRs | Currently zero locked ADRs; future ingest passes have no authoritative decision anchors | — Pending (Phase 3) |
    ```
    Change the Outcome column to:
    ```
    | Promote IMPLICIT-001..008 to locked ADRs | Currently zero locked ADRs; future ingest passes have no authoritative decision anchors | ✓ Applied (Phase 3 plan 03-01, 2026-05-10) |
    ```

    3. Update the closing line:
    ```
    *Last updated: 2026-05-10 after gsd-new-project bootstrap from `pltu-tenayan-full-backup` ingest.*
    ```
    to:
    ```
    *Last updated: 2026-05-10 after Phase-3 plan 03-04 ADR cross-link.*
    ```

    4. Commit (outer repo):
    ```
    git add .planning/PROJECT.md
    git commit -m "docs(docs-04): add ADR cross-link section to PROJECT.md (STAB-04 closure)"
    ```
  </action>
  <verify>
    <automated>grep -q '^## ADR Cross-Links (Phase-3 lock-in)$' .planning/PROJECT.md && for n in 1 2 3 4 5 6 7 8; do grep -q "ADR-00${n}-" .planning/PROJECT.md || { echo "missing ADR-00${n} cross-link"; exit 1; }; done && grep -q '✓ Applied (Phase 3 plan 03-01, 2026-05-10)' .planning/PROJECT.md && grep -q 'Last updated: 2026-05-10 after Phase-3 plan 03-04' .planning/PROJECT.md && git log -1 --pretty=format:%s -- .planning/PROJECT.md | grep -q 'docs(docs-04).*ADR cross-link.*STAB-04' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - PROJECT.md contains an `## ADR Cross-Links (Phase-3 lock-in)` H2 with all 8 ADR rows.
    - Key Decisions table "Promote IMPLICIT-001..008..." row shows `✓ Applied`.
    - Closing footer reads `Last updated: 2026-05-10 after Phase-3 plan 03-04`.
    - Outer-repo commit lands.
  </acceptance_criteria>
  <done>
    PROJECT.md cross-link landed; STAB-04 closed at the project-doc layer.
  </done>
</task>

</tasks>

<verification>
After all tasks:
```
# Inner repo
grep -c '^## Known Issues$' pltu-tenayan-full-backup/documentation.md   # → 1
grep -c '^- \*\*\[' pltu-tenayan-full-backup/documentation.md           # → ≥5
grep -q 'documentation.md#known-issues' pltu-tenayan-full-backup/readme.md
bash pltu-tenayan-full-backup/scripts/check_credentials.sh   # → exit 0

# Outer repo
grep -c '^## ADR Cross-Links' .planning/PROJECT.md   # → 1
grep -c 'ADR-00[1-8]-' .planning/PROJECT.md         # → ≥8
```
</verification>

<success_criteria>
- DOCS-05: documentation.md gains Known Issues H2 with 5 initial entries (login mitigated, Smart Blending pending Phase 6, parser pending Phase 6, collection debt pending Phase 5, audit-probe accepted/clean).
- DOCS-04 tail: PROJECT.md has an ADR cross-link table mapping IMPLICIT-NNN → ADR-NNN-slug.md, and the Key Decisions table reflects STAB-04 closure.
- README points at documentation.md#known-issues so operators have a single canonical state surface.
- Two atomic commits: one inner (documentation.md + readme.md), one outer (PROJECT.md).
</success_criteria>

<output>
After tasks complete, write `.planning/phases/03-documentation-refresh-decision-lock-in/03-04-SUMMARY.md` per standard summary template — record the inner + outer commit SHAs, the live audit-probe count from the verification step (so future readers know whether the [accepted] status is still valid), and the literal anchor text for `documentation.md#known-issues` to aid downstream Plan 03-05 readme verification.
</output>
