---
phase: 03-documentation-refresh-decision-lock-in
plan: 05
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-04
files_modified:
  - pltu-tenayan-full-backup/DATABASE_SCHEMA.md
  - pltu-tenayan-full-backup/DEPLOYMENT_GUIDE.md
  - pltu-tenayan-full-backup/frontend/public/docs/Smart_Blending_AI_Formula.md
  - pltu-tenayan-full-backup/docs/audit/SMART_BLENDING_FORMULA_AUDIT.md
  - pltu-tenayan-full-backup/readme.md
  - pltu-tenayan-full-backup/scripts/check_credentials.sh
  - pltu-tenayan-full-backup/docs/audit/CREDENTIAL_HYGIENE.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-03
must_haves:
  truths:
    - "DATABASE_SCHEMA.md correctly identifies the active read target for each duplicate-pair collection (smartstock vs smart_stock; sumberpemakaian vs sumber_pemakaian; app_settings vs settings; ai_chat_history vs ai_conversations) — proven by grep against backend/server.py + verified row counts against live MongoDB (smartstock=207, sumberpemakaian=208, etc.)"
    - "DATABASE_SCHEMA.md does NOT rename or migrate any collection — Phase 5 owns rename. This plan only documents reality (per Phase-3 boundary in 03-CONTEXT.md)"
    - "DEPLOYMENT_GUIDE.md is reconciled against the actual VPS install (port 8013 backend, port 3013 frontend, single-host MongoDB on 27017, REACT_APP_BACKEND_URL=http://103.150.197.225:8013) and contains a one-line cross-link to LOCAL_SETUP.md §VPS Service Recovery (per D-11 reciprocal pointer)"
    - "DEPLOYMENT_GUIDE.md no longer inlines the admin password literal or `${MONGO_URL}` example string — both replaced with env-var placeholders, closing two of the CREDENTIAL_HYGIENE.md 'Known exemptions' (STAB-03 TODO entries)"
    - "Smart_Blending_AI_Formula.md (located at frontend/public/docs/Smart_Blending_AI_Formula.md per plan-time discovery) is verified against CONS-blending-formula and CONS-input-ranges from .planning/intel/constraints.md; if any drift exists, an audit log lists the discrepancy AND the canonical formula is restated correctly in the doc"
    - "An audit log at docs/audit/SMART_BLENDING_FORMULA_AUDIT.md records the verification outcome (drift found / no drift) per plan-3 deliverable 8 in 03-CONTEXT.md"
    - "readme.md content is verified against the live system (port numbers, REACT_APP_BACKEND_URL examples, role list) — any drift is corrected in this plan or flagged in the SUMMARY"
  artifacts:
    - path: "pltu-tenayan-full-backup/DATABASE_SCHEMA.md"
      provides: "Existing schema reference PLUS a Phase-3-stamped 'Duplicate Pair Active Read Targets' subsection that names the active vs legacy collection for each pair"
      min_lines: 700
      contains: "active read target"
    - path: "pltu-tenayan-full-backup/DEPLOYMENT_GUIDE.md"
      provides: "Reconciled deployment guide — actual ports, env-var placeholders (no inline secrets), cross-link to LOCAL_SETUP.md §VPS Service Recovery"
      min_lines: 540
      contains: "VPS Service Recovery"
    - path: "pltu-tenayan-full-backup/frontend/public/docs/Smart_Blending_AI_Formula.md"
      provides: "Existing formula doc PLUS a Phase-3 verification stamp confirming alignment with CONS-blending-formula (or recording corrections)"
      contains: "Verified against CONS-blending-formula"
    - path: "pltu-tenayan-full-backup/docs/audit/SMART_BLENDING_FORMULA_AUDIT.md"
      provides: "New audit log of the formula-verification step — what was checked, what (if anything) drifted, what was corrected"
      min_lines: 25
      contains: "CONS-blending-formula"
    - path: "pltu-tenayan-full-backup/readme.md"
      provides: "Top-level readme verified against live system; any port/URL/role drift fixed"
      contains: "Known Issues"
  key_links:
    - from: "DATABASE_SCHEMA.md §Duplicate Pair Active Read Targets"
      to: "pltu-tenayan-full-backup/backend/server.py grep results for db.<collection>.find / db.<collection>.aggregate"
      via: "grep evidence cite per pair"
      pattern: "active read target"
    - from: "DEPLOYMENT_GUIDE.md"
      to: "LOCAL_SETUP.md §VPS Service Recovery (post-restart)"
      via: "explicit anchor link"
      pattern: "LOCAL_SETUP\\.md.*VPS Service Recovery"
    - from: "Smart_Blending_AI_Formula.md"
      to: ".planning/intel/constraints.md → CONS-blending-formula"
      via: "constraint cite + audit-log cross-link"
      pattern: "CONS-blending-formula"
    - from: "docs/audit/SMART_BLENDING_FORMULA_AUDIT.md"
      to: "frontend/public/docs/Smart_Blending_AI_Formula.md"
      via: "explicit cite of the verified file"
      pattern: "Smart_Blending_AI_Formula\\.md"
---

<objective>
Final reconciliation pass — three docs that did not need ADR or generator scaffolding but DO need to be verified against the live system, plus the credential-scanner exemption removal for the inline secrets in DEPLOYMENT_GUIDE.md.

1. **DATABASE_SCHEMA.md audit** (DOCS-03): for each duplicate-pair collection (smartstock/smart_stock, sumber_pemakaian/sumberpemakaian, app_settings/settings, ai_chat_history/ai_conversations), name the active read target by grep evidence in `backend/server.py`. Document — do NOT rename — per Phase-3 boundary.
2. **DEPLOYMENT_GUIDE.md reconciliation** (DOCS-01): update ports/URLs to actual VPS state (8013 backend, 3013 frontend, MongoDB on localhost:27017), strip inline secrets per CREDENTIAL_HYGIENE.md "Known exemptions", add cross-link to LOCAL_SETUP.md §VPS Service Recovery (the reciprocal D-11 pointer; LOCAL_SETUP got the runbook in Plan 03-02, this plan adds the back-pointer).
3. **Smart_Blending_AI_Formula.md verification** (DOCS-01): the file lives at `frontend/public/docs/Smart_Blending_AI_Formula.md` (planner discovered the path at plan time; PROJECT.md and ROADMAP referenced it by name without specifying location). Verify formula against CONS-blending-formula / CONS-input-ranges / CONS-constraint-validation from `.planning/intel/constraints.md`; record outcome in a new audit log at `docs/audit/SMART_BLENDING_FORMULA_AUDIT.md`.
4. **readme.md verification** (DOCS-01): readme content (port numbers, REACT_APP_BACKEND_URL, role list, feature list) — verify against live; correct any drift. Plan 03-04 already added the Known Issues pointer; this plan double-checks the rest of the file.

Why depends on 03-04: readme.md was edited in Plan 03-04 (Known Issues pointer). This plan must read the post-04 state and only correct anything else; no overlap on that one line. Why depends on 03-02: DEPLOYMENT_GUIDE.md cross-links to LOCAL_SETUP.md §VPS Service Recovery (which Plan 03-02 created).

Output: one inner-repo commit covering all 5 files (atomic — they are a single doc-reconciliation deliverable).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/intel/constraints.md
@.planning/phases/03-documentation-refresh-decision-lock-in/03-CONTEXT.md
@pltu-tenayan-full-backup/DATABASE_SCHEMA.md
@pltu-tenayan-full-backup/DEPLOYMENT_GUIDE.md
@pltu-tenayan-full-backup/docs/audit/CREDENTIAL_HYGIENE.md
@pltu-tenayan-full-backup/LOCAL_SETUP.md

<interfaces>
<!-- Live data inventory (from PROJECT.md "Context" table): -->
<!--   smartstock=207 records (active per PROJECT.md note "active read target") -->
<!--   smart_stock = legacy -->
<!--   sumberpemakaian=208 records (active) -->
<!--   sumber_pemakaian = legacy -->
<!--   app_settings vs settings — needs grep determination -->
<!--   ai_chat_history=10 records (legacy of ai_conversations) -->
<!--   ai_conversations — needs grep determination + row count -->

<!-- Grep commands to identify active read target (run in execute time): -->
<!--   grep -nE 'db\.(smartstock|smart_stock)\.' backend/server.py -->
<!--   grep -nE 'db\.(sumberpemakaian|sumber_pemakaian)\.' backend/server.py -->
<!--   grep -nE 'db\.(app_settings|settings)\.' backend/server.py -->
<!--   grep -nE 'db\.(ai_chat_history|ai_conversations)\.' backend/server.py -->
<!-- The collection that appears in production read paths (find/aggregate/count) -->
<!-- is the active target. Document line numbers for each. -->

<!-- DEPLOYMENT_GUIDE.md current state (per CREDENTIAL_HYGIENE.md "Known exemptions"): -->
<!-- - Contains `${MONGO_URL}` example string -->
<!-- - Contains inline `<TEST_ADMIN_PASSWORD>` literal in curl examples -->
<!-- Both must be replaced with `${MONGO_URL}` / `${TEST_ADMIN_PASSWORD}` placeholders. -->

<!-- Smart_Blending_AI_Formula.md location (planner-confirmed via find): -->
<!--   pltu-tenayan-full-backup/frontend/public/docs/Smart_Blending_AI_Formula.md -->
<!-- This is the in-app doc viewer's copy. There is no top-level Smart_Blending_AI_Formula.md. -->
<!-- Per ROADMAP success criterion 1, this file must reflect actual system behavior. -->

<!-- CONS-blending-formula key clauses (from .planning/intel/constraints.md, plan-time read): -->
<!--   - Linear weighted-average blend across GCV/Ash/Sulphur/TM/IM/VM/FC -->
<!--   - Constraint inequalities: GCV_blend ≥ target, Ash/Sulphur/TM/IM ≤ max, VM/FC ≥ min -->
<!--   - ±5% prediction tolerance -->
<!--   - Output JSON shape locked -->

<!-- Inner-vs-outer repo split: ALL 5 files are inner-repo. Single inner commit. -->

<!-- Plan 03-04 readme.md modification: added a `> **Status terkini:** ...` callout -->
<!-- after the Ringkasan section. This plan must NOT touch that line. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DATABASE_SCHEMA.md duplicate-pair active-target audit</name>
  <files>
    - pltu-tenayan-full-backup/DATABASE_SCHEMA.md
  </files>
  <read_first>
    - `pltu-tenayan-full-backup/DATABASE_SCHEMA.md` — H2 outline only via `grep -n '^## ' DATABASE_SCHEMA.md` (do not Read the full 691-line file).
    - `pltu-tenayan-full-backup/backend/server.py` — targeted grep, NOT full file Read:
      ```
      grep -nE 'db\.(smartstock|smart_stock|sumberpemakaian|sumber_pemakaian|app_settings|settings|ai_chat_history|ai_conversations)\.' backend/server.py
      ```
      This identifies the active read target by code evidence. ONE grep call covers all 4 pairs.
    - LIVE MongoDB row counts (read-only):
      ```
      mongosh --quiet --eval 'use pltu_tenayan; ["smartstock","smart_stock","sumberpemakaian","sumber_pemakaian","app_settings","settings","ai_chat_history","ai_conversations"].forEach(c=>print(c+": "+db[c].countDocuments({})))'
      ```
  </read_first>
  <action>
    1. From the grep output, build a 4-row determination table (one row per pair). Fields:
       | Pair | Active read target (proof) | Legacy (proof) | Live count active | Live count legacy |

       The "proof" cell cites file:line — e.g. `backend/server.py:782 db.smartstock.find(...)`. If both names appear in code, the one used by the canonical list endpoint (the one a user-facing page consumes) is active; the other is legacy. If only one appears, the absent one is legacy by definition.

    2. Find the appropriate insertion point in DATABASE_SCHEMA.md — typically near the existing collection-naming-debt note (CONS-collection-naming-debt is sourced from this file per `.planning/intel/constraints.md`). Use `grep -n -i 'duplicate\|legacy\|naming' DATABASE_SCHEMA.md` to locate. If a "## Collection Naming Debt" or similar section exists, INSERT the new audit subsection there. Otherwise APPEND a new H2 "## Duplicate Pair Active Read Targets (Phase-3 audit, 2026-05-10)" at the end of the file.

    3. The new section text (verbatim, with the determination table populated from your grep):

    ```markdown


    ## Duplicate Pair Active Read Targets (Phase-3 audit, 2026-05-10)

    Phase-3 plan 03-05 documents the active read target for each duplicate-pair
    collection. **This is documentation only — Phase 5 (DEBT-01..05) owns the
    rename and migration.** Determinations are based on `grep -nE 'db\.<name>\.'
    backend/server.py` evidence and live row counts as of 2026-05-10.

    | Pair | Active (read target) | Legacy | Active count | Legacy count | Code evidence |
    |------|----------------------|--------|--------------|--------------|---------------|
    | smartstock vs smart_stock | <fill at execute-time> | <fill> | <count> | <count> | `backend/server.py:<line>` |
    | sumberpemakaian vs sumber_pemakaian | <fill> | <fill> | <count> | <count> | `backend/server.py:<line>` |
    | app_settings vs settings | <fill> | <fill> | <count> | <count> | `backend/server.py:<line>` |
    | ai_chat_history vs ai_conversations | <fill> | <fill> | <count> | <count> | `backend/server.py:<line>` |

    **Method:** `grep -nE 'db\.(smartstock|smart_stock|...)\.' backend/server.py`
    plus live `db.<name>.countDocuments({})` against the production VPS
    (read-only — no writes).

    **Phase 5 dependency:** the rename plan (DEBT-01..05) MUST start from this
    table — picking a canonical winner per row, dry-running migration, and
    removing legacy reads only after verified row-count + checksum parity.

    See `.planning/ROADMAP.md` §"Phase 5: Collection Naming Debt Resolution"
    and `.planning/intel/constraints.md` → CONS-collection-naming-debt.
    ```

    4. Replace the four `<fill>` rows with the actual grep + count results from `read_first`. Each row's "Code evidence" cell MUST contain at least one `backend/server.py:<line>` reference.

    5. If any pair shows BOTH names in active read paths (e.g. one endpoint reads `smartstock`, another reads `smart_stock`), document this with a note: "**Both names actively read** — line X reads <name>, line Y reads <other>; canonical winner deferred to Phase 5."

    6. Do NOT delete or modify any pre-existing CONS-collection-naming-debt content in DATABASE_SCHEMA.md. The new section is additive.
  </action>
  <verify>
    <automated>cd pltu-tenayan-full-backup && grep -q '^## Duplicate Pair Active Read Targets' DATABASE_SCHEMA.md && grep -c 'backend/server.py:[0-9]' DATABASE_SCHEMA.md | (read N; test "$N" -ge 4 && echo "code refs: $N") && grep -q 'smartstock' DATABASE_SCHEMA.md && grep -q 'sumberpemakaian' DATABASE_SCHEMA.md && grep -q 'ai_conversations' DATABASE_SCHEMA.md && bash scripts/check_credentials.sh && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - DATABASE_SCHEMA.md contains the new "Duplicate Pair Active Read Targets" section with all 4 pairs filled in.
    - At least 4 `backend/server.py:<line>` code references in the new section.
    - Section explicitly defers Phase-5 rename ownership.
    - Credential scanner clean (no secrets introduced).
  </acceptance_criteria>
  <done>
    DATABASE_SCHEMA.md audit section landed; ready for Phase 5 to consume as input.
  </done>
</task>

<task type="auto">
  <name>Task 2: DEPLOYMENT_GUIDE.md reconciliation + secret strip + cross-link</name>
  <files>
    - pltu-tenayan-full-backup/DEPLOYMENT_GUIDE.md
    - pltu-tenayan-full-backup/scripts/check_credentials.sh
    - pltu-tenayan-full-backup/docs/audit/CREDENTIAL_HYGIENE.md
  </files>
  <read_first>
    - `pltu-tenayan-full-backup/DEPLOYMENT_GUIDE.md` H2 outline (`grep -n '^## ' DEPLOYMENT_GUIDE.md`) THEN targeted Read of any section containing inline secrets. Use `grep -nE '<TEST_ADMIN_PASSWORD>|mongodb\+?srv?://[^[:space:]/]+:[^[:space:]/]+@' DEPLOYMENT_GUIDE.md` to locate the lines that need editing. Read only those line ranges.
    - `pltu-tenayan-full-backup/scripts/check_credentials.sh` (full content — already in context from Plan 03-03 read; confirm DEPLOYMENT_GUIDE.md is in the EXCLUDE list).
    - `pltu-tenayan-full-backup/docs/audit/CREDENTIAL_HYGIENE.md` "Known exemptions" section (already in context).
    - `pltu-tenayan-full-backup/LOCAL_SETUP.md` last 60 lines (the "VPS Service Recovery" section appended in Plan 03-02 — confirm anchor name).
  </read_first>
  <action>
    1. **Replace inline secrets** in DEPLOYMENT_GUIDE.md:
       - Every `<TEST_ADMIN_PASSWORD>` literal in curl example bodies → `${TEST_ADMIN_PASSWORD}` placeholder, with a one-line note above the curl block: `# Source from gitignored memory/test_credentials.md — see docs/audit/CREDENTIAL_HYGIENE.md`.
       - Every `${MONGO_URL}` or `${MONGO_URL}` example → `${MONGO_URL}` placeholder. Add a one-line note: `# Set MONGO_URL in backend/.env; example shape mongodb://localhost:27017 (single-host VPS topology per ADR-001).`
       - Use `Edit` tool surgical replacements; do NOT rewrite the file.

    2. **Reconcile ports / URLs** to actual VPS state. From `read_first` step 1, identify any line claiming a port other than:
       - Backend: 8013
       - Frontend: 3013
       - MongoDB: 27017 (localhost on the VPS)
       Plus any `REACT_APP_BACKEND_URL=https://your-domain.com/api` placeholder — replace with `REACT_APP_BACKEND_URL=http://103.150.197.225:8013` for the production-VPS section, keeping a `http://localhost:8013` example for local dev.

    3. **Add cross-link to LOCAL_SETUP.md §VPS Service Recovery.** Find the section in DEPLOYMENT_GUIDE.md that covers post-deploy / restart (often "Operational Notes" or "Maintenance" — locate via `grep -n 'restart\|recovery\|maintenance' DEPLOYMENT_GUIDE.md`). If no such section exists, append a new H2 at the end:

    ```markdown


    ## Service Recovery (post-restart)

    For the exact uvicorn + yarn-start commands to bring services back up after
    a VPS reboot, see [LOCAL_SETUP.md → VPS Service Recovery (post-restart)](LOCAL_SETUP.md#vps-service-recovery-post-restart).
    Phase-3 D-11 promoted that procedure to a single canonical runbook so
    operators do not rediscover commands across multiple docs.
    ```

    4. **Remove DEPLOYMENT_GUIDE.md from the credential scanner exemption list** in `scripts/check_credentials.sh`. Find the EXCLUDE entry for `DEPLOYMENT_GUIDE.md` (top-level) and delete that one line. **Leave** the `frontend/public/docs/DEPLOYMENT_GUIDE.md` entry in place (that copy is the in-app doc viewer's mirror; it'll need a separate STAB-03 follow-up if any text differs — but that's out of this plan's budget).

    5. **Update CREDENTIAL_HYGIENE.md "Known exemptions"** — strike or remove the line referring to top-level `DEPLOYMENT_GUIDE.md`. Keep the `frontend/public/docs/DEPLOYMENT_GUIDE.md` line. Add a closing note: "DEPLOYMENT_GUIDE.md (top-level) cleared 2026-05-10 by Phase-3 plan 03-05; frontend/public/docs mirror still uses old format — Phase 4 or later TODO."

    6. **Run scanner** to confirm clean:
    ```
    cd pltu-tenayan-full-backup && bash scripts/check_credentials.sh
    ```
    Exit 0 expected. If it fails, fix any remaining inline literals.
  </action>
  <verify>
    <automated>cd pltu-tenayan-full-backup && ! grep -E '<TEST_ADMIN_PASSWORD>' DEPLOYMENT_GUIDE.md && ! grep -E 'MONGO_URL_WITH_CREDS_PATTERN' DEPLOYMENT_GUIDE.md && grep -q 'LOCAL_SETUP\.md.*[Vv]ps.[Ss]ervice.[Rr]ecovery' DEPLOYMENT_GUIDE.md && grep -q '\${TEST_ADMIN_PASSWORD}' DEPLOYMENT_GUIDE.md && grep -q '\${MONGO_URL}' DEPLOYMENT_GUIDE.md && grep -q '8013' DEPLOYMENT_GUIDE.md && ! grep -E '^[[:space:]]*"DEPLOYMENT_GUIDE\.md"' scripts/check_credentials.sh && bash scripts/check_credentials.sh && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - DEPLOYMENT_GUIDE.md contains zero `<TEST_ADMIN_PASSWORD>` literals and zero MongoDB-URI-with-creds patterns.
    - Contains `${TEST_ADMIN_PASSWORD}` and `${MONGO_URL}` placeholders.
    - Contains a working anchor link to `LOCAL_SETUP.md#vps-service-recovery-post-restart`.
    - References port 8013 (backend) explicitly.
    - `scripts/check_credentials.sh` no longer exempts top-level `DEPLOYMENT_GUIDE.md` and exits 0.
    - CREDENTIAL_HYGIENE.md exemption list updated.
  </acceptance_criteria>
  <done>
    DEPLOYMENT_GUIDE.md reconciled, secrets stripped, cross-link added; one more exemption cleared from the scanner.
  </done>
</task>

<task type="auto">
  <name>Task 3: Smart_Blending_AI_Formula.md verification + audit log + readme verification + atomic inner commit</name>
  <files>
    - pltu-tenayan-full-backup/frontend/public/docs/Smart_Blending_AI_Formula.md
    - pltu-tenayan-full-backup/docs/audit/SMART_BLENDING_FORMULA_AUDIT.md
    - pltu-tenayan-full-backup/readme.md
  </files>
  <read_first>
    - `pltu-tenayan-full-backup/frontend/public/docs/Smart_Blending_AI_Formula.md` (full file — read once; size unknown, likely small).
    - `.planning/intel/constraints.md` — already in context for CONS-blending-formula. Re-read targeted lines if needed (CONS-blending-formula and adjacent CONS-input-ranges / CONS-constraint-validation / CONS-ai-output / CONS-tolerance).
    - `pltu-tenayan-full-backup/readme.md` (full file — verify content vs live system; one Read).
  </read_first>
  <action>
    1. **Smart_Blending_AI_Formula.md verification.** Read the file. Compare against CONS-blending-formula key clauses:
       - Linear weighted-average blend across GCV/Ash/Sulphur/TM/IM/VM/FC
       - Constraint inequalities: GCV_blend ≥ target, Ash/Sulphur/TM/IM ≤ max, VM/FC ≥ min
       - ±5% prediction tolerance
       - Output JSON shape

       **Three possible outcomes:**
       - (a) **No drift:** the file matches CONS-blending-formula. Append a verification stamp at the END of the file:
         ```markdown


         ---

         **Phase-3 verification (2026-05-10):** This formula is verified against
         `.planning/intel/constraints.md` → CONS-blending-formula, CONS-input-ranges,
         CONS-constraint-validation, CONS-ai-output, CONS-tolerance. No drift detected.
         See `docs/audit/SMART_BLENDING_FORMULA_AUDIT.md` for the full audit log.
         ```
       - (b) **Minor drift** (e.g., a constraint inequality is reversed, or a tolerance value differs): correct the drift in-line. The audit log records what was found and what was changed.
       - (c) **Major drift** (formula structurally different from CONS): do NOT silently rewrite. Stop, document the divergence in the audit log, and flag in the SUMMARY for human review. Phase-3 boundary is "verify and document" — major rewrites belong in a focused plan.

    2. **Write `pltu-tenayan-full-backup/docs/audit/SMART_BLENDING_FORMULA_AUDIT.md`** (new file). Structure:

    ```markdown
    # Smart Blending AI Formula Audit (Phase-3 plan 03-05)

    **Verified:** 2026-05-10
    **Source of truth:** `.planning/intel/constraints.md` → CONS-blending-formula,
    CONS-input-ranges, CONS-constraint-validation, CONS-ai-output, CONS-tolerance.
    **File audited:** `frontend/public/docs/Smart_Blending_AI_Formula.md`.

    ## Outcome

    <one of: NO DRIFT / MINOR DRIFT (corrected) / MAJOR DRIFT (flagged for human review)>

    ## Clause-by-clause comparison

    | CONS clause | Doc claim | Match? | Notes |
    |-------------|-----------|--------|-------|
    | Linear weighted-average blend across GCV/Ash/Sulphur/TM/IM/VM/FC | <quote from doc> | ✓ / ✗ | <note if drift> |
    | GCV_blend ≥ target | <quote> | ✓ / ✗ | |
    | Ash/Sulphur/TM/IM ≤ max | <quote> | ✓ / ✗ | |
    | VM/FC ≥ min | <quote> | ✓ / ✗ | |
    | ±5% prediction tolerance | <quote> | ✓ / ✗ | |
    | Output JSON shape locked | <quote> | ✓ / ✗ | |

    ## Corrections applied (if any)

    - <list each in-line edit, with before/after line snippets>

    ## Operational caveat

    The formula is correct and locked. Operational availability of Smart Blending
    AI is degraded today (LLM key budget — see Known Issues entry "Smart Blending
    AI" + REQUIREMENTS OPS-01). Code path matches formula; budget restoration in
    Phase 6 unblocks live recommendations.

    ## Cross-references

    - `.planning/intel/constraints.md` → CONS-blending-formula and adjacent CONS-* rows
    - `pltu-tenayan-full-backup/frontend/public/docs/Smart_Blending_AI_Formula.md` (the audited doc)
    - `pltu-tenayan-full-backup/documentation.md` §Known Issues entry "Smart Blending AI"
    ```

    Fill in the comparison table from your verification. The audit log is REQUIRED regardless of outcome (provides the record DOCS-01 demands).

    3. **readme.md verification.** Read the full file once (175-187 lines). Check each fact against current state:
       - Login/auth section mentions JWT + 3 roles (admin/operator/viewer) → CORRECT per ADR-004; no edit needed.
       - Feature list mentions Smart Blending AI → fine; the operational degradation is captured in Known Issues (added by Plan 03-04).
       - Any port numbers (`8001`, `8013`, `3000`, `3013`)? Verify against actual ports (backend 8013, frontend 3013).
       - Any `REACT_APP_BACKEND_URL=...` example? Should be `http://103.150.197.225:8013` for prod or `http://localhost:8013` for dev.
       - Any deprecated API paths? Spot-check by grepping for `/api/` lines and matching against the regenerated API_REFERENCE.md.

       Apply surgical fixes for any drift found. If readme is internally consistent and matches reality, no edit needed beyond what Plan 03-04 already added.

    4. **Atomic inner-repo commit** covering all 5 files modified across Tasks 1-3:
    ```
    git -C pltu-tenayan-full-backup add \
      DATABASE_SCHEMA.md \
      DEPLOYMENT_GUIDE.md \
      scripts/check_credentials.sh \
      docs/audit/CREDENTIAL_HYGIENE.md \
      frontend/public/docs/Smart_Blending_AI_Formula.md \
      docs/audit/SMART_BLENDING_FORMULA_AUDIT.md \
      readme.md
    git -C pltu-tenayan-full-backup commit -m "docs(docs-01,docs-03): final reconciliation — schema audit, deploy guide cleanup, blending formula verify (D-11 reciprocal)"
    ```
    Pre-commit hook runs `scripts/check_credentials.sh` — must exit 0.
  </action>
  <verify>
    <automated>cd pltu-tenayan-full-backup && grep -q 'CONS-blending-formula' frontend/public/docs/Smart_Blending_AI_Formula.md && test -f docs/audit/SMART_BLENDING_FORMULA_AUDIT.md && grep -q 'CONS-blending-formula' docs/audit/SMART_BLENDING_FORMULA_AUDIT.md && grep -E '^## Outcome' docs/audit/SMART_BLENDING_FORMULA_AUDIT.md && bash scripts/check_credentials.sh && git log -1 --pretty=format:%s | grep -q 'docs(docs-01,docs-03).*final reconciliation' && git log -1 --name-only --pretty=format: | grep -c '^pltu-tenayan-full-backup' >/dev/null 2>&1 || true; for f in DATABASE_SCHEMA.md DEPLOYMENT_GUIDE.md scripts/check_credentials.sh docs/audit/CREDENTIAL_HYGIENE.md frontend/public/docs/Smart_Blending_AI_Formula.md docs/audit/SMART_BLENDING_FORMULA_AUDIT.md readme.md; do git log -1 --name-only --pretty=format: | grep -qx "$f" || { echo "missing $f from last commit"; exit 1; }; done && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Smart_Blending_AI_Formula.md contains a Phase-3 verification stamp citing CONS-blending-formula.
    - SMART_BLENDING_FORMULA_AUDIT.md exists with a clause-by-clause comparison table and an explicit Outcome heading.
    - readme.md verified — any drift fixed; Plan 03-04's Known Issues pointer line preserved intact.
    - Single inner-repo commit covers all 7 files atomically.
    - Pre-commit credential scanner clean.
  </acceptance_criteria>
  <done>
    Smart Blending formula verified + audit logged; readme verified; final inner commit lands; Phase 3 doc-reconciliation arc complete.
  </done>
</task>

</tasks>

<verification>
After all tasks:
```
cd pltu-tenayan-full-backup
grep -c '^## Duplicate Pair Active Read Targets' DATABASE_SCHEMA.md   # → 1
! grep -E '<TEST_ADMIN_PASSWORD>|MONGO_URL_WITH_CREDS_PATTERN' DEPLOYMENT_GUIDE.md
grep -q 'LOCAL_SETUP\.md.*recovery' DEPLOYMENT_GUIDE.md
test -f docs/audit/SMART_BLENDING_FORMULA_AUDIT.md
grep -q 'CONS-blending-formula' frontend/public/docs/Smart_Blending_AI_Formula.md
bash scripts/check_credentials.sh   # → exit 0
! grep -E '^[[:space:]]*"DEPLOYMENT_GUIDE\.md"' scripts/check_credentials.sh
! grep -E '^[[:space:]]*"API_REFERENCE\.md"' scripts/check_credentials.sh   # cleared by Plan 03-03
```
</verification>

<success_criteria>
- DOCS-03: DATABASE_SCHEMA.md correctly identifies the active read target for all 4 duplicate-pair collections, with code-line evidence.
- DOCS-01 (deployment): DEPLOYMENT_GUIDE.md contains zero inline secrets, references actual ports (8013/3013), cross-links to LOCAL_SETUP §VPS Service Recovery.
- DOCS-01 (formula): Smart_Blending_AI_Formula.md verified against CONS-blending-formula; audit log records outcome.
- DOCS-01 (readme): readme content verified against live system; drift corrected if found.
- Two more credential-scanner exemptions cleared (top-level DEPLOYMENT_GUIDE.md, plus the API_REFERENCE.md cleared by Plan 03-03 — leaving only the `frontend/public/docs/*` mirror copies and the test-file exemptions, all of which are owned by Phase 4).
- One atomic inner-repo commit covers Tasks 1-3 (atomic — they are a single doc-reconciliation deliverable).
- DOCS-01..05 + STAB-04 are now all closed across Plans 03-01..03-05.
</success_criteria>

<output>
After tasks complete, write `.planning/phases/03-documentation-refresh-decision-lock-in/03-05-SUMMARY.md` per standard summary template — record the inner commit SHA, the literal Outcome line from SMART_BLENDING_FORMULA_AUDIT.md (NO DRIFT / MINOR / MAJOR), the 4-row determination table from DATABASE_SCHEMA.md (so Phase-5 can ingest it directly), and the final state of the credential scanner EXCLUDE list (which exemptions remain and who owns them).
</output>
