---
phase: 06-operational-unblocks
plan: 06
type: execute
wave: 4
depends_on: [06-01, 06-02, 06-03, 06-04, 06-05]
files_modified:
  - pltu-tenayan-full-backup/backend/.env
  - pltu-tenayan-full-backup/PHASE6_CUTOVER_RUNBOOK.md
autonomous: false
requirements: [OPS-01, OPS-04]
tags: [cutover, production, openrouter-key-rotation, smoke-test]

must_haves:
  truths:
    - "OPENROUTER_API_KEY rotated into live .env; old LEGACY_LLM_KEY value removed"
    - "uvicorn restarted; /api/health returns 200 after restart"
    - "3 smart-blending smoke tests at GCV 4000/4200/4500 each return parseable JSON with non-empty blend array (OPS-01 SC-1)"
    - "AI Chat UI live smoke: operator creates conversation, sends message, receives AI response, switches conversation, sees persisted history (OPS-04 SC-4)"
    - "Indonesian error toast UX verified by intentionally-invalid-key probe (OPS-02 cross-cut)"
    - "PHASE6_CUTOVER_RUNBOOK.md documents the full procedure with rollback path"
  artifacts:
    - path: "pltu-tenayan-full-backup/PHASE6_CUTOVER_RUNBOOK.md"
      provides: "Operator cutover runbook following Phase 5 MIGRATION_RUNBOOK.md template (Prerequisites / Apply / Smoke / Rollback / Cleanup)"
  key_links:
    - from: "operator action"
      to: "OPENROUTER_API_KEY in .env"
      via: "manual file edit + uvicorn restart"
      pattern: "OPENROUTER_API_KEY=<OPENROUTER_API_KEY_PREFIX>"
    - from: "/api/smart-blending/recommend"
      to: "Live OpenRouter API"
      via: "3 curl smoke tests"
      pattern: "smart-blending/recommend"
---

<objective>
Cut over the Phase-6 OpenRouter migration to production. This plan is NON-AUTONOMOUS — it has 3 human-verify checkpoints in sequence:
1. Operator rotates OPENROUTER_API_KEY into backend/.env, restarts uvicorn, confirms /api/health 200 (per RESEARCH §Runtime State Inventory + LOCAL_SETUP.md §VPS Service Recovery)
2. Operator runs 3 smart-blending smoke tests at target GCV 4000/4200/4500 (OPS-01 SC-1 per D-08)
3. Operator opens the new /ai-chat UI, creates conversation, sends message, verifies AI response renders + history persists across conversation switch (OPS-04 SC-4)

Purpose: Closes OPS-01 (production functional verification) + OPS-04 (live UI smoke). Bridges the code/test work of Plans 01-05 into actual operational health.
Output: PHASE6_CUTOVER_RUNBOOK.md committed; live .env updated by operator; signed-off smoke evidence in 06-06-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-operational-unblocks/06-CONTEXT.md
@.planning/phases/06-operational-unblocks/06-RESEARCH.md
@.planning/phases/06-operational-unblocks/06-VALIDATION.md
@pltu-tenayan-full-backup/MIGRATION_RUNBOOK.md
@pltu-tenayan-full-backup/LOCAL_SETUP.md
</context>

<tasks>

<task type="auto">
  <name>Task 06-06-00: Author PHASE6_CUTOVER_RUNBOOK.md before checkpoints</name>
  <files>pltu-tenayan-full-backup/PHASE6_CUTOVER_RUNBOOK.md</files>
  <read_first>
    - pltu-tenayan-full-backup/MIGRATION_RUNBOOK.md (Phase 5 deliverable — runbook style precedent: Prerequisites / Backup / Apply / Smoke / Rollback / Cleanup)
    - pltu-tenayan-full-backup/LOCAL_SETUP.md §VPS Service Recovery (Phase 3 deliverable for uvicorn restart procedure)
    - 06-CONTEXT.md D-08 (3-GCV smoke contract)
    - 06-RESEARCH.md §Runtime State Inventory lines 938-948 (env-var rename runtime impact)
  </read_first>
  <action>
    Create `pltu-tenayan-full-backup/PHASE6_CUTOVER_RUNBOOK.md` following Phase-5 MIGRATION_RUNBOOK.md's 6-section style. Sections:

    **§0 Pre-Cutover Sanity**
    - Confirm Plans 06-01..06-05 SUMMARY files all present at .planning/phases/06-operational-unblocks/
    - Confirm `AI_FAKE=1 pytest backend/tests/ -x -q` exits 0 (full suite green)
    - Confirm grep gates: zero legacy-ai-sdk, zero LEGACY_LLM_KEY in inner-repo markdown, zero ai_conversations references
    - Confirm operator has obtained an OPENROUTER_API_KEY from https://openrouter.ai/keys (account funded; verify dashboard credit balance > $5 — covers ~5,500 smart-blending calls at $0.0009/call per RESEARCH §Focus 1 cost estimate)

    **§1 Prerequisites**
    - VPS access (SSH 103.150.197.225)
    - Operator's OPENROUTER_API_KEY (format starts with "sk-or-")
    - Optional: `screen` or `tmux` for safe uvicorn restart

    **§2 Backup**
    - `cp pltu-tenayan-full-backup/backend/.env pltu-tenayan-full-backup/backend/.env.bak-$(date +%Y%m%d-%H%M%S)` (preserves old LEGACY_LLM_KEY value if rollback needed)

    **§3 Apply (Operator manual steps)**
    1. Edit `pltu-tenayan-full-backup/backend/.env`:
       - Set `OPENROUTER_API_KEY=<OPENROUTER_API_KEY>` (replace placeholder set by Plan 06-01)
       - Confirm `OPENROUTER_DEFAULT_MODEL=openai/gpt-4o-mini` is present
       - Ensure NO legacy `LEGACY_LLM_KEY=` line remains
    2. Stop the running uvicorn process per LOCAL_SETUP.md §VPS Service Recovery:
       - `pkill -f "uvicorn.*server:app" || true`
       - Wait 2s; verify `pgrep -f uvicorn` returns nothing
    3. Start uvicorn fresh:
       - From `pltu-tenayan-full-backup/backend/`: `.venv/bin/uvicorn server:app --host 0.0.0.0 --port 8013 --reload &` (background) — OR per LOCAL_SETUP.md systemd recipe if applicable
    4. Wait 5s for warmup.

    **§4 Smoke Tests (3 checkpoints below — operator verifies one by one)**
    See task 06-06-01..03.

    **§5 Rollback (if any smoke fails)**
    - `cp backend/.env.bak-* backend/.env` (restore previous env)
    - `pkill -f uvicorn; restart per LOCAL_SETUP.md` — Phase-6 backend reverts to pre-cutover state
    - Roll back inner-repo commits via `git revert <plan-06-01..06-05 commit shas>` if necessary
    - Documentation: append rollback note to 06-06-SUMMARY.md

    **§6 Cleanup**
    - 48h observation window — operator monitors `/var/log/uvicorn.log` for LLMUnavailableError frequency
    - After 48h with zero 503s reaching users → delete `.env.bak-*` files
    - Note OpenRouter monthly spend via dashboard
  </action>
  <acceptance_criteria>
    - pltu-tenayan-full-backup/PHASE6_CUTOVER_RUNBOOK.md exists with 7 sections (§0..§6) — matches Phase-5 MIGRATION_RUNBOOK.md template
    - grep -c "OPENROUTER_API_KEY" PHASE6_CUTOVER_RUNBOOK.md >= 3 (Apply + Rollback + Cleanup refs)
    - grep -c "GCV 4000\|GCV 4200\|GCV 4500\|target_gcv" PHASE6_CUTOVER_RUNBOOK.md >= 3 (D-08 smoke contract verbatim)
  </acceptance_criteria>
  <verify>
    <automated>ls pltu-tenayan-full-backup/PHASE6_CUTOVER_RUNBOOK.md && grep -c "OPENROUTER_API_KEY" pltu-tenayan-full-backup/PHASE6_CUTOVER_RUNBOOK.md | awk '{ exit ($1 >= 3) ? 0 : 1 }'</automated>
  </verify>
  <done>Runbook committed; all 6 sections present; rollback path documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 06-06-01: Operator rotates OPENROUTER_API_KEY + restarts uvicorn + verifies /api/health</name>
  <what-built>
    Plan 06-01 created backend/.env with `OPENROUTER_API_KEY=` (empty placeholder) and `OPENROUTER_DEFAULT_MODEL=openai/gpt-4o-mini`. Plans 06-01..05 also rewired all AI code paths to use the OpenRouter client through the existing AIClient Protocol seam. This checkpoint completes the rotation by injecting the real key and restarting the service.
  </what-built>
  <how-to-verify>
    Follow PHASE6_CUTOVER_RUNBOOK.md §2 + §3:
    1. Backup current .env: `cp pltu-tenayan-full-backup/backend/.env pltu-tenayan-full-backup/backend/.env.bak-$(date +%Y%m%d-%H%M%S)`
    2. Edit `pltu-tenayan-full-backup/backend/.env` and set `OPENROUTER_API_KEY=<OPENROUTER_API_KEY>` (key from https://openrouter.ai/keys dashboard)
    3. Confirm `OPENROUTER_DEFAULT_MODEL=openai/gpt-4o-mini` line is present
    4. Confirm no `LEGACY_LLM_KEY=` line remains: `grep -c "LEGACY_LLM_KEY" pltu-tenayan-full-backup/backend/.env` must equal 0
    5. Stop uvicorn: `pkill -f "uvicorn.*server:app" || true; sleep 2; pgrep -f uvicorn || echo "uvicorn stopped"`
    6. Restart uvicorn per LOCAL_SETUP.md §VPS Service Recovery (cwd = pltu-tenayan-full-backup/backend/, .venv activated, port 8013)
    7. Wait ~5s for warmup
    8. Run: `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8013/api/health`
       Expected output: `200`
    9. Tail logs for 30s to confirm no `ImportError`, no `NameError: legacy-ai-sdk`, no startup tracebacks

    Expected outcomes:
    - `.env` contains real OPENROUTER_API_KEY, openai/gpt-4o-mini, zero LEGACY_LLM_KEY references
    - uvicorn process is running (`pgrep -f uvicorn` returns a PID)
    - /api/health returns 200
    - Log tail shows no startup errors

    Maps to 06-VALIDATION.md row 06-06-01.
  </how-to-verify>
  <resume-signal>Reply "approved" + paste the /api/health curl output + uvicorn PID. OR "issues: <description>" with log excerpt; if startup fails, follow PHASE6_CUTOVER_RUNBOOK.md §5 Rollback.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 06-06-02: Operator runs 3 smart-blending smoke tests at GCV 4000/4200/4500 (OPS-01 SC-1)</name>
  <what-built>
    Plans 06-01 (OpenRouter client + retry) + 06-02 (aggregation field fixes) together restore Smart Blending AI against live data. This checkpoint verifies the full path end-to-end against the real OpenRouter API at the 3 target GCV values that ROADMAP §Phase 6 SC-1 + 06-CONTEXT.md D-08 lock as the acceptance contract.
  </what-built>
  <how-to-verify>
    For each of 3 target GCV values (4000, 4200, 4500):
    1. Obtain a fresh admin JWT (see memory/test_credentials.md for admin login, NEVER paste credentials into the runbook):
       ```
       TOKEN=$(curl -s -X POST http://localhost:8013/api/auth/login \
         -H "Content-Type: application/json" \
         -d "$(cat memory/test_credentials.md | grep admin_login_body)" | jq -r .access_token)
       ```
    2. Call smart-blending:
       ```
       curl -s -X POST http://localhost:8013/api/smart-blending/recommend \
         -H "Authorization: Bearer $TOKEN" \
         -H "Content-Type: application/json" \
         -d '{"target_gcv": 4000, "max_ash": 12, "max_sulphur": 0.7, "max_tm": 30}' \
         | jq .
       ```
       Repeat with `"target_gcv": 4200` then `"target_gcv": 4500`.
    3. For EACH of the 3 responses, verify:
       - HTTP 200 returned
       - Response body is valid JSON (jq parses without error)
       - Response includes `ai_recommendation` key (per CONS-blending-ai-output)
       - `ai_recommendation.blend` exists and is a non-empty array

    Expected outcomes: 3/3 calls return 200 with parseable JSON + non-empty blend array.

    If any of the 3 calls returns 503 with detail "Layanan AI sementara tidak tersedia. Silakan coba lagi sebentar.":
    - This is expected error-UX behaviour, NOT a green smoke result
    - Check OpenRouter dashboard credit balance + API key validity
    - If key OK and credit OK → re-attempt (transient upstream); if still failing → rollback per PHASE6_CUTOVER_RUNBOOK.md §5

    Capture the 3 jq outputs for the SUMMARY.

    Maps to 06-VALIDATION.md row 06-06-02 (OPS-01 SC-1).
  </how-to-verify>
  <resume-signal>Reply "approved" + paste a one-line summary per GCV (e.g., "GCV 4000: 200, blend=[...5 items...]; GCV 4200: 200, blend=[...4 items...]; GCV 4500: 200, blend=[...6 items...]"). OR "issues: <description>" with full jq output and HTTP status; if 503 persists, run rollback per runbook §5.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 06-06-03: Operator AI Chat UI live smoke (OPS-04 SC-4)</name>
  <what-built>
    Plans 06-04 + 06-05 added the 4 /api/ai/conversations/* endpoints + 8 frontend chat components. This checkpoint verifies the end-to-end UX under live OpenRouter.
  </what-built>
  <how-to-verify>
    1. Open a browser to http://localhost:3013/login (or live VPS URL)
    2. Log in as the test admin user (credentials per memory/test_credentials.md, never paste here)
    3. Navigate to /ai-chat via the "Riwayat Percakapan AI" nav link (left sidebar, adjacent to "AI Intelligence")
    4. EXPECTED — empty state OR existing conversations:
       - If user has no prior conversations: sidebar shows "Percakapan Saya" label + "Percakapan Baru" button + main panel shows "Belum ada percakapan" + "Mulai Percakapan" CTA
       - If user has prior conversations (e.g., the existing 4 production session_ids belonging to user b94bfaa5 or c837c681): sidebar shows them, main panel shows the most recent conversation's messages
    5. Click "Percakapan Baru" — EXPECTED: new conversation appears at top of sidebar with title "Percakapan tanpa judul"
    6. Click into the new conversation; textarea visible with placeholder "Ketik pesan..."
    7. Type "Berapa total stok batubara saat ini?" and press Enter
    8. EXPECTED:
       - User message bubble appears immediately (right-aligned, cyan)
       - "AI sedang mengetik..." indicator appears below
       - Within ~3-8s: AI response bubble appears (left-aligned, glass-card)
       - AI response is in Indonesian (since system prompt is Indonesian-aware)
       - Sidebar updates conversation title to first 50 chars of user message
       - last_message_at timestamp updates to "baru saja"
    9. Click on a DIFFERENT conversation in the sidebar — EXPECTED: main panel switches; messages from that conversation render; history persists
    10. Click back on the new conversation — EXPECTED: the user+AI exchange from step 8 still visible (persisted in ai_chat_history)

    Indonesian error toast smoke (OPS-02 cross-cut):
    11. Temporarily change OPENROUTER_API_KEY in .env to an invalid value (e.g., "sk-or-invalid")
    12. Restart uvicorn
    13. Try sending another chat message
    14. EXPECTED: sonner toast appears top-right with title "Layanan AI tidak tersedia", description "Layanan AI sementara tidak tersedia. Silakan coba lagi sebentar.", "Coba lagi" button visible; optimistic user message removed from view
    15. Restore the real key in .env and restart uvicorn
    16. Click "Coba lagi" OR resend the message — EXPECTED: AI response succeeds

    Expected outcomes: All 16 sub-steps observed; no raw "BadGatewayError" or English error message reaches the user.

    Maps to 06-VALIDATION.md row 06-06-03 (OPS-04 SC-4 + OPS-02 cross-cut).
  </how-to-verify>
  <resume-signal>Reply "approved" with: (a) screenshot or text description of each of the 4 substantive states (empty-state, new-conversation, AI-response, conversation-switch, Indonesian-toast); (b) note any UI bugs (likely candidates: scroll position, lazy-load, focus management — log under "follow-up" not "blocker"). OR "issues: <description>" with screenshot + browser console errors; revert per runbook §5 if backend cannot serve /api/ai/conversations/*.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator -> backend/.env | Manual filesystem write; sensitive secret material |
| backend process -> live OpenRouter (production keyholder) | First real outbound call with real credits |
| Operator browser -> live FastAPI :8013 | Production smoke surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Information Disclosure | OPENROUTER_API_KEY accidentally committed | mitigate | .env is in .gitignore (verified by Phase-2 CREDENTIAL_HYGIENE pre-commit hook); runbook never asks operator to paste the key into anything but the local .env file |
| T-06-06-02 | Denial of Service | uvicorn restart causes prod downtime window | mitigate | LOCAL_SETUP.md §VPS Service Recovery documented procedure; restart window ~5-10s; operator does this during planned cutover, not in-flight workday |
| T-06-06-03 | Tampering | Wrong key value rotated -> total AI surface broken | mitigate | Rollback path in PHASE6_CUTOVER_RUNBOOK.md §5: `cp .env.bak-* .env; restart uvicorn`; restores previous state in seconds |
| T-06-06-04 | Repudiation | No audit trail of cutover time / actor | accept | Operator logs the timestamp + their identity in 06-06-SUMMARY.md; outside CI/CD audit scope this milestone |
| T-06-06-05 | Elevation of Privilege | Operator pastes admin credentials into chat (during smoke) | mitigate | Runbook step 11 explicitly says "never paste credentials"; smoke message is generic ("Berapa total stok batubara saat ini?") |
</threat_model>

<verification>
- Manual verification per the 3 checkpoint tasks
- PHASE6_CUTOVER_RUNBOOK.md committed in inner repo
- 06-06-SUMMARY.md records operator timestamp + 3 smoke outputs + UI smoke notes
</verification>

<success_criteria>
1. /api/health returns 200 after key rotation + uvicorn restart
2. 3/3 smart-blending smoke tests pass at GCV 4000/4200/4500 (OPS-01 SC-1 LOCKED)
3. AI Chat UI live smoke passes (empty state -> create -> send -> response -> switch -> persistence) (OPS-04 SC-4 LOCKED)
4. Indonesian error toast verified by intentional invalid-key probe (OPS-02 cross-cut)
5. Rollback procedure exercised at least once on paper (no actual rollback unless smoke fails)
</success_criteria>

<output>
Create .planning/phases/06-operational-unblocks/06-06-SUMMARY.md with: cutover timestamp, operator identifier, /api/health result, 3 smoke-test jq outputs, AI Chat UI smoke walkthrough notes, Indonesian-toast verification screenshot/description, and final phase closure note "Phase 6 cutover complete — OPS-01..04 satisfied; ROADMAP §Phase 6 advanceable to checked".

## Commit Boundary
- Inner repo: PHASE6_CUTOVER_RUNBOOK.md, backend/.env (operator action, not Claude — .env is gitignored so no actual commit), .env.bak-* (gitignored)
- Outer repo: 06-06-SUMMARY.md, STATE.md, ROADMAP.md (mark Phase 6 [x] after operator approves)
</output>
