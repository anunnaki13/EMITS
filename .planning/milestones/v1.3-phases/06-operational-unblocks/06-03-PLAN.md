---
phase: 06-operational-unblocks
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - pltu-tenayan-full-backup/backend/tests/fixtures/excel/regression/loading_sample.xlsx
  - pltu-tenayan-full-backup/backend/tests/fixtures/excel/regression/unloading_sample.xlsx
  - pltu-tenayan-full-backup/backend/tests/fixtures/excel/regression/lab_internal_sample.xlsx
  - pltu-tenayan-full-backup/backend/tests/test_upload_excel.py
  - pltu-tenayan-full-backup/backend/services/coa_reconciliation.py
  - pltu-tenayan-full-backup/docs/audit/EXCEL_PARSER_VERIFICATION.md
autonomous: true
requirements: [OPS-03]
tags: [excel-parser, regression-fixtures, coa-reconciliation]

must_haves:
  truths:
    - "Each of the 3 proxy xlsx files round-trips through parse_coa_excel() with row count matching expected subset and 3 deterministic field values matching"
    - "Sanitized regression fixtures contain dummy supplier names + dummy contract numbers (no real PII)"
    - "Any parser discrepancy surfaced during verification is fixed in services/coa_reconciliation.py"
  artifacts:
    - path: "pltu-tenayan-full-backup/backend/tests/fixtures/excel/regression/loading_sample.xlsx"
      provides: "Sanitized loading regression fixture (<=50 rows)"
    - path: "pltu-tenayan-full-backup/backend/tests/fixtures/excel/regression/unloading_sample.xlsx"
      provides: "Sanitized unloading regression fixture (<=50 rows)"
    - path: "pltu-tenayan-full-backup/backend/tests/fixtures/excel/regression/lab_internal_sample.xlsx"
      provides: "Sanitized lab_internal regression fixture (<=50 rows)"
    - path: "pltu-tenayan-full-backup/backend/tests/test_upload_excel.py"
      provides: "3 new COA regression tests"
      contains: "test_coa_regression_loading|test_coa_regression_unloading|test_coa_regression_internal"
  key_links:
    - from: "tests/test_upload_excel.py"
      to: "services/coa_reconciliation.parse_coa_excel"
      via: "POST /api/coa-reconciliation/upload"
      pattern: "parse_coa_excel|coa-reconciliation/upload"
---

<objective>
Close OPS-03 by verifying the Excel parser against the 3 real production xlsx samples (Loading/Unloading/Lab_Internal). All 3 map to parse_coa_excel() in services/coa_reconciliation.py via POST /api/coa-reconciliation/upload (per RESEARCH Focus 4 — NOT 4 separate vessel/barge/trucking parsers). Sanitize each xlsx to a max-50-row subset, commit as regression fixtures, and add 3 round-trip pytest tests using deterministic anchors (Shipment 555 / PT PLN BB-SAE / DS_MT 5626.021) per RESEARCH Focus 8.

Purpose: Closes OPS-03. The literal total-penerimaan.xlsx is NOT yet provided by the operator (deferred to Phase 7 per D-11); these 3 proxies satisfy OPS-03 because all 4 modes consolidate into the COA parser.
Output: 3 sanitized xlsx fixtures + 3 green pytest tests + EXCEL_PARSER_VERIFICATION.md discrepancy log.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-operational-unblocks/06-CONTEXT.md
@.planning/phases/06-operational-unblocks/06-RESEARCH.md
@.planning/phases/06-operational-unblocks/06-VALIDATION.md
@pltu-tenayan-full-backup/backend/services/coa_reconciliation.py
@pltu-tenayan-full-backup/Loading.xlsx
@pltu-tenayan-full-backup/Unloading.xlsx
@pltu-tenayan-full-backup/Lab_Internal.xlsx
</context>

<tasks>

<task type="auto">
  <name>Task 06-03-01: Sanitize 3 real xlsx files to regression fixtures (max 50 rows each)</name>
  <files>
    pltu-tenayan-full-backup/backend/tests/fixtures/excel/regression/loading_sample.xlsx,
    pltu-tenayan-full-backup/backend/tests/fixtures/excel/regression/unloading_sample.xlsx,
    pltu-tenayan-full-backup/backend/tests/fixtures/excel/regression/lab_internal_sample.xlsx
  </files>
  <read_first>
    - 06-RESEARCH.md Focus 8 lines 671-716 (file inventory, deterministic anchors, sanitization rule)
    - 06-CONTEXT.md D-11 + D-13 (proxy selection + sanitization rule)
    - pltu-tenayan-full-backup/backend/services/coa_reconciliation.py:62 (parse_coa_excel)
  </read_first>
  <action>
    Per D-13. Write a Python one-off sanitization script (do NOT commit the script; it is a build tool). The script must:

    1. Use openpyxl (already in requirements) to open each of Loading.xlsx, Unloading.xlsx, Lab_Internal.xlsx.
    2. Copy header row + first 50 data rows (rows 1-51 in 1-indexed openpyxl notation).
    3. Apply sanitization per RESEARCH Focus 8:
       - Replace real supplier names (PT PLN BB-SAE, PT KARYA BUMI BARATAMA, etc.) with deterministic dummies of the form "PT DEMO SUPPLIER N" where N is a sequential integer assigned per unique supplier in encountered order.
       - Replace real contract numbers in columns NO.COA and NO.COW with "COA-DEMO-NNNN" (NNNN = zero-padded row index).
       - KEEP numeric quality values unchanged (GCV, TM, ASH, TS, TVM, FC) — needed for parser verification.
       - KEEP shipment numbers unchanged (join key for COA merging; anchor row Shipment 555 verified in RESEARCH Focus 8).
       - KEEP tugboat/barge names + surveyor names unchanged (organizational identifiers, not PII per CONS-pii). If security checker flags, replace with "DEMO TB N" / "DEMO BG N" / "SURVEYOR DEMO N" using same pattern.
       - KEEP date columns unchanged.
    4. Save sanitized workbooks to backend/tests/fixtures/excel/regression/{loading,unloading,lab_internal}_sample.xlsx.

    After sanitization, verify each output xlsx via openpyxl:
    - max_row attribute is at most 51 (1 header + max 50 data)
    - Row 2 still has Shipment=555 (the deterministic anchor row preserved per RESEARCH Focus 8)
    - Supplier column for at least row 2 matches the pattern "PT DEMO SUPPLIER" (sanitized)
    - No real supplier substrings ("PT PLN BB", "PT KARYA BUMI") remain in the workbook text content
  </action>
  <acceptance_criteria>
    - File count: ls backend/tests/fixtures/excel/regression/*.xlsx returns exactly 3 files
    - Each xlsx max_row attribute is at most 51 (verified via openpyxl load_workbook)
    - Row 2 of each file has Shipment field equal to 555 (anchor row preserved)
    - Grep over all sheet cells: zero matches for "PT PLN BB" or "PT KARYA BUMI" in any of the 3 sanitized files
    - Maps to 06-VALIDATION.md row 06-03-04
  </acceptance_criteria>
  <verify>
    <automated>cd pltu-tenayan-full-backup && .venv/bin/python -c "from openpyxl import load_workbook; import os; files=[f'backend/tests/fixtures/excel/regression/{n}_sample.xlsx' for n in ('loading','unloading','lab_internal')]; [print(f, load_workbook(f).active.max_row) for f in files]; assert all(os.path.exists(f) and load_workbook(f).active.max_row <= 51 for f in files), 'fixture missing or oversized'"</automated>
  </verify>
  <done>3 sanitized fixtures committed; all verifications pass; deterministic anchor (Shipment 555) intact; zero real supplier name leakage.</done>
</task>

<task type="auto">
  <name>Task 06-03-02: Add 3 COA regression tests to test_upload_excel.py</name>
  <files>pltu-tenayan-full-backup/backend/tests/test_upload_excel.py</files>
  <read_first>
    - pltu-tenayan-full-backup/backend/tests/test_upload_excel.py (Phase-4 existing tests; extend pattern)
    - pltu-tenayan-full-backup/backend/services/coa_reconciliation.py (parse_coa_excel signature)
    - 06-RESEARCH.md Focus 8 lines 683-707 (deterministic anchor for round-trip assertions)
    - 06-VALIDATION.md rows 06-03-01..03
  </read_first>
  <action>
    Extend the existing pytest module (Phase 4 created it) with 3 new tests. Use parametrize OR 3 separate functions — separate functions are clearer for VALIDATION.md row alignment.

    Approach (preferred): direct unit test of parse_coa_excel() — read fixture file via `open(path, "rb").read()` into bytes, call `parse_coa_excel(file_contents, source_type)` synchronously, assert on the returned list of dicts. This avoids needing the full FastAPI test backend.

    Fallback (if parse_coa_excel requires multi-file merging): POST all 3 fixtures via test_client to /api/coa-reconciliation/upload then query the test DB for inserted records.

    Test 1 — test_coa_regression_loading:
    - Load backend/tests/fixtures/excel/regression/loading_sample.xlsx as bytes.
    - Call parse_coa_excel(bytes, "loading").
    - Assert: returns list of at most 50 records (sanitized fixture size).
    - Assert: record where shipment=="555" exists.
    - Assert: that record's ds_mt equals 5626.021 (deterministic anchor per RESEARCH Focus 8 line 700).

    Test 2 — test_coa_regression_unloading:
    - Same pattern, fixture = unloading_sample.xlsx, source_type="unloading".
    - Assert: shipment=="555" record exists.
    - Assert: that record has surveyor field that does NOT equal raw "PT GEOSERVICES" — must be sanitized to "SURVEYOR DEMO" pattern OR unchanged if checker allowed.

    Test 3 — test_coa_regression_internal:
    - Same pattern, fixture = lab_internal_sample.xlsx, source_type="internal".
    - Assert: shipment=="555" record exists.
    - Assert: numeric fields gcv_arb/tm_arb/ash_arb/ts_arb are not None for the anchor row (parser correctly maps quality columns).

    For each test, if parse_coa_excel raises OR a deterministic anchor field fails the assertion: log the discrepancy in `pltu-tenayan-full-backup/docs/audit/EXCEL_PARSER_VERIFICATION.md` with file:line of the parser bug, then fix `services/coa_reconciliation.py` (most likely a header-normalization edge case in `clean_column_name()` per RESEARCH Focus 4 line 544 — verbatim newline-stripping behaviour). Re-run until all 3 tests green.
  </action>
  <acceptance_criteria>
    - grep -c "def test_coa_regression_loading\|def test_coa_regression_unloading\|def test_coa_regression_internal" tests/test_upload_excel.py == 3
    - All 3 new tests pass under AI_FAKE=1 pytest tests/test_upload_excel.py -x -q -k coa_regression
    - Phase-4 existing test_upload_excel.py tests still GREEN (no regression on original 4-mode test set)
    - If any fix applied: EXCEL_PARSER_VERIFICATION.md exists with discrepancy entry + fix description
    - Maps to 06-VALIDATION.md rows 06-03-01, 06-03-02, 06-03-03
  </acceptance_criteria>
  <verify>
    <automated>cd pltu-tenayan-full-backup/backend && AI_FAKE=1 .venv/bin/pytest tests/test_upload_excel.py -x -q</automated>
  </verify>
  <done>3 new tests green; deterministic anchors verified across all 3 modes; any parser discrepancy fixed and logged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-uploaded xlsx -> parse_coa_excel | Untrusted file input crosses into parser |
| committed fixture xlsx -> git repo | Sanitized data must not leak real supplier/contract identifiers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Information Disclosure | PII leak in committed regression fixtures | mitigate | Sanitization replaces real supplier names + contract numbers with deterministic dummies per RESEARCH Focus 8; row count capped at 50 to limit blast radius; grep gate enforces zero real-name leakage |
| T-06-03-02 | Tampering | Parser drift on future xlsx format changes | mitigate | 3 regression tests assert deterministic anchor (Shipment 555 / DS_MT 5626.021) — any silent parser drift breaks the suite |
| T-06-03-03 | Denial of Service | Malformed xlsx triggers parser exception (in production) | accept | parse_coa_excel is wrapped in HTTPException(400) by the upload endpoint; out-of-scope for OPS-03 |
</threat_model>

<verification>
- AI_FAKE=1 pytest tests/test_upload_excel.py -x -q exits 0 (existing + 3 new tests green)
- 3 fixture xlsx files exist at the canonical paths
- Zero real supplier name leakage in committed fixtures
- EXCEL_PARSER_VERIFICATION.md present at pltu-tenayan-full-backup/docs/audit/ (may be brief "no discrepancies" if all tests pass first try)
</verification>

<success_criteria>
1. 3 sanitized xlsx fixtures committed at backend/tests/fixtures/excel/regression/
2. 3 round-trip tests assert deterministic anchor row (Shipment 555) across loading/unloading/internal modes
3. Any parser discrepancy surfaced and fixed in services/coa_reconciliation.py
4. EXCEL_PARSER_VERIFICATION.md logs the verification outcome
5. Phase-4 existing test_upload_excel.py tests still pass
</success_criteria>

<output>
Create .planning/phases/06-operational-unblocks/06-03-SUMMARY.md with: per-mode row count + anchor-field verification table; list of any parser fixes applied with line numbers; final pytest output.

## Commit Boundary
- Inner repo: 3 .xlsx fixtures, tests/test_upload_excel.py, services/coa_reconciliation.py (if fixed), docs/audit/EXCEL_PARSER_VERIFICATION.md
- Outer repo: 06-03-SUMMARY.md, STATE.md
</output>
